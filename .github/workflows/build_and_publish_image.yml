name: Build & Publish Image

on:
    push:
        tags: ["v*"] # only versioned releases
    pull_request:
        branches: [main] # publish sha-only image for PR testing
    workflow_dispatch: # manual trigger

permissions:
    contents: read
    packages: write
    id-token: write

jobs:
    build:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                # PRs build only 3.13 for speed; releases build all supported versions
                python-version: ${{ github.event_name == 'pull_request' && fromJSON('["3.13"]') || fromJSON('["3.11", "3.12", "3.13"]') }}
        env:
            IMAGE_NAME: hassette
            REGISTRY: ghcr.io
            OWNER: ${{ github.repository_owner }}
            PYTHON_VERSION: ${{ matrix.python-version }}
            IS_STABLE_RELEASE: "false"
        steps:
            - uses: actions/checkout@v4

            - name: Set up QEMU
              uses: docker/setup-qemu-action@v3

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v3

            - name: Login to GHCR
              uses: docker/login-action@v3
              with:
                  registry: ${{ env.REGISTRY }}
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Install uv
              uses: astral-sh/setup-uv@v6
              with:
                  activate-environment: true
                  enable-cache: true

            - name: Classify release tag
              if: github.event_name == 'push'
              run: uv run ./tools/check_tag_version.py --ref-name ${{ github.ref_name }} --project-version $(uv version --short)

            - name: Docker metadata
              id: meta
              uses: docker/metadata-action@v5
              with:
                  images: ${{ env.REGISTRY }}/${{ env.OWNER }}/${{ env.IMAGE_NAME }}
                  flavor: |
                      latest=false
                      suffix=-py${{ env.PYTHON_VERSION }}
                  tags: |
                      type=sha,prefix=sha-,format=short
                      type=pep440,pattern={{version}},enable=${{ github.event_name == 'push' }}
                      type=raw,value=latest,enable=${{ github.event_name == 'push' && env.IS_STABLE_RELEASE == 'true' }}

            - name: Build & Push
              if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
              uses: docker/build-push-action@v6
              with:
                  context: .
                  push: true
                  tags: ${{ steps.meta.outputs.tags }}
                  labels: ${{ steps.meta.outputs.labels }}
                  cache-from: type=gha
                  cache-to: type=gha,mode=max
                  platforms: linux/amd64,linux/arm64
                  build-args: |
                      PYTHON_VERSION=${{ matrix.python-version }}
