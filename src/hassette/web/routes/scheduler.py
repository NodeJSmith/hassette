# TOMBSTONE: The /scheduler/jobs global live endpoint was removed in spec 2039 (WP02).
# Live job state is now exposed via /telemetry/app/{app_key}/jobs (enriched with heap data).
# ScheduledJobResponse was deleted from web/models.py at the same time.
