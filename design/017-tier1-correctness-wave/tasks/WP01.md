---
work_package_id: "WP01"
title: "Extract shared ListenerSummary-to-ListenerWithSummary conversion helper"
lane: "planned"
plan_section: "Fix 1: Bus endpoint model alignment (#408 + #407)"
depends_on: []
---

## Objectives & Success Criteria

- A `to_listener_with_summary(ls: ListenerSummary) -> ListenerWithSummary` function exists in `src/hassette/web/telemetry_helpers.py`
- `routes/telemetry.py` calls the extracted helper instead of its inline 30-line mapping
- All existing tests pass — no behavior change, pure refactor
- The helper is imported and available for WP02 to use in the bus route

## Subtasks

1. Read `src/hassette/web/routes/telemetry.py:96-126` — the inline ListenerSummary→ListenerWithSummary mapping
2. Extract the mapping into a new `to_listener_with_summary(ls: ListenerSummary) -> ListenerWithSummary` function in `src/hassette/web/telemetry_helpers.py` (where `format_handler_summary` already lives)
3. Replace the inline mapping in `routes/telemetry.py:96-126` with a call to the extracted helper: `[to_listener_with_summary(ls) for ls in listeners]`
4. Update imports in `routes/telemetry.py`
5. Run existing tests to confirm no regressions: `uv run pytest tests/integration/test_telemetry_query_service.py tests/integration/test_web_api.py -v`

## Test Strategy

No new tests — this is a pure extraction refactor. Existing integration tests in `tests/integration/test_web_api.py` and `tests/integration/test_telemetry_query_service.py` cover the telemetry endpoint's behavior and will catch any extraction errors.

## Review Guidance

- Verify the extracted function is a 1:1 copy of the inline mapping — no logic changes
- Verify `routes/telemetry.py` calls the helper and produces identical output
- Verify `format_handler_summary` is called inside the helper, not left inline

## Activity Log

- 2026-03-24T12:00:00Z — system — lane=planned — WP created
