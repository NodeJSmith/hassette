---
work_package_id: "WP02"
title: "Fix bus endpoint: retire ListenerMetricsResponse, use ListenerWithSummary"
lane: "planned"
plan_section: "Fix 1: Bus endpoint model alignment (#408 + #407)"
depends_on: ["WP01"]
---

## Objectives & Success Criteria

- `/bus/listeners` endpoint returns `list[ListenerWithSummary]` with all fields correctly populated
- `ListenerMetricsResponse` class is deleted from `src/hassette/web/models.py`
- `once` field in the bus endpoint response is `int`, not `bool`
- `pyright: ignore[reportReturnType]` suppressions removed from `routes/bus.py`
- `gather_all_listeners` passes `session_id` for current-session scoping
- `frontend/openapi.json` regenerated to reflect the schema change
- Integration test confirms the endpoint returns `ListenerWithSummary` schema

## Subtasks

1. In `src/hassette/web/routes/bus.py`: change `response_model` to `list[ListenerWithSummary]`, import `to_listener_with_summary` from `telemetry_helpers`, convert results from `gather_all_listeners` and `telemetry.get_listener_summary` using the helper
2. In `src/hassette/web/routes/bus.py`: add `session_id=safe_session_id(runtime)` to the `gather_all_listeners` call path. Import `safe_session_id` from `telemetry_helpers`
3. Remove `pyright: ignore[reportReturnType]` comments from `routes/bus.py`
4. In `src/hassette/web/utils.py`: update `gather_all_listeners` to accept and pass through `session_id` parameter
5. Delete `ListenerMetricsResponse` from `src/hassette/web/models.py` (lines 109-132)
6. Remove any imports of `ListenerMetricsResponse` (grep codebase)
7. Regenerate `frontend/openapi.json` — run the FastAPI app and fetch `/api/openapi.json`
8. Update `frontend/src/api/ws-types.ts` if it references `ListenerMetricsResponse`
9. Write integration test in `tests/integration/test_bus.py`: `test_get_listener_metrics_returns_listener_with_summary` — hit `/bus/listeners`, assert response matches `ListenerWithSummary` schema, assert `once` is `int`
10. Run `uv run pyright` to verify no type errors remain
11. Run full test suite: `uv run nox -s dev`

## Test Strategy

- **New test**: `tests/integration/test_bus.py::test_get_listener_metrics_returns_listener_with_summary` — create test app with a listener, hit `/bus/listeners`, assert response contains `ListenerWithSummary` fields including `handler_summary`, `source_location`, and `once` as int
- **Existing tests**: `tests/integration/test_web_api.py` covers the web API surface and will catch regressions
- **Type check**: `uv run pyright` must pass clean (no `pyright: ignore` suppressions)

## Review Guidance

- Verify `ListenerMetricsResponse` is fully removed — no references remain anywhere
- Verify `gather_all_listeners` now passes `session_id` — bus and telemetry routes should return same-scoped data
- Verify `openapi.json` was regenerated (not hand-edited) — check that the `ListenerMetricsResponse` schema is gone and `ListenerWithSummary` appears in the bus endpoint's response schema
- Verify no `pyright: ignore` suppressions remain in `routes/bus.py`

## Activity Log

- 2026-03-24T12:00:00Z — system — lane=planned — WP created
