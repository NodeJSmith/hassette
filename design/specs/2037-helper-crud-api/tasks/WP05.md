---
depends_on:
- WP02
- WP04
lane: done
title: Extend RecordingApi with helper seed surface and regenerate recording facade
work_package_id: WP05
---

## Objectives & Success Criteria

Make the 35 new `Api` methods first-class citizens of the test harness. `RecordingApi` gains a `helper_definitions` seed dict, 32 CRUD method implementations that mutate the seed dict, and 3 counter action methods that record `ApiCall` directly. `AppTestHarness` gains a public `seed_helper(record)` method. `ApiProtocol` is extended with all 35 method signatures. `_KNOWN_READ_METHODS` in the parity test gains the 8 `list_*` names. The recording sync facade is regenerated.

This WP is where the test-harness parity gates are enforced. **Co-commit rule**: the `_KNOWN_READ_METHODS` update and the `list_*` additions to `RecordingApi` MUST land in the same commit, otherwise the parity test fails with a confusing error.

Concrete outcomes:

- `src/hassette/test_utils/recording_api.py` has:
  - Module-level `_SUPPORTED_HELPER_DOMAINS: frozenset[str]` — single source of truth for the 8 domains.
  - Module-level `_RECORD_TYPE_TO_DOMAIN: dict[type, str]` — hand-maintained lookup from Record class → domain name (with comment explaining the hand-maintained nature per round-3 finding F6).
  - Module-level `_slugify_helper_name(name: str) -> str` helper — HARNESS-ONLY; NOT in `api.py`. Uses either a local implementation or an existing hassette slugify utility if one exists (check via `grep -r slugify src/hassette/`).
  - `RecordingApi.helper_definitions: dict[str, dict[str, Any]]` attribute, initialized in `__init__` to `{d: {} for d in _SUPPORTED_HELPER_DOMAINS}`.
  - 8 `list_*` methods that return `cast(list[{Domain}Record], list(self.helper_definitions["{domain}"].values()))`.
  - 8 `create_*` methods that record an `ApiCall`, construct a `{Domain}Record` with `id=_slugify_helper_name(params.name)` and other fields from `params.model_dump(exclude_unset=True)`, insert into `helper_definitions`, return the new record.
  - 8 `update_*` methods that record an `ApiCall`, raise `FailedMessageError(code="not_found")` if `helper_id` not in seed dict, otherwise use `existing.model_copy(update=params.model_dump(exclude_unset=True))` to build the new record, replace in-place, return it. (WP01 verified real HA raises `ItemNotFound` → `ERR_NOT_FOUND` for this case; exception-class parity lets tests catch the same exception against both harness and live HA.)
  - 8 `delete_*` methods that record an `ApiCall`, raise `FailedMessageError(code="not_found")` on missing id, otherwise pop from the seed dict. Matches real HA's behavior (WP01 verified `DictStorageCollectionWebsocket` returns `ERR_NOT_FOUND` for delete-of-nonexistent).
  - 3 counter action methods (`increment_counter`, `decrement_counter`, `reset_counter`) that record an `ApiCall` directly — NOT via `self.call_service(...)`. Following the existing `turn_on` pattern at `recording_api.py:167-176`.
  - `reset()` extended to reinitialize `self.helper_definitions` to empty-per-domain state (round-2 finding F8).
- `ApiProtocol` in `recording_api.py` gains 35 new method signatures (8 list + 8 create + 8 update + 8 delete + 3 counter actions).
- `src/hassette/test_utils/app_harness.py` gains `seed_helper(record: BaseModel) -> None` — derives domain from record type via `_RECORD_TYPE_TO_DOMAIN`, raises `ValueError` with the valid record class names on unknown type (round-2 finding F5).
- `tests/unit/test_recording_api_write_parity.py::_KNOWN_READ_METHODS` frozenset is extended with the 8 `list_*` names. **Co-commit required.**
- `src/hassette/test_utils/sync_facade.py` regenerated via `uv run tools/generate_sync_facade.py --target recording`.
- Drift gate `uv run tools/generate_sync_facade.py --target recording --check` exits 0.
- Parity gate `uv run pytest tests/unit/test_recording_api_write_parity.py -n 2` passes.
- `tests/unit/test_recording_api_helpers.py` (new file) exercises the full seed/list/create/update/delete lifecycle.
- `uv run pytest tests/unit/test_recording_api_helpers.py tests/unit/test_recording_api.py tests/unit/test_recording_sync_facade.py -n 2` passes.
- `uv run pyright` passes on all changed files.

## Subtasks

1. Read `src/hassette/test_utils/recording_api.py` fully to understand the current `RecordingApi` class structure, `ApiProtocol` Protocol body, `reset()` method, and module-level imports.
2. Add module-level constants to `recording_api.py` near the imports:
   - `_SUPPORTED_HELPER_DOMAINS: frozenset[str] = frozenset({"input_boolean", "input_number", "input_text", "input_select", "input_datetime", "input_button", "counter", "timer"})`
   - `_RECORD_TYPE_TO_DOMAIN: dict[type, str]` — literal dict mapping each of the 8 Record classes to its domain string. Add a multi-line comment above the dict per round-3 finding F6: "hand-maintained dict literal that must stay in sync with the 8 Record classes above. Adding a 9th helper domain requires adding an entry here. A future refactor could add `domain: ClassVar[str]` to each Record model and auto-populate this dict, but that is out of scope for this PR."
3. Add `_slugify_helper_name(name: str) -> str` module-level helper. Implementation: `from slugify import slugify as _py_slugify; return _py_slugify(name, separator="_")`. This matches HA's exact call (`homeassistant.util.slugify` wraps `python-slugify` with the same `separator="_"` argument — WP01 verified). The `python-slugify` package is added as a direct runtime dependency by WP02. Do NOT roll a local implementation — WP01 explicitly rejected that path because it cannot handle unicode and punctuation as HA does.
4. Add `_generate_helper_id(existing_ids: set[str], name: str) -> str` module-level helper that mirrors HA's `IDManager.generate_id` (WP01 verified at `collection.py:102-112` in HA tag `2026.4.1`). Implementation: compute `base_id = _slugify_helper_name(name)`, then if `base_id not in existing_ids` return it; otherwise find the smallest integer `n >= 2` such that `f"{base_id}_{n}"` is not in `existing_ids` and return that. This ensures `RecordingApi.create_*` produces the same id as real HA would for the same name under the same existing-id set.
5. Extend `ApiProtocol` in `recording_api.py` with 35 new method signatures. Follow the existing signature style in the Protocol body (e.g., `async def turn_on(self, entity_id: str | StrEnum, domain: str = ..., **data) -> None: ...`). Group the 32 CRUD methods into 8 sections by domain with a comment above each section, and put counter action methods in their own section.
6. Add `helper_definitions: dict[str, dict[str, Any]]` to `RecordingApi`'s class-level attribute declarations (matching the existing `calls: list[ApiCall]` pattern).
7. In `RecordingApi.__init__`, add `self.helper_definitions = {d: {} for d in _SUPPORTED_HELPER_DOMAINS}` after `self.calls = []`.
8. Add the first full CRUD quartet for `input_boolean` to `RecordingApi`:
   - `list_input_booleans(self) -> list[InputBooleanRecord]` — `return cast(list[InputBooleanRecord], list(self.helper_definitions["input_boolean"].values()))`. No recording (this is a read).
   - `create_input_boolean(self, params: CreateInputBooleanParams) -> InputBooleanRecord` — record `ApiCall(method="create_input_boolean", args=(), kwargs=params.model_dump(exclude_unset=True))`, then compute `generated_id = _generate_helper_id(set(self.helper_definitions["input_boolean"].keys()), params.name)` (auto-suffixes on collision). Construct `record = InputBooleanRecord(id=generated_id, **params.model_dump(exclude_unset=True))`, insert into `helper_definitions["input_boolean"][record.id]`, return `record`. The auto-suffix behavior matches real HA so tests see the same id-generation semantics against both harness and live HA.
   - `update_input_boolean(self, helper_id: str, params: UpdateInputBooleanParams) -> InputBooleanRecord` — record `ApiCall`, raise `FailedMessageError(f"No input_boolean helper with id {helper_id!r} seeded. Use harness.seed_helper() or create_input_boolean() first.", code="not_found")` if `helper_id not in self.helper_definitions["input_boolean"]`, otherwise `existing = self.helper_definitions["input_boolean"][helper_id]; updated = existing.model_copy(update=params.model_dump(exclude_unset=True)); self.helper_definitions["input_boolean"][helper_id] = updated; return updated`.
   - `delete_input_boolean(self, helper_id: str) -> None` — record `ApiCall`, raise `FailedMessageError(..., code="not_found")` with the same helpful message on missing id, otherwise `del self.helper_definitions["input_boolean"][helper_id]`.
9. Repeat step 8 for the other 7 domains — 32 method definitions total. Use the same pattern parameterized by domain name and Record/Params classes.
10. Add 3 counter action methods to `RecordingApi`:
    - `increment_counter(self, entity_id: str) -> None` — record `ApiCall(method="increment_counter", args=(entity_id,), kwargs={"entity_id": entity_id})`. Do NOT delegate to `self.call_service(...)` — that would trigger the async-peer-call guard in the sync facade generator (round-2 finding F12).
    - `decrement_counter` — same pattern.
    - `reset_counter` — same pattern.
11. Update `RecordingApi.reset()` to reinitialize `helper_definitions`:
    ```python
    def reset(self) -> None:
        self.calls.clear()
        self.helper_definitions = {d: {} for d in _SUPPORTED_HELPER_DOMAINS}
    ```
12. Locate `src/hassette/test_utils/app_harness.py` (the `AppTestHarness` class) and add a new public method `seed_helper`:
    ```python
    def seed_helper(self, record: BaseModel) -> None:
        """Seed a stored helper config for tests that read helper CRUD.

        Domain is derived from the record class. Passing a record of a type
        not registered in _RECORD_TYPE_TO_DOMAIN raises ValueError immediately.
        """
        try:
            domain = _RECORD_TYPE_TO_DOMAIN[type(record)]
        except KeyError as e:
            raise ValueError(
                f"Unknown helper record type: {type(record).__name__}. "
                f"Expected one of: {sorted(t.__name__ for t in _RECORD_TYPE_TO_DOMAIN)}"
            ) from e
        self.api_recorder.helper_definitions[domain][record.id] = record
    ```
    Import `_RECORD_TYPE_TO_DOMAIN` from `recording_api`, or — if the import graph would be cleaner — move `_RECORD_TYPE_TO_DOMAIN` to `test_utils/_helpers.py` and import from both `recording_api.py` and `app_harness.py`. Either works; pick the lower-drama option.
13. **Co-commit**: update `tests/unit/test_recording_api_write_parity.py::_KNOWN_READ_METHODS` frozenset to include the 8 new read method names: `"list_input_booleans", "list_input_numbers", "list_input_texts", "list_input_selects", "list_input_datetimes", "list_input_buttons", "list_counters", "list_timers"`. **This update must be in the same commit as steps 8–10 in `recording_api.py`** — otherwise the parity test will fail with "RecordingApi is missing write methods: {list_input_booleans, ...}", a confusing error that wastes debugging time.
14. Run `uv run pytest tests/unit/test_recording_api_write_parity.py -n 2` — must pass. If it fails, step 13 was not done or was not co-committed.
15. Run `uv run tools/generate_sync_facade.py --target recording` to regenerate `src/hassette/test_utils/sync_facade.py`.
16. Run `uv run tools/generate_sync_facade.py --target recording --check` — must exit 0.
17. Run `uv run pytest tests/unit/test_recording_sync_facade_generation.py::test_generator_check_mode_exits_zero -n 2` — drift gate for the recording target.
18. Create `tests/unit/test_recording_api_helpers.py` (new file) with a module docstring, imports, and a fixture providing an `AppTestHarness` + `api_recorder`.
19. Add tests to `test_recording_api_helpers.py`:
    - `test_seed_helper_then_list_returns_seeded_record` — seed an `InputBooleanRecord`, call `list_input_booleans()`, assert it contains exactly one record with matching fields.
    - `test_seed_helper_rejects_unknown_type` — pass a `BaseModel` subclass not in `_RECORD_TYPE_TO_DOMAIN`, assert `ValueError` with the expected class list in the message.
    - `test_create_input_boolean_adds_to_list` — call `create_input_boolean(CreateInputBooleanParams(name="vacation_mode"))`, then `list_input_booleans()`, assert the returned list contains the new record with `id == "vacation_mode"` (post-slug).
    - `test_create_input_boolean_slugifies_name` — call `create_input_boolean(CreateInputBooleanParams(name="Vacation Mode"))`, assert the stored id is `"vacation_mode"` (or whatever WP01's slug rule dictates).
    - `test_create_input_boolean_auto_suffixes_collision` — call create twice with the same `name="vacation_mode"`; assert first record has `id="vacation_mode"`, second record has `id="vacation_mode_2"`, both exist in `list_input_booleans()`. This mirrors HA's `IDManager.generate_id` behavior.
    - `test_update_input_boolean_raises_on_missing_id` — without seeding anything, call `update_input_boolean("nonexistent", UpdateInputBooleanParams(initial=True))`, assert `FailedMessageError` with `e.code == "not_found"` and "No input_boolean helper" in the message.
    - `test_update_input_boolean_mutates_seed` — seed a record, call update, assert the returned record has updated fields and `list_input_booleans()` reflects the change.
    - `test_delete_input_boolean_raises_on_missing_id` — assert `FailedMessageError` with `e.code == "not_found"` and helpful message.
    - `test_delete_input_boolean_removes_from_list` — seed → delete → assert `list_input_booleans()` is empty.
    - `test_reset_clears_helper_definitions` — seed 3 records across different domains, call `harness.api_recorder.reset()`, assert all `list_*` return empty lists.
    - `test_create_records_api_call` — call `create_input_boolean`, assert `harness.api_recorder.calls[0].method == "create_input_boolean"` and kwargs match.
    - `test_counter_action_records_api_call` — call `increment_counter("counter.foo")`, assert `harness.api_recorder.calls[0].method == "increment_counter"` with `entity_id="counter.foo"`.
20. Run `uv run pytest tests/unit/test_recording_api_helpers.py -n 2` — all tests pass.
21. Run `uv run pytest tests/unit/test_recording_api.py tests/unit/test_recording_sync_facade.py -n 2` — existing recording tests continue to pass.
22. Run `uv run pyright` on all changed files and fix any type errors.

## Test Strategy

**Existing parity + drift gates — extended**:

- `tests/unit/test_recording_api_write_parity.py::test_api_write_methods_covered_by_recording_api` — must pass after `_KNOWN_READ_METHODS` update. Enforces that `RecordingApi` implements every write method `Api` has.
- `tests/unit/test_recording_sync_facade_generation.py::test_generator_check_mode_exits_zero` — must pass after regenerating `test_utils/sync_facade.py`.

**New file: `tests/unit/test_recording_api_helpers.py`** — ~12 tests covering:

1. Seed → list round trip
2. Seed with unknown record type → ValueError
3. Create → list round trip (including slug transformation)
4. Create stores record with slugified id
5. Create auto-suffixes on collision — two consecutive creates with the same name produce ids `vacation_mode` and `vacation_mode_2`, mirroring HA's `IDManager.generate_id`
6. Update on missing id → `FailedMessageError(code="not_found")` with helpful message
7. Update mutates seed dict
8. Delete on missing id → `FailedMessageError(code="not_found")` with helpful message
9. Delete removes from seed dict
10. Reset clears `helper_definitions`
11. Create records an ApiCall
12. Counter action records an ApiCall (pattern test for all 3)

**Regression tests**: run existing `tests/unit/test_recording_api.py` and `tests/unit/test_recording_sync_facade.py` to ensure no regressions in the harness.

Run command: `uv run pytest tests/unit/test_recording_api_helpers.py tests/unit/test_recording_api.py tests/unit/test_recording_sync_facade.py tests/unit/test_recording_api_write_parity.py tests/unit/test_recording_sync_facade_generation.py -n 2`.

## Review Guidance

**Code reviewer must verify:**

- Counter action methods on `RecordingApi` record an `ApiCall` directly — NO `await self.call_service(...)` call. The generator's `_check_no_async_peer_calls` guard at `generate_sync_facade.py:509` would abort regeneration if they did (round-2 finding F12).
- `_slugify_helper_name` lives in `recording_api.py` (or a shared test_utils helpers module), NOT in `api.py` (round-3 finding F5). Production `Api.create_*` methods must NOT call it — they let HA slugify server-side.
- `seed_helper(record)` has no `domain` parameter. It derives domain from `type(record)`. A call with `seed_helper("input_boolean", record)` (old-style) should fail at type-check time (Pyright) or raise at runtime. (Round-2 finding F5.)
- `update_*` and `delete_*` both raise `FailedMessageError(code="not_found")` on missing id — strictly consistent, and matches real HA (WP01 verified `ItemNotFound → ERR_NOT_FOUND`). Neither silently ignores. (Round-2 finding F7, updated per WP01 finding.)
- `reset()` clears `helper_definitions`, not just `calls`. (Round-2 finding F8.)
- `_KNOWN_READ_METHODS` update in `test_recording_api_write_parity.py` is IN THE SAME COMMIT as the `list_*` additions. Use `git log --oneline -3` and verify.
- The regenerated `test_utils/sync_facade.py` contains sync wrappers for all 35 new methods, and none of the module-level `_SUPPORTED_HELPER_DOMAINS`/`_RECORD_TYPE_TO_DOMAIN`/`_slugify_helper_name` helpers.
- `ApiProtocol` entries match the real `Api` method signatures exactly (including type annotations on `params` and return types).

**Integration reviewer must verify:**

- Drift gate passes for the recording target: `uv run pytest tests/unit/test_recording_sync_facade_generation.py::test_generator_check_mode_exits_zero -n 2`.
- Parity gate passes: `uv run pytest tests/unit/test_recording_api_write_parity.py -n 2`.
- No circular imports introduced between `recording_api.py` and `app_harness.py`.
- `harness.seed_helper(InputBooleanRecord(id="x", name="y"))` works at the public API level (end-to-end smoke test via the test harness fixture).

**Blocker-level deviations:**

- `self.call_service(...)` in any counter action method on `RecordingApi` → BLOCKER. Generator will abort.
- `_slugify_helper_name` in `api.py` → BLOCKER (F5 reversal). Harness-only logic leaking into production.
- `_KNOWN_READ_METHODS` NOT co-committed → BLOCKER. Parity test fails on commit.
- `delete_*` or `update_*` silently succeeds on missing id → BLOCKER (F7). Divergence from each other and from real HA.
- `delete_*` or `update_*` raises `KeyError` on missing id (instead of `FailedMessageError(code="not_found")`) → BLOCKER. Exception-class parity with real HA is required so tests can use a single `except FailedMessageError` block against both harness and live HA. Updated per WP01 finding — was `KeyError` in round-2 finding F7, reclassified after HA source verification showed real HA raises `FailedMessageError(code="not_found")` for the same case.
- `reset()` not clearing `helper_definitions` → BLOCKER (F8). Silent test-isolation bug.

## Visual Verification

Not applicable — no UI changes.
