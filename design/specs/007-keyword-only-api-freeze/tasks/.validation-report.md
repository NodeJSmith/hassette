## Verdict: PASS

> PASS — all 12 FRs and 11 ACs are covered by an implementing task with a matching Verify item, no contradictions between task Prompt/Focus sections and the design doc, no identifier format violations, and context.md has all five required sections plus a populated optional sixth. Two minor warnings noted below; neither blocks approval.

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1  | T02 | "Calling `scheduler.run_in(func, 5, "positional_name")` raises `TypeError` (name is keyword-only)" |
| FR#2  | T03 | "`on_homeassistant_start(handler, ...)` with positional `handler` raises `TypeError`" |
| FR#3  | T03 | "`uv run pyright` reports error when any Bus method called without `name=`" |
| FR#4  | T02 | "`SchedulerNameRequiredError` raised when `name=""` passed to `schedule()`, and when `add_job()` called with empty `job.name`" |
| FR#5  | T01 | "`ScheduledJob.__post_init__` no longer generates a name from callable+trigger when `name` is empty" |
| FR#6  | T01 | "`name_auto` field does not exist on `ScheduledJob`, `ScheduledJobRegistration`, or `JobSummary`" |
| FR#7  | T01 | "`010.sql` contains `ALTER TABLE scheduled_jobs DROP COLUMN name_auto`" |
| FR#8  | T05 | "`nameAutoHint` does not appear anywhere in `frontend/src/`" |
| FR#9  | T03 | "`on_error` handler fires when passed to each of the 10 delegate methods" |
| FR#10 | T04 | "`App.cleanup`, `Resource.cleanup`, `DatabaseService.cleanup` accept `timeout: float \| None`" |
| FR#11 | T04 | "Generated `SchedulerSyncFacade` and `BusSyncFacade` mirror their async counterparts' signature changes" |
| FR#12 | T06 | "`tools/check_registration_signatures.py` exits 0 on current (correct) `bus.py` and `scheduler.py`" |
| AC#1  | T02 | "`uv run pyright` reports error when `scheduler.run_in(func, 5, handler=h)` omits `name=`" |
| AC#1  | T03 | "Pyright catches missing `name=` on Bus registration calls" |
| AC#2  | T02 | "Positional `name`, `group`, `jitter`, `timeout`, `timeout_disabled` raise `TypeError`" |
| AC#3  | T03 | "Positional `handler` on `on_homeassistant_start` raises `TypeError`" |
| AC#4  | T02 | "`SchedulerNameRequiredError` raised for empty name on all scheduling methods" |
| AC#5  | T03 | "`ListenerNameRequiredError` raised when `name=""` passed (new: empty string rejection)" |
| AC#6  | T03 | "`on_error` fires on all 4 delegation chains" |
| AC#7  | T01 | "After migration 010 runs, the `name_auto` column does not exist in `scheduled_jobs`" |
| AC#8  | T05 | "`grep -r 'nameAutoHint' frontend/src/` returns no results; `grep -r 'name_auto' frontend/src/api/generated-types.ts` returns no results" |
| AC#9  | T04 | "`uv run nox -s dev` passes with all changes from T01-T04" |
| AC#10 | T06 | "`prek -a` passes (all hooks including the new linter)" |
| AC#11 | T06 | "The linter exits 1 when given a test input with `name` before `*` or with a default value" |

Total FRs in design.md: 12 (FR#1–FR#12), all covered.
Total ACs in design.md: 11 (AC#1–AC#11), all covered.
AC#1 is legitimately claimed by both T02 (scheduler half) and T03 (bus half) — the design's AC#1 text ("any Bus or Scheduler registration method") spans both subsystems, and both tasks carry their own concrete Verify item for their half.

## Coverage Gaps

None.

## Contradictions

None. Spot-checked technical claims in task Prompt/Focus sections against both the design doc and the actual source:
- T03's enumerated list of 18 Bus registration methods (`on`, `on_state_change`, ... `on_app_stopping`) matches the actual `def on_*`/`def on(` methods in `src/hassette/bus/bus.py` exactly (verified via grep).
- T02's enumerated list of 8 Scheduler methods (`schedule`, `run_in`, `run_once`, `run_every`, `run_minutely`, `run_hourly`, `run_daily`, `run_cron`) matches `src/hassette/scheduler/scheduler.py` exactly.
- T04's citation of `format_signature_and_call()` in `codegen/src/hassette_codegen/sync_facade/ast_utils.py:78-134` was verified — the function does emit `kwarg=kwarg` keyword-style call arguments for `kwonlyargs`, supporting the task's claim that regeneration needs no manual intervention for keyword-only delegation.
- T03's `on_error` delegation-chain mapping (4 forwarding targets) matches design.md's Test Strategy section verbatim.
- T01's Focus note ("do NOT change the `name: str = ""` default in this task") correctly defers that change to T02, matching the design's stated separation between auto-naming removal (T01) and required-name enforcement (T02).

## Warnings

- T04 Verify item for FR#11 ("Generated `SchedulerSyncFacade` and `BusSyncFacade` mirror their async counterparts' signature changes") is written at a level of abstraction that borders on needing code inspection to verify. It is not code-blind, however — the same task's Prompt section (item 1) already spells out the concrete, binary checks ("`name` is keyword-only ... with no default on all 8 methods", "all methods have `name: str` (no default)", "delegate methods include `on_error`", "delegation calls use keyword args"). Recommend tightening the Verify bullet itself to restate those concrete checks rather than relying on the reader to cross-reference the Prompt.
- Design doc's "Documentation Updates" section states docstrings on all modified methods should be updated, including "update `timeout` type in cleanup docstrings." T04 (which owns the `App.cleanup`/`Resource.cleanup`/`DatabaseService.cleanup` timeout widening) does not mention updating those methods' docstrings — only the type annotation change. This is not a FR/AC coverage gap (no FR/AC identifier ties to cleanup docstrings specifically), but it is a design-doc directive with no explicit home in any task. Low risk since it's a one-line docstring tweak likely to be caught incidentally, but worth a one-line addition to T04's Prompt for completeness.
- design.md's "Migration" section under Documentation Updates suggests "Consider adding a migration note for the `name=` requirement change" to the migration guide docs. This is explicitly framed as optional ("Consider"), and T06 does not address it. Not a gap given its optional framing.

None of the above block approval — no coverage gaps, no contradictions, and context.md is complete.
