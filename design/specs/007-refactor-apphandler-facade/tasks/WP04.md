---
work_package_id: "WP04"
title: "Update integration tests and test harness"
lane: "done"
plan_section: "Architecture"
depends_on: ["WP01", "WP02"]
---

## Objectives & Success Criteria

Update integration tests that directly construct `AppFactory`/`AppLifecycleManager` or patch AppHandler internals to work with the new AppLifecycleService structure. Update HassetteHarness if needed.

**Observable outcomes:**
- `tests/integration/test_app_factory_lifecycle.py` updated — imports and constructs through AppLifecycleService
- `tests/integration/test_hot_reload.py` updated — patches point to correct locations
- `tests/integration/test_apps.py` verified — should work without changes (uses public API)
- `uv run nox -s dev` passes (full suite including integration tests)
- `uv run pyright` passes

## Subtasks

1. **Update `tests/integration/test_app_factory_lifecycle.py`**:
   - Replace `from hassette.core.app_lifecycle import AppLifecycleManager` with `from hassette.core.app_lifecycle_service import AppLifecycleService`
   - Update `app_lifecycle` fixture: instead of creating standalone `AppLifecycleManager(hassette, registry)`, create an `AppLifecycleService` — this requires Resource-compatible construction. Options:
     a. If tests can use `hassette_with_app_handler` and access `hassette._app_handler.lifecycle`, do that (simplest)
     b. If standalone construction is needed, instantiate `AppLifecycleService(hassette, parent=None, registry=registry)` (verify Resource allows parent=None for testing)
   - Update `app_factory` fixture: AppFactory is still a plain class, but verify it can be accessed via `lifecycle_service.factory` if tests need it directly
   - Update all test references from `app_lifecycle.` to the new service's methods
   - Verify `app_key` and `instance_index` assertions still hold (identity model)

2. **Update `tests/integration/test_hot_reload.py`**:
   - Check all `patch()` calls — any that patch `app_handler.change_detector` or `app_handler.factory` need to be updated to `app_handler.lifecycle.change_detector` / `app_handler.lifecycle.factory`
   - Verify the `hassette_and_handler` fixture still works (it accesses `hassette._app_handler` which is unchanged)
   - Verify Bus event routing — file watcher events should reach AppLifecycleService's handler through its own Bus child

3. **Verify `tests/integration/test_apps.py`** — should work without changes since it uses public API (`hassette._app_handler.start_app()`, `.stop_app()`, `.reload_app()`, `.get_status_snapshot()`). Run and confirm.

4. **Verify `tests/integration/test_apps_env.py`** — check for any direct internal access that might break.

5. **Check HassetteHarness** (`src/hassette/test_utils/harness.py`):
   - `_start_app_handler` (line 391-394) creates AppHandler via `add_child(AppHandler)` — AppLifecycleService is auto-created as AppHandler's child, so this should work without changes
   - Verify `get_app` (line 127-129) still works
   - If any component dependency declarations need updating (line 162 `"app_handler": {"bus", "state_proxy"}`), update them

6. **Check test fixtures** (`src/hassette/test_utils/fixtures.py`):
   - Verify `hassette_with_app_handler` fixture still works
   - Add optional `create_app_lifecycle_service` helper if useful for future tests

7. Run full test suite: `uv run nox -s dev`

## Test Strategy

This WP updates existing tests — it doesn't create new test behaviors. The full integration test suite must pass, confirming the refactored structure is transparent to the existing test assertions.

Key verification:
- `uv run pytest tests/integration/test_app_factory_lifecycle.py -v` — all tests pass
- `uv run pytest tests/integration/test_hot_reload.py -v` — all tests pass
- `uv run pytest tests/integration/test_apps.py -v` — all tests pass
- `uv run nox -s dev` — full suite green

## Review Guidance

- **BLOCKER**: No test assertions should be weakened or removed — the existing tests are the safety net for this refactor. If a test needs to change, it should only be construction/setup changes, not assertion changes.
- **BLOCKER**: Identity model — `test_app_factory_lifecycle.py` tests verify `app_key` and `instance_index` flow through factory and lifecycle. These assertions must remain intact.
- **WARNING**: Patch paths — any `patch("hassette.core.app_handler.AppHandler.X")` may need updating to `patch("hassette.core.app_lifecycle_service.AppLifecycleService.X")` if the patched method moved
- **WARNING**: HassetteHarness component dependency declarations — if `"app_handler"` previously depended on `"bus"` because AppHandler owned a Bus child, check if this dependency is still correct (Bus is now in AppLifecycleService, but AppLifecycleService is a child of AppHandler so the dependency chain should still hold)

## Activity Log

- 2026-03-16T21:30:00Z — system — lane=planned — WP created
- 2026-03-16T22:37:20Z — system — lane=doing — moved from planned
- 2026-03-16T22:45:37Z — system — lane=done — moved from doing
