---
work_package_id: "WP03"
title: "Write unit tests for AppLifecycleService"
lane: "done"
plan_section: "Architecture"
depends_on: ["WP01", "WP02"]
---

## Objectives & Success Criteria

Replace `tests/unit/test_app_lifecycle.py` (which tests the now-deleted AppLifecycleManager) with `tests/unit/core/test_app_lifecycle_service.py` covering AppLifecycleService's full interface. Tests use mocks for Hassette, AppRegistry, and AppFactory — no full integration wiring needed.

**Observable outcomes:**
- `tests/unit/test_app_lifecycle.py` is deleted
- `tests/unit/core/test_app_lifecycle_service.py` exists with comprehensive coverage
- `uv run pytest tests/unit/core/test_app_lifecycle_service.py -v` passes
- `uv run nox -s dev` passes (full suite)

## Subtasks

1. Delete `tests/unit/test_app_lifecycle.py`

2. Create `tests/unit/core/test_app_lifecycle_service.py` with the following test classes and fixtures:

   **Fixtures:**
   - `mock_hassette` — Mock with config (startup_timeout, shutdown_timeout, dev_mode, allow_reload_in_prod, app_handler_log_level, app_manifests), `send_event` as AsyncMock, `wait_for_ready` as AsyncMock returning True
   - `mock_registry` — Mock AppRegistry with `record_failure`, `all_apps`, `clear_all`, `get_manifest`, `get_apps_by_key`, `register_app`, `unregister_app`, `set_manifests`, `set_only_app`, `apps`, `manifests`, `enabled_manifests`, `active_manifests`, `only_app`, `get_snapshot`, `block_app`, `unblock_apps`
   - `mock_factory` — Mock AppFactory with `create_instances`, `check_only_app_decorator`
   - `mock_manifest` — Mock AppManifest with `class_name`, `app_key`, `full_path`, `display_name`
   - `mock_app_instance` — AsyncMock App with `app_config.instance_name`, `status`, `initialize`, `shutdown`, `mark_ready`, `logger`
   - `lifecycle_service` — create AppLifecycleService with mock_hassette, mock_registry, patching AppFactory and AppChangeDetector creation

   **Test classes (TDD — write test first, verify it exercises the right code):**

   `TestAppLifecycleServiceInit`:
   - `test_stores_registry_reference` — verify registry is the passed-in reference
   - `test_creates_factory_internally` — verify AppFactory created with hassette and registry
   - `test_creates_change_detector_internally` — verify AppChangeDetector created
   - `test_creates_bus_child` — verify Bus added as child

   `TestAppLifecycleServiceProperties`:
   - `test_startup_timeout_from_config` — reads hassette.config.app_startup_timeout_seconds
   - `test_shutdown_timeout_from_config` — reads hassette.config.app_shutdown_timeout_seconds

   `TestInitializeInstances`:
   - `test_success_calls_initialize_and_mark_ready` — same as old test, adapted for service
   - `test_multiple_instances` — initializes all provided instances
   - `test_timeout_records_failure` — sets STOPPED, records to registry
   - `test_exception_records_failure` — sets STOPPED, records to registry
   - `test_continues_after_failure` — remaining instances still initialized
   - `test_emits_running_event_on_success` — hassette.send_event called with RUNNING
   - `test_emits_failed_event_on_error` — hassette.send_event called with FAILED

   `TestShutdownInstance`:
   - `test_calls_shutdown` — inst.shutdown() called
   - `test_catches_exceptions` — logs error, doesn't raise
   - `test_emits_stopped_event` — hassette.send_event called with STOPPED

   `TestShutdownInstances`:
   - `test_empty_dict_returns_early` — no error
   - `test_calls_shutdown_for_each` — each instance shut down
   - `test_emits_stopping_event` — STOPPING emitted before shutdown

   `TestShutdownAll`:
   - `test_shuts_down_all_registered_apps` — all registry apps shut down
   - `test_clears_registry` — registry.clear_all() called

   `TestStartApp`:
   - `test_creates_instances_via_factory` — factory.create_instances called
   - `test_emits_not_started_event` — NOT_STARTED event for each instance
   - `test_skips_disabled_app` — returns early if no manifest
   - `test_handles_factory_load_error` — graceful on UndefinedUserConfigError

   `TestStopApp`:
   - `test_unregisters_and_shuts_down` — registry.unregister + shutdown_instances
   - `test_warns_if_not_found` — logs warning if app not in registry

   `TestReloadApp`:
   - `test_stops_then_starts` — stop_app then start_app called in order

   `TestHandleChangeEvent`:
   - `test_detects_and_applies_changes` — refresh_config + change_detector + apply_changes
   - `test_no_changes_returns_early` — ChangeSet.has_changes=False, no apply

   `TestReconcileBlockedApps`:
   - `test_blocks_non_only_apps` — apps other than only_app get blocked
   - `test_unblocks_when_only_app_cleared` — previously blocked apps unblocked

3. Verify all tests pass: `uv run pytest tests/unit/core/test_app_lifecycle_service.py -v`

4. Verify full suite: `uv run nox -s dev`

## Test Strategy

This WP IS the test strategy — it creates the unit tests for the new service. The tests are mock-based (unit level), verifying AppLifecycleService's behavior in isolation.

Coverage target: all public methods of AppLifecycleService have at least one test.

## Review Guidance

- **BLOCKER**: `tests/unit/test_app_lifecycle.py` must be deleted — no dead test files
- **BLOCKER**: Every test must exercise actual AppLifecycleService code, not just mock behavior — verify mock setup doesn't trivially pass
- **WARNING**: Event emission tests should verify the correct Topic and event type, not just that `send_event` was called
- **WARNING**: Identity model — `test_creates_instances_via_factory` should verify `app_key` is passed through correctly (not `unique_name`)
- Check that the fixture creates AppLifecycleService using the Resource pattern (via constructor, not `add_child`) for unit testing isolation

## Activity Log

- 2026-03-16T21:30:00Z — system — lane=planned — WP created
- 2026-03-16T22:18:26Z — system — lane=doing — moved from planned
- 2026-03-16T22:36:57Z — system — lane=done — moved from doing
