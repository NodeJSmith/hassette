---
depends_on:
- WP02
- WP04
lane: doing
title: Update E2E tests and documentation
work_package_id: WP05
---

## Objectives & Success Criteria

All E2E tests pass with the new unified dashboard and per-component framework keys. Documentation reflects the new behavior. After this WP:

- `test_dashboard.py` panel heading assertions reflect unified layout
- `test_dashboard.py` framework-health tests verify summary badge (no expandable error list)
- `test_dashboard.py` error feed tests verify unified feed with both tiers
- `conftest.py` framework fixture seeds use component-specific keys
- `docs/pages/web-ui/dashboard.md` describes unified "Recent Errors" with framework badges and tracebacks
- `docs/pages/core-concepts/database-telemetry.md` describes per-component framework identity

## Subtasks

1. Update `tests/e2e/conftest.py` lines 634-647 — change `framework_tier_errors` fixture to use component-specific `app_key` (e.g., `"__hassette__.service_watcher"`) instead of bare `"__hassette__"`
2. Update `tests/e2e/test_dashboard.py` lines 150-152 — update `PANEL_HEADINGS` if panel names changed
3. Update `tests/e2e/test_dashboard.py` lines 201-224 — `test_framework_affordance_visible` and `test_framework_affordance_shows_framework_errors` to verify summary badge behavior (no expandable error list)
4. Update `tests/e2e/test_dashboard.py` error feed tests — verify unified feed shows both app and framework errors with appropriate badges
5. Update `docs/pages/web-ui/dashboard.md` — rewrite "System Health" section (now summary badge only); rewrite "Recent Errors" section (includes framework errors with tier badges, tracebacks, component names); update KPI strip description (counts include framework)
6. Update `docs/pages/core-concepts/database-telemetry.md` — update source-tier note to describe per-component identity (`__hassette__.<component>`)

## Test Strategy

- E2E tests are the tests — they verify the full rendered UI
- Run `uv run pytest -m e2e -v` to verify all dashboard e2e tests pass
- No additional unit tests in this WP

## Review Guidance

- Verify e2e fixture seeds use the exact component keys from the design (`__hassette__.app_handler`, `__hassette__.core`, `__hassette__.service_watcher`)
- Verify docs accurately describe the new unified error feed behavior
- Verify docs don't reference the old "System Health" expandable section
- Verify docs mention the "Framework" badge on framework errors
