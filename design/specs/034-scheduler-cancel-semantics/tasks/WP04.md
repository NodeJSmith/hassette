---
depends_on:
- WP02
lane: doing
title: Update documentation — management guide, snippets, and migration
work_package_id: WP04
---

## Objectives & Success Criteria

- `management.md` attribute table has no `cancelled` row
- `management.md` cancel semantics prose describes dequeue-based cancellation (not flag-based)
- `management.md` includes `list_jobs()` idiom for checking cancellation state and null-reference pattern for double-cancel guard
- `scheduler_job_metadata.py` snippet has no `job.cancelled` reference
- No user-facing documentation references `remove_job` as a callable method or `job.cancelled` as an inspectable attribute
- `mkdocs serve` builds without errors

## Subtasks

1. Update `docs/pages/core-concepts/scheduler/management.md`:
   - Remove `cancelled | bool | True once cancel() has been called` row from the attribute table
   - Update cancel semantics prose — replace flag-based dispatch-skip explanation with dequeue-based explanation
   - Add "Checking cancellation state" section documenting `list_jobs()` idiom and null-reference pattern
2. Update `docs/pages/core-concepts/scheduler/snippets/scheduler_job_metadata.py` — remove `self.logger.info("Cancelled: %s", job.cancelled)` line
3. Grep all docs for remaining `remove_job` or `job.cancelled` references and update any found
4. Verify `mkdocs serve` builds cleanly

## Test Strategy

N/A — documentation-only changes. Verify via `mkdocs serve` build.

## Review Guidance

- Verify no docs reference `remove_job` as a user-callable method
- Verify no docs reference `job.cancelled` as an inspectable attribute
- Verify the new "Checking cancellation state" section describes `list_jobs()` and the null-reference pattern
- Verify the cancel semantics prose accurately reflects the new dequeue-based mechanism
