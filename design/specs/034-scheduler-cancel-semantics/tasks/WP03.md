---
depends_on:
- WP02
lane: doing
title: Update internal callers, telemetry, and test_utils
work_package_id: WP03
---

## Objectives & Success Criteria

- `state_proxy.py` uses `self.scheduler._dequeue_job()` instead of `self.scheduler.remove_job()`
- Telemetry enrichment uses `js.cancelled` only (no `live_job.cancelled` reference)
- `test_utils/reset.py` uses `scheduler._remove_all_jobs()`
- `JobSummary.cancelled` docstring updated to reflect DB-only derivation
- Integration test verifies `job.cancel()` via back-reference produces `cancelled_at IS NOT NULL` in DB
- All existing tests pass

## Subtasks

1. Update `src/hassette/core/state_proxy.py` lines 87 and 291: `self.scheduler.remove_job(self.poll_job)` → `self.scheduler._dequeue_job(self.poll_job)`
2. Simplify `src/hassette/web/routes/telemetry.py` line 255: `is_cancelled = live_job.cancelled or js.cancelled` → `is_cancelled = js.cancelled`
3. Update `src/hassette/test_utils/reset.py` line 62: `await scheduler.remove_all_jobs()` → `await scheduler._remove_all_jobs()`
4. Update `src/hassette/core/telemetry_models.py` `JobSummary.cancelled` docstring — now derived solely from `cancelled_at IS NOT NULL`
5. Add integration test for the back-reference cancel path in `tests/integration/test_scheduler.py`:
   - `test_job_cancel_via_back_reference_persists_cancelled_at` — schedule a job, call `job.cancel()`, verify `cancelled_at IS NOT NULL` in DB
   - `test_cancel_before_db_id_set_does_not_raise` — cancel a job before DB registration completes, verify no exception
6. Update remaining `job.cancelled` assertions in `tests/integration/test_scheduler.py` with behavioral equivalents

## Test Strategy

- `tests/integration/test_scheduler.py`:
  - `test_job_cancel_via_back_reference_persists_cancelled_at` — verifies AC6 for the `job.cancel()` path specifically
  - `test_cancel_before_db_id_set_does_not_raise` — verifies AC5 pre-registration edge case
  - Existing tests updated: replace `job.cancelled` assertions with `list_jobs()` absence or `cancelled_at` DB checks

## Review Guidance

- Verify `state_proxy.py` calls `_dequeue_job`, NOT `cancel_job` — StateProxy lifecycle stops must not write `cancelled_at` records
- Verify the telemetry OR expression is fully removed (not just commented out)
- Verify the integration test uses a real DB (not mocks) to verify `cancelled_at` persistence
- Grep all of `src/` and `tests/` for remaining `job.cancelled` references — none should remain after this WP
- Check that no references to `job.cancelled` (the transient flag) remain anywhere in `src/` or `tests/`
