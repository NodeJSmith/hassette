---
work_package_id: "WP05"
title: "Update entry pages, README, and run final verification"
lane: "planned"
plan_section: "5. Navigation & Metadata"
depends_on: ["WP01", "WP02", "WP03", "WP04"]
---

## Objectives & Success Criteria

- `docs/pages/getting-started/docker/index.md` no longer says "Hassette does not yet have a UI" and no longer recommends Dozzle
- `README.md` Web UI feature description lists the actual 4 pages, not entity/event-bus/scheduler
- `docs/index.md` feature description matches current UI capabilities
- `README.md` roadmap section reviewed for stale items
- `uv run mkdocs build --strict` passes with zero warnings
- Grep for stale references returns zero hits: `DataSyncService`, `/healthz`, `/api/healthz`, `scheduler.md`, `event-bus.md`, `entities.md`
- No doc page claims app actions require `dev_mode` or `allow_reload_in_prod`

## Subtasks

1. **docker/index.md** — Read `docs/pages/getting-started/docker/index.md`. Remove the "Hassette does not yet have a UI" note (~line 155) and the Dozzle recommendation/setup section. Replace with a brief mention of the built-in web dashboard with link to `pages/web-ui/index.md`.

2. **README.md** — Read `README.md`. Update the Web UI feature description (line ~32) to list Dashboard, Apps, Logs, Sessions. Remove references to entity browser, event bus, and scheduler. Review the roadmap section ("What's Next?") for items that are now done or stale.

3. **docs/index.md** — Read `docs/index.md`. Update the feature description (line ~49) to match current UI: remove entity browser/event bus/scheduler references, add Sessions and telemetry features.

4. **Final stale reference audit** — Run grep across `docs/` and `examples/` for:
   - `DataSyncService` → should be zero
   - `/healthz` and `/api/healthz` → should be zero
   - `scheduler.md`, `event-bus.md`, `entities.md` → should be zero (as cross-reference targets)
   - Verify no doc claims `dev_mode` or `allow_reload_in_prod` is required for app actions

5. **Final build** — Run `uv run mkdocs build --strict` one last time with all changes in place

## Test Strategy

N/A — no testable code changes. Verification is the comprehensive grep audit and `mkdocs build --strict`.

## Review Guidance

- This is the final WP — all previous WPs must be complete before this runs
- The stale reference audit is the primary quality gate. Any remaining stale references indicate a gap in earlier WPs.
- **README.md**: the feature description is what PyPI and GitHub display. It must be accurate and concise.
- **docs/index.md**: the homepage videos (`autocomplete.*`, `filtered_events.*`) may show old UI but are deferred to Phase 2 — do NOT delete them
- **docker/index.md**: removing Dozzle is intentional — the built-in UI replaces that need. Don't add Dozzle back as an "alternative".

## Activity Log

- 2026-04-01T23:30:00Z — system — lane=planned — WP created
