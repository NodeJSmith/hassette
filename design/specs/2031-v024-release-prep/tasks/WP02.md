---
depends_on: []
lane: done
plan_section: 3. Delete Orphaned Pages
title: Delete orphaned pages, assets, and update mkdocs nav
work_package_id: WP02
---

## Objectives & Success Criteria

- Orphaned web-ui doc pages deleted: `scheduler.md`, `event-bus.md`, `entities.md`
- Orphaned screenshot assets deleted: `web_ui_scheduler.png`, `web_ui_event_bus.png`, `web_ui_entities.png`
- Orphaned assets deleted: `app-logs.png`, `dozzle-service.yml`
- `mkdocs.yml` nav updated: Scheduler/Event Bus/Entities removed from Web UI section; Sessions added; Database & Telemetry added to Core Concepts; Log Level Tuning added to Advanced
- `dashboard.md` "View All" cross-links to deleted pages removed (lines 35 and 47)
- All changes land in a single atomic commit
- `uv run mkdocs build --strict` passes after this commit

## Subtasks

1. Verify `docs/pages/getting-started/docker/snippets/dozzle-service.yml` is only referenced by the Dozzle section in `docs/pages/getting-started/docker/index.md` (grep for `dozzle-service` in docs/)
2. Delete orphaned doc pages:
   - `docs/pages/web-ui/scheduler.md`
   - `docs/pages/web-ui/event-bus.md`
   - `docs/pages/web-ui/entities.md`
3. Delete orphaned assets:
   - `docs/_static/web_ui_scheduler.png`
   - `docs/_static/web_ui_event_bus.png`
   - `docs/_static/web_ui_entities.png`
   - `docs/_static/app-logs.png`
   - `docs/pages/getting-started/docker/snippets/dozzle-service.yml` (if confirmed orphaned in step 1)
4. Update `mkdocs.yml` nav (lines 60-75):
   - Remove Scheduler, Event Bus, Entities entries from Web UI section
   - Add `Sessions: pages/web-ui/sessions.md` to Web UI section
   - Add `Database & Telemetry: pages/core-concepts/database-telemetry.md` to Core Concepts section
   - Add `Log Level Tuning: pages/advanced/log-level-tuning.md` to Advanced section
5. Remove cross-links in `docs/pages/web-ui/dashboard.md`:
   - Line 35: Remove "Click **View All** to navigate to the [Scheduler](scheduler.md) page."
   - Line 47: Remove "Click **View All** to navigate to the [Event Bus](event-bus.md) page."
6. Create placeholder files for new nav entries so `mkdocs build --strict` passes:
   - `docs/pages/web-ui/sessions.md` (minimal placeholder: title + "Content coming in WP04")
   - `docs/pages/core-concepts/database-telemetry.md` (minimal placeholder)
   - `docs/pages/advanced/log-level-tuning.md` (minimal placeholder)
7. Run `uv run mkdocs build --strict` to verify no broken references

## Test Strategy

N/A — no testable code changes. Verification is `mkdocs build --strict` passing.

## Review Guidance

- **All deletions and nav updates must be in a single commit** — this is a design constraint. Splitting will break `mkdocs build --strict` at intermediate states.
- Placeholder files for new pages are temporary scaffolding — they will be replaced with real content in WP04
- Verify no other docs reference the deleted pages (grep for `scheduler.md`, `event-bus.md`, `entities.md` in `docs/`)
- The `autocomplete.*` and `filtered_events.*` assets in `docs/_static/` must NOT be deleted — they are referenced in `docs/index.md`

## Activity Log

- 2026-04-01T23:30:00Z — system — lane=planned — WP created
- 2026-04-02T04:33:05Z — system — lane=doing — moved from planned
- 2026-04-02T04:44:35Z — system — lane=done — moved from doing
