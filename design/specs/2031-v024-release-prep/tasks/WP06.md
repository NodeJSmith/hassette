---
work_package_id: "WP06"
title: "Bump version to 0.24.0 and stamp changelog"
lane: "planned"
plan_section: "7. Version Bump"
depends_on: ["WP05"]
---

## Objectives & Success Criteria

- `pyproject.toml` version updated from `0.23.0` to `0.24.0`
- `CHANGELOG.md` header stamped from `## Unreleased` to `## [0.24.0] - YYYY-MM-DD` with the actual date
- No other version references to `0.23.0` remain in the repo (grep verify)

## Subtasks

1. Edit `pyproject.toml` line 7: change `version = "0.23.0"` to `version = "0.24.0"`
2. Edit `CHANGELOG.md`: change `## Unreleased` to `## [0.24.0] - YYYY-MM-DD` using today's date
3. Grep for `0.23.0` across the repo to verify no stale version references remain

## Test Strategy

N/A — no testable code changes. Verification is grep for stale version strings.

## Review Guidance

- This WP executes as part of Phase 2 — after Phase 1 WPs are complete AND screenshots have been taken
- The date should be the actual stamp date, not a placeholder
- Phase 2 screenshots (Section 6 of the design doc) are handled outside the caliper workflow — they require manual steps (demo repo, hautomate service bringdown)

## Activity Log

- 2026-04-01T23:45:00Z — system — lane=planned — WP created
