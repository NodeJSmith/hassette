---
depends_on:
- WP02
lane: done
plan_section: 2. Fix Broken/Stale Documentation
title: Fix stale and broken documentation
work_package_id: WP03
---

## Objectives & Success Criteria

- All 3 broken code examples in `persistent-storage.md` fixed (`P.to_state.is_on` → `where=P.to_state.is_on`)
- `core-concepts/index.md` service map updated: `DataSyncService` replaced, `DatabaseService` added, Mermaid diagram accurate
- `apps/configuration.md` type annotations corrected
- `configuration/global.md` expanded with subsectioned tables for ~30 user-relevant config fields
- `web-ui/index.md` nav list updated to 4 routes with accurate descriptions
- `web-ui/dashboard.md` rewritten to describe current Preact SPA layout
- `web-ui/apps.md` updated: `dev_mode` note removed, columns updated
- `web-ui/logs.md` updated: Source column, multi-column sort documented
- `bus/index.md` debounce/throttle ValueError constraint documented
- `scheduler/methods.md` has new Idempotent Registration section
- `uv run mkdocs build --strict` passes
- Grep for `DataSyncService` in `docs/` returns zero hits

## Subtasks

1. **persistent-storage.md** — Read the full file, grep for all `P.` usages passed without a keyword. Fix the 3 known locations (WaterLeakAlertApp, MultiSensorAlertApp, MotionCounterApp examples) by adding `where=` keyword. Verify the correct keyword by checking `src/hassette/bus/bus.py` `on_state_change()` signature.

2. **core-concepts/index.md** — Read `src/hassette/core/core.py` lines 95-115 to get the current service roster. Replace `DataSyncService` with `RuntimeQueryService` + `TelemetryQueryService` in both prose and Mermaid diagram. Add `DatabaseService` (user-configurable via `db_*` fields). Keep the diagram focused on user-relevant services per the user-facing lens.

3. **apps/configuration.md** — Read `src/hassette/app/app_config.py` for ground truth. Fix:
   - `instance_name: str | None` → `instance_name: str = ""`
   - `log_level: str | None` → `log_level: LOG_LEVEL_TYPE` (with factory default)

4. **configuration/global.md** — Read `src/hassette/config/config.py` for all field definitions. Add subsection tables:
   - Database: `db_path`, `db_retention_days`, `db_max_size_mb`
   - Timeouts: `startup_timeout_seconds`, `app_startup_timeout_seconds`, `app_shutdown_timeout_seconds`, `total_shutdown_timeout_seconds`, websocket timeouts
   - Scheduler: `scheduler_min_delay_seconds`, `scheduler_max_delay_seconds`, `scheduler_default_delay_seconds`
   - Logging: all per-service `*_log_level` fields as a single table with link to log-level tuning page
   - Bus Filtering: `bus_excluded_domains`, `bus_excluded_entities`
   - Production: `allow_reload_in_prod`, `allow_only_app_in_prod`
   - App Detection: `autodetect_apps`, `run_app_precheck`, `allow_startup_if_app_precheck_fails`
   - Web UI: add `web_ui_hot_reload` to existing section
   - Advanced: `hassette_event_buffer_size`, `asyncio_debug_mode`, `watch_files`

5. **web-ui/index.md** — Replace 6-item nav list with 4 routes (Dashboard, Apps, Logs, Sessions). Update description text for each item to match current UI. Update Configuration Quick Reference table if it exists (add `web_ui_hot_reload`).

6. **web-ui/dashboard.md** — Rewrite to describe current Preact SPA layout. Read `frontend/src/pages/dashboard.tsx` for the actual structure. Cover: KPI strip (health cards), app grid with telemetry (invocations, errors, error rate, instance count), recent errors feed with session scope.

7. **web-ui/apps.md** — Remove the `dev_mode`/`allow_reload_in_prod` requirement note. Update column descriptions for Preact SPA. Confirm no doc page claims app actions require `dev_mode`.

8. **web-ui/logs.md** — Add Source column (`func_name:lineno` with hover), multi-column sort, auto-pause on sort.

9. **bus/index.md** — Add one-line note to rate-limiting section: "Both `debounce` and `throttle` must be positive; zero or negative values raise `ValueError` at registration."

10. **scheduler/methods.md** — Add "Idempotent Registration" section covering job name uniqueness, `if_exists="skip"` parameter, default behavior `if_exists="error"`. Check if `run_once` exists in the scheduler and ensure it's covered.

11. Run `uv run mkdocs build --strict` and grep for `DataSyncService` in docs/

## Test Strategy

N/A — no testable code changes. Verification is `mkdocs build --strict` + stale reference grep.

## Review Guidance

- **User-facing lens**: no internal implementation details should appear in any doc page. Reject mentions of Alembic internals, TelemetryRepository, CommandExecutor architecture, domain model layer.
- **global.md**: skip deeply internal fields per the design doc's explicit list (e.g., `file_watcher_step_milliseconds`, `service_restart_backoff_multiplier`)
- **Code examples**: verify `where=P.to_state.is_on` is the correct keyword by checking `bus.py` signature — do not guess
- **dashboard.md**: the rewrite must match the actual Preact SPA layout, not the old htmx layout. Read the frontend components to verify.
- **apps.md**: confirm no doc page claims `dev_mode` or `allow_reload_in_prod` is required for app actions

## Activity Log

- 2026-04-01T23:30:00Z — system — lane=planned — WP created
- 2026-04-02T04:44:49Z — system — lane=doing — moved from planned
- 2026-04-02T04:59:30Z — system — lane=done — moved from doing
