---
work_package_id: "WP07"
title: "Rewrite e2e tests, delete old UI layer, update CI and docs"
lane: "planned"
plan_section: "Test Strategy"
depends_on: ["WP04", "WP05", "WP06"]
---

## Objectives & Success Criteria

- All 10 Playwright e2e test files are rewritten with updated selectors targeting the Preact SPA.
- E2e test server enables WebSocket (`ws="auto"` instead of `ws="none"`).
- E2e tests use `runtime_query_service.broadcast()` to send real WS messages and assert the SPA reacts correctly.
- The critical regression test passes: expand a handler row, broadcast `app_status_changed`, verify row stays expanded.
- Old UI layer is fully deleted: `web/ui/` directory, `web/templates/` directory, `web/static/js/*.js`, `web/static/css/style.css`.
- `tests/integration/test_web_ui.py` is deleted (replacement coverage exists in `test_web_api.py` telemetry tests + Playwright).
- `tools/check_template_patterns.py` is deleted.
- `design/specs/010-alpine-morph-fix/` contents are marked superseded.
- `design/interface-design/system.md` is archived to `system.md.archived`.
- `pyproject.toml` removes `jinja2` from dependencies.
- `CLAUDE.md` is updated with new frontend development instructions (Vite dev server, component structure, test commands).
- `web/ui/context.py` re-exports are removed (all consumers now import from `telemetry_helpers.py` or are deleted).

## Subtasks

1. Update `tests/e2e/conftest.py`: change `ws="none"` to `ws="auto"` (line 544). Add broadcast test helper fixture.
2. Rewrite `tests/e2e/test_navigation.py` — sidebar navigation between all 5 pages via SPA routing.
3. Rewrite `tests/e2e/test_dashboard.py` — verify KPI strip, app grid, error feed render with test data.
4. Rewrite `tests/e2e/test_apps_list.py` — verify manifest list, status filters, action buttons.
5. Rewrite `tests/e2e/test_app_detail.py` — verify health strip, handler/job expand/collapse, invocation/execution history.
6. Rewrite `tests/e2e/test_websocket.py` — use real WS broadcast: send `app_status_changed`, verify badge updates. **Critical test**: expand handler row, broadcast status change, verify row stays expanded.
7. Rewrite `tests/e2e/test_logs.py` — verify log table rendering, filtering, real-time streaming via WS.
8. Rewrite `tests/e2e/test_theme.py` — verify dark/light theme toggle.
9. Rewrite `tests/e2e/test_apps.py` — verify app lifecycle actions.
10. Rewrite `tests/e2e/test_hot_reload.py` — verify Vite HMR replaces the old `dev_reload` WS message (or remove if HMR is not testable in e2e).
11. Delete `src/hassette/web/ui/` directory (all 4 files: `router.py`, `partials.py`, `context.py`, `__init__.py`).
12. Delete `src/hassette/web/templates/` directory (all 26 template files).
13. Delete `src/hassette/web/static/js/` (3 files: `ws-handler.js`, `live-updates.js`, `log-table.js`).
14. Delete `src/hassette/web/static/css/style.css`.
15. Delete `tests/integration/test_web_ui.py`.
16. Delete `tools/check_template_patterns.py`.
17. Archive `design/interface-design/system.md` to `system.md.archived`.
18. Remove `jinja2` from `pyproject.toml` dependencies.
19. Remove Jinja2 imports from `web/app.py` (the `from hassette.web.ui import templates` import and related).
20. Update `CLAUDE.md` — replace frontend development instructions with Vite/Preact workflow, component structure, test commands (`npm run dev`, `npm run build`, `npm test`, `uv run nox -s e2e`).

## Test Strategy

- **All rewritten e2e tests must pass**: `uv run nox -s e2e`.
- **All existing integration and unit tests must still pass**: `uv run nox -s dev`.
- **Critical regression test** in `tests/e2e/test_websocket.py`:
  - `test_expanded_row_survives_ws_update` — navigate to app detail, expand a handler row, broadcast `app_status_changed` via the test fixture, verify the expanded row is still open and invocation history is visible.
- **Coverage verification**: no function that was tested in `test_web_ui.py` should lose coverage. The telemetry endpoint tests (WP02) and Playwright tests (this WP) must cover everything.

## Review Guidance

- **BLOCKER**: The `test_expanded_row_survives_ws_update` e2e test MUST pass. This is the entire reason for the migration.
- Verify `ws="auto"` is used in conftest (not `ws="none"`).
- Verify no imports of `web/ui/` remain in the codebase after deletion (grep for `from hassette.web.ui`).
- Verify `jinja2` is removed from `pyproject.toml` dependencies.
- Verify `CLAUDE.md` accurately describes the new frontend workflow.
- Verify all deleted files are actually gone (not just emptied).

## Activity Log

- 2026-03-20T00:00:00Z — system — lane=planned — WP created
