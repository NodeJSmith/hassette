## Status: APPROVED

> All 14 FRs and 11 ACs are covered with matching Verify items; context.md has all five required sections non-empty; no contradictions with the design doc or its Non-Goals found.

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|---|---|---|
| FR#1 | T01 | `enrich_jobs_with_live_heap` exists in `web/utils.py`, snapshots once, and returns unenriched `db_jobs` (with a warning log) on `OSError\|RuntimeError\|ValueError`. |
| FR#2 | T01 | `all_jobs` and `app_jobs` call the helper; no inline snapshot/enrich/fallback block remains; their DB-fetch `except DB_ERRORS` is untouched. |
| FR#3 | T02 | A committed classification table sorts all 17 sites into A/B/C/D with file:line, status, and default per site; `get_listener_metrics` is marked deferred to #1095. |
| FR#4 | T02 | `db_degrades_to(response)` exists in `dependencies.py`, catches `DB_ERRORS`, logs with `exc_info`, sets 503, and does not force a return. |
| FR#5 | T03 | All category-A/B sites use `db_degrades_to`; category-B post-query work is inside the block; category-C/D and `get_listener_metrics` are untouched. |
| FR#6 | T03 | `web/CLAUDE.md` documents the minimal shape, post-query-work shape, the both-paths warning, and the category-C/D exceptions. |
| FR#7 | T04 | `TelemetryUnavailableError(HassetteError)` defined in `exceptions.py`. |
| FR#8 | T04 | Every read path (including the `get_all_app_summaries` bypass) translates the storage tuple to `TelemetryUnavailableError`; a `self._db.execute`-outside-`execute()` audit shows no uncovered read path. |
| FR#9 | T04 | `db_degrades_to` and the category-C/D catches catch `TelemetryUnavailableError`; `DB_ERRORS` no longer appears anywhere in `web/`. |
| FR#10 | T04 | A non-DB `ValueError` in a handler body returns HTTP 500 (test asserts this). |
| FR#11 | T05 | Two methods (`get_listener_summary`, `get_job_summary`) with optional `app_key`/`instance_index` replace four; `get_all_listeners_summary` and `get_all_jobs_summary` deleted; `app_key is None` omits the filter. |
| FR#12 | T05 | `bus.py` guard is `if app_key is None:`; `get_listener_metrics` migrated to `db_degrades_to`. |
| FR#13 | T05 | All production + ~25 test call sites updated; dispatch-assertion tests rewritten as argument-based; `web_mocks.py` and `tests/e2e/mock_fixtures.py` attribute assignments updated. |
| FR#14 | T06 | `telemetry_repository.py` lives at `core/telemetry/repository.py`; all 6 importers updated; no compat shim; a repo-wide grep for the old path returns nothing. |
| AC#1 | T01 | Existing heap-failure route tests stay green; a new unit test exercises the helper's success and fallback paths. |
| AC#2 | T03 | The classification table exists and no category-C/D site's HTTP status code changed. |
| AC#3 | T02 | A unit test confirms the CM sets 503 on a `DB_ERRORS` member, passes success through untouched, and does not suppress unrelated exceptions. |
| AC#4 | T03 | Existing per-route 503/default and 200/partial degradation tests stay green. |
| AC#5 | T03 | The "DB_ERRORS Catch Pattern" section reflects the four documented points. |
| AC#6 | T04 | Forced storage error in `get_all_app_summaries` degrades `dashboard_app_grid` to 200-partial, not 500. |
| AC#7 | T04 | HTTP layer catches only `TelemetryUnavailableError`; the footgun-fixed test passes. |
| AC#8 | T05 | Full suite green after consolidation; no reference to the deleted method names remains. |
| AC#9 | T05 | A test asserts `?app_key=` (empty string) returns empty/422, not all-apps data. |
| AC#10 | T06 | `uv run pyright` is clean after the move. |
| AC#11 | T06 | Across the cluster — zero `scripts/export_schemas.py --types` diff, Pyright clean, and `nox -s system` + `nox -s e2e` pass locally. |

## Coverage Gaps

None.

## Contradictions

None.

The deliberate behavior change (FR#10: non-DB `ValueError` now surfaces as HTTP 500) is correctly scoped to T04 only. T01, T02, and T03 correctly claim byte-for-byte / behavior-identical results for their respective units. No task attempts to move `telemetry_models.py` or extract a SessionManager repository, consistent with the Non-Goals.

The task sequencing (T01 → T02 → T03 → T04 → T05 → T06) matches the design's mandated order (#1107 → #1108a → #1108b → #1095), with #1108a split across T02 (classify + add CM) and T03 (migrate handlers) and #1095 split across T05 (query consolidation + guard) and T06 (repository move).

## Warnings

None.
