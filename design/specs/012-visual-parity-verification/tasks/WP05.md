---
work_package_id: "WP05"
title: "Final verification pass and sign-off"
lane: "planned"
plan_section: "Phase 6: Implementation + Visual Verification Loop"
depends_on: ["WP04"]
---

## Objectives & Success Criteria

- A final full 42-scenario capture of the new UI is taken after all fixes.
- A comparison agent runs against old + new screenshots one final time.
- Zero GAP or REGRESSION items remain — all are either Fixed or explicitly accepted as DIFFERENT.
- The Docker image is rebuilt and deployed to the homelab.
- The gap checklist is fully checked off and committed.

## Subtasks

1. Rebuild the Preact SPA: `cd frontend && npm run build`.
2. Rebuild the Docker image: `docker build -t hassette:ui-rebuild .`
3. Redeploy: `docker compose -f /home/jessica/homelab/hautomate/docker-compose.yml up -d hassette`.
4. Wait for healthy.
5. Launch a final screenshotter agent capturing all 42 scenarios to `design/audits/visual-parity/final/`.
6. Launch a comparison agent comparing `old/` vs `final/` screenshots.
7. Confirm zero remaining GAPs/REGRESSIONs.
8. Commit the gap checklist with all items checked off.
9. Push all changes.

## Test Strategy

- Final comparison must produce zero GAP or REGRESSION findings.
- If any remain, loop back to WP04 for fixes before re-running this WP.
- `uv run nox -s dev` and `npm test` must pass.

## Visual Verification

| Page | Setup | Verify |
|------|-------|--------|
| All pages | Full 42-scenario capture | Every scenario matches the old UI structurally. No missing components, sections, or interactive states. |

## Review Guidance

- This WP is the final gate. The gap checklist must be 100% resolved.
- The comparison agent's output should show only DIFFERENT and IMPROVEMENT entries — no GAPs or REGRESSIONs.
- The deployed Docker image must be visually verified by the user via the live URL.

## Activity Log

- 2026-03-21T00:00:00Z — system — lane=planned — WP created
