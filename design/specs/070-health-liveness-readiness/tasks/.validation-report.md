# Plan Validation Report

**Design doc:** `design/specs/070-health-liveness-readiness/design.md`
**Tasks:** T01, T02, T03, T04
**context.md:** present

---

## Status: APPROVED

---

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|---|---|---|
| FR#1 | T01 | `ever_connected` is `False` before any connection, becomes `True` after a `CONNECTED` transition, and remains `True` after a subsequent disconnect (unit test). |
| FR#2 | T01 | `get_system_status().status == "degraded"` when `ever_connected` is set and WS is not ready; `== "starting"` when never set (unit test). |
| FR#3 | T01 | `get_system_status().status == "ok"` when `websocket_service.is_ready()` is `True` (unit test). |
| FR#4 | T02 | `GET /api/health/live` returns 200 with the liveness body, with no dependency or service-state check in the handler. |
| FR#5 | T02 | `GET /api/health/ready` returns 200 when `get_system_status().status == "ok"` and 503 otherwise. |
| FR#6 | T02 | `GET /api/health` returns 200 for `ok`, `degraded`, and `starting`; the handler sets no 503. |
| FR#7 | T03 | A failure-driven shutdown (PERMANENT exhaustion / fatal error / startup failure) results in a non-zero process exit; an operator SIGTERM results in exit 0. |
| FR#8 | T02 | `hassette status` against a `degraded`/`starting` instance prints the status object without an error envelope or non-zero exit attributable to the health code. |
| FR#9 | T03 | After a fatal service crash, the active session row in the telemetry DB carries the failure status, written before shutdown completes (assert on the persisted row, not logs). |
| AC#1 | T01 | With the latch set and the WS reporting not-connected, `get_system_status().status == "degraded"` (reboot-loop regression test). |
| AC#2 | T01 | With the latch never set and the WS not connected, `get_system_status().status == "starting"`. |
| AC#3 | T02 | `GET /api/health` returns 200 with body `status == "degraded"` for a booted, WS-disconnected instance. |
| AC#4 | T02 | `GET /api/health/ready` returns 503 for `degraded`/`starting` and 200 for `ok`. |
| AC#5 | T02 | `GET /api/health/live` returns 200 while the WS is disconnected and during the startup window (response independent of HA/service state). |
| AC#6 | T02 | `GET /api/health` never returns 503 from the handler. |
| AC#7 | T02 | `openapi.json` and frontend types regenerate cleanly and `npm run build` passes. |
| AC#8 | T02 | `hassette status` prints `degraded`/`starting` without a non-zero exit attributable to the health code. |
| AC#9 | T02 | `GET /api/health` returns 200 with body `status == "ok"` when the WS is connected. |
| AC#10 | T03 | A fatal shutdown (a PERMANENT service exhausting its budget) causes a non-zero process exit code. |
| AC#11 | T03 | An operator/SIGTERM shutdown causes exit code 0. |
| AC#12 | T03 | After a fatal crash, the session row in the telemetry DB carries the failure status, written before shutdown completes. |

---

## Coverage Gaps

None.

All nine FRs (FR#1–FR#9) and all twelve ACs (AC#1–AC#12) are covered by at least one task's `implements` list, and each has a corresponding binary-verifiable Verify item in that task.

---

## Contradictions

None.

---

## Warnings

1. **T04 Verify items are inspection-verifiable, not assertion-verifiable.** The criteria ("has a 'restarts when HA goes down' entry", "A health-endpoint reference documents all three endpoints") are confirmable by reading the output files — they pass the binary bar — but they cannot be checked by running a command the way T01–T03 criteria can. `mkdocs build` succeeds is the only mechanical check. This is expected for a documentation task and does not block.

2. **context.md "Visual Artifacts" section contains only "None."** The five required sections are all present and non-empty. The "None." value satisfies the structural requirement; noted in case the validator has a stricter interpretation of "non-empty."

3. **T01 test-utils change (`create_hassette_stub`) is in `implements: ["FR#1", ...]` but is a supporting infrastructure change.** The stub change is described in T01's Prompt (step 3) and correctly supports FR#1/AC#1 by making the `ever_connected` attribute explicit. No action required — just noting that the test-infra edit is bundled into T01 rather than a separate task, which is appropriate given its small scope.
