## Status: APPROVED

> APPROVED — all FRs and ACs are covered, no contradictions, all Verify items are concrete. Two minor warnings noted but do not block execution.

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1 | T01 | "`CLIContext` is a frozen dataclass with fields `json_mode`, `debug_mode`, `env_file_override`, `config_file_override` and correct defaults" |
| FR#2 | T01 | "The launcher uses `app.parse_args(tokens)` + `bound.arguments["ctx"] = ctx` + `command(*bound.args, **bound.kwargs)` instead of `app(tokens)`" |
| FR#3 | T02 | "Every command function that previously read `cli_globals.json_mode` now has `ctx: Annotated[CLIContext, Parameter(parse=False)]` in its signature" |
| FR#4 | T01 | "`make_client` requires an explicit `CLIContext` parameter and reads all config from it (no `cli_globals` references in `make_client`)" |
| FR#5 | T03 | "`src/hassette/cli/globals.py` does not exist and `grep -r \"cli_globals\|cli\.globals\" src/` returns no matches" |
| AC#1 | T03 | "No module named `globals` exists in the CLI package" |
| AC#2 | T02 | "No test file contains `patch(\"hassette.cli.globals.*\")` — verified by `grep -r \"cli.globals\|cli_globals\" tests/`" |
| AC#3 | T02 | "All 15 command functions that previously read globals have `ctx` in their signature" |
| AC#4 | T01 | "`make_client(ctx: CLIContext)` has no fallback to module state — calling `make_client()` with no arguments raises `TypeError`" |
| AC#5 | T02 | "`timeout 300 uv run pytest tests/unit/cli/ -n 2` passes with zero failures" |
| AC#6 | T03 | "`uv run pyright src/hassette/cli/` reports no type errors related to this change" |

## Coverage Gaps

None.

## Contradictions

None.

## Warnings

- **T01 test count diverges from design doc:** T01 Prompt defines 5 tests (`test_defaults`, `test_frozen_raises_on_mutation`, `test_make_client_receives_json_mode`, `test_make_client_receives_debug_mode`, `test_launcher_injects_ctx`). The design doc Test Strategy lists 4 (`test_defaults`, `test_frozen`, `test_make_client_receives_context`, `test_launcher_injects_ctx`). T01 splits the single `test_make_client_receives_context` into two tests and renames `test_frozen` → `test_frozen_raises_on_mutation`. The additional granularity is harmless and the coverage is equivalent, but the names diverge from the design doc's specification.

- **AC#3 Verify item claims 15 functions; enumerated count is 14:** T02's Verify item states "All 15 command functions that previously read globals have `ctx` in their signature." Counting the functions explicitly listed in T02's Prompt: `cmd_status`, `cmd_telemetry`, `cmd_dashboard` (3) + `cmd_app`, `cmd_app_health`, `cmd_app_activity`, `cmd_app_config`, `cmd_app_source` (5) + `cmd_job` (1) + `cmd_listener` (1) + `cmd_log`, `cmd_execution` (2) + `cmd_config`, `cmd_event` (2) = 14. The discrepancy is minor — either the Verify item is overcounting by one, or there is a 15th command function not named in the Prompt. Verify the actual count against the source files before marking AC#3 complete.
