---
depends_on: []
lane: done
plan_section: 'Shared Type: `SourceTier`'
title: Add SourceTier type, fresh schema migration, and config field
work_package_id: WP01
---

## Objectives & Success Criteria

- `SourceTier = Literal['app', 'framework']` is defined in `src/hassette/types/types.py` and importable.
- A single fresh migration (`007_source_tier_unification.py`) replaces the logical intent of migrations 001-006 with a clean schema containing: `source_tier` column on all 5 tables, nullable `listener_id`/`job_id` FKs, `is_di_failure` flag, CHECK constraints on `status`/`duration_ms`/`source_tier`, and updated views (`active_app_listeners`, `active_app_scheduled_jobs`, `active_framework_listeners`, `active_framework_scheduled_jobs`, plus backward-compatible `active_listeners`/`active_scheduled_jobs`).
- `DatabaseService.on_initialize()` detects schema version mismatch and auto-recreates the database (deletes file + reruns migrations) with a WARNING log. Refuses to auto-delete if the DB version is ahead of the code's head revision.
- `HassetteConfig` gains `telemetry_write_queue_max: int = 1000`.
- Existing migrations 001-006 are deleted (destructive migration strategy — no backward compat).
- All existing tests pass (schema changes are backward-compatible at the migration level since we recreate).

## Subtasks

1. Define `SourceTier = Literal['app', 'framework']` in `src/hassette/types/types.py`.
2. Delete migration files `001_initial_schema.py` through `006_preserve_registrations.py`.
3. Write `007_source_tier_unification.py` with the complete fresh schema: all 5 tables with `source_tier`, nullable FKs, `is_di_failure`, CHECK constraints, all indexes, all 6 views.
4. Add schema version mismatch detection in `DatabaseService.on_initialize()`: if DB file exists and Alembic `current` != expected head, delete DB file and rerun migrations. If DB version is ahead of code's head, log ERROR and halt startup. Handle `FileNotFoundError`/`PermissionError` on delete.
5. Add `telemetry_write_queue_max: int = 1000` to `HassetteConfig` in `src/hassette/config/config.py`.
6. Update `alembic.ini` or migration env to point to the new migration as head.

## Test Strategy

- **`tests/unit/test_schema_migration.py`**: New test file.
  - `test_source_tier_type_is_literal` — verify `SourceTier` is `Literal['app', 'framework']`.
  - `test_fresh_migration_creates_all_tables` — run migration against in-memory SQLite, verify all 5 tables exist with `source_tier` column.
  - `test_check_constraints_reject_invalid_status` — INSERT with `status='invalid'`, verify `IntegrityError`.
  - `test_check_constraints_reject_negative_duration` — INSERT with `duration_ms=-1.0`, verify `IntegrityError`.
  - `test_check_constraints_reject_invalid_source_tier` — INSERT with `source_tier='invalid'`, verify `IntegrityError`.
  - `test_nullable_listener_id_allows_null` — INSERT `handler_invocations` with `listener_id=NULL`, verify success.
  - `test_views_filter_by_tier` — INSERT listeners with both tiers, query each view, verify correct filtering.
  - `test_db_version_mismatch_recreates` — mock a version mismatch, verify DB file is deleted and recreated.
  - `test_db_version_ahead_halts_startup` — mock a version-ahead scenario, verify startup halts with ERROR log.

## Review Guidance

- Verify the migration produces the exact schema from the design doc (all CHECK constraints, all views, nullable FKs).
- Verify `is_di_failure` column exists on both `handler_invocations` and `job_executions`.
- Verify the `sessions` table defaults `source_tier` to `'framework'`, not `'app'`.
- Verify existing migrations are deleted, not just ignored.
- Verify the version-ahead guard refuses auto-delete (this is a safety gate for future releases).

## Activity Log

- 2026-04-07T00:00:00Z — system — lane=planned — WP created
- 2026-04-08T03:30:01Z — system — lane=doing — moved from planned
- 2026-04-08T03:59:05Z — system — lane=done — moved from doing
