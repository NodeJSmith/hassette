---
work_package_id: "WP02"
title: "Extract shared icons module"
lane: "planned"
plan_section: "Issue #356 — Extract shared icons module"
depends_on: []
---

## Objectives & Success Criteria

- New file `frontend/src/components/shared/icons.tsx` exports all SVG icon components
- Inline SVG definitions removed from `app-detail.tsx`, `logs.tsx`, `sidebar.tsx`, `apps.tsx`, `action-buttons.tsx`
- Each icon preserves its original SVG attributes (fill-based sidebar icons stay fill-based, stroke-based icons stay stroke-based)
- All icons have `class="ht-icon-svg"` and their original `viewBox`
- No visual regressions — icons render identically

## Subtasks

1. Create `frontend/src/components/shared/icons.tsx`. Extract these icons from their current locations:
   - `IconLayers` from `app-detail.tsx:19-22` (stroke-based)
   - `IconBell` from `app-detail.tsx:24-27` (stroke-based)
   - `IconClock` from `app-detail.tsx:29-32` (stroke-based)
   - `IconScroll` from `app-detail.tsx:34-38` (stroke-based)
   - `IconPlay` from `action-buttons.tsx:11-15` (stroke-based)
   - `IconSquare` from `action-buttons.tsx:17-21` (stroke-based)
   - `IconRefresh` from `action-buttons.tsx:23-28` (stroke-based)
   - `IconDashboard` from `sidebar.tsx:10-15` (fill-based, no stroke)
   - `IconBoxes` from `sidebar.tsx:23-37` (fill-based, no stroke)
   - `IconScrollText` from `sidebar.tsx:46-51` (fill-based, no stroke)
   - `IconWarning` from `dashboard.tsx:69-71` (stroke-based)
   - `IconHeart` from `dashboard.tsx:52-54` (stroke-based)
   - `IconGrid` from `apps.tsx:27-31` (stroke-based)
2. Update `pages/app-detail.tsx`: remove inline icon definitions (lines 19-38), import from `icons.tsx`.
3. Update `pages/logs.tsx`: remove inline SVG (lines 12-15), import `IconScrollText` from `icons.tsx`.
4. Update `components/layout/sidebar.tsx`: remove inline SVG definitions in NAV_ITEMS, import from `icons.tsx`.
5. Update `pages/apps.tsx`: remove inline SVG (lines 26-31), import from `icons.tsx`.
6. Update `components/apps/action-buttons.tsx`: remove inline icon definitions (lines 11-28), import from `icons.tsx`.
7. Update `pages/dashboard.tsx`: remove inline SVGs, import `IconHeart` and `IconWarning` from `icons.tsx`.

## Test Strategy

- No new unit tests needed — icons are pure presentational components with no logic.
- Verify via visual verification that all icons render correctly.
- ESLint must pass (no unused imports left behind from extraction).

## Review Guidance

- Verify sidebar icons preserve fill-based attributes (no `stroke="currentColor"` added)
- Verify stroke-based icons preserve their stroke attributes
- Verify no inline SVG definitions remain in the updated files
- BLOCKER if: any icon visually changes (wrong attributes transferred)

## Visual Verification

| Page | Setup | Verify |
|------|-------|--------|
| Dashboard | Load the dashboard | Heart icon (App Health heading) and Warning icon (Recent Errors heading) render correctly |
| Apps list | Navigate to /apps | Grid icon in page heading renders correctly |
| App Detail | Navigate to any app | All 4 section icons (Layers, Bell, Clock, Scroll) render correctly |
| Logs | Navigate to /logs | Scroll icon in page heading renders correctly |
| Sidebar | Any page | All 3 nav icons (Dashboard, Boxes, Scroll) render correctly with correct fill |

## Pitfalls to Avoid

- Do not normalize fill/stroke attributes across icon styles. Sidebar icons use fill-based paths; other icons use stroke-based paths.

## Activity Log

- 2026-03-22 — system — lane=planned — WP created
