---
depends_on:
- WP03
lane: done
plan_section: 'Part 3: Decouple RuntimeQueryService from Web Models'
title: Decouple RuntimeQueryService from web.models
work_package_id: WP04
---

## Objectives & Success Criteria

- `RuntimeQueryService` has zero imports from `hassette.web.models`
- `get_system_status()` returns `SystemStatus` (from `core/domain_models`)
- `get_app_status_snapshot()` returns `AppStatusSnapshot` (from `core/app_registry`)
- `get_all_manifests_snapshot()` returns `AppFullSnapshot` (from `core/app_registry`)
- WS event handlers (`_on_state_change`, `_on_app_state_changed`, `_on_service_status`, `_on_ws_connected`, `_on_ws_disconnected`) use domain model types from `core/domain_models` with direct field construction
- `_serialize_payload()` helper is deleted entirely
- All existing unit tests in `test_runtime_query_service.py` pass (with updated assertions)

## Subtasks

1. Change `get_system_status()` to return `SystemStatus` instead of `SystemStatusResponse` — construct `SystemStatus` with all 6 fields including `services_running` from `self.hassette.children`
2. Change `get_app_status_snapshot()` to return the `AppStatusSnapshot` from `app_registry` directly, instead of constructing `AppStatusResponse` inline
3. Change `get_all_manifests_snapshot()` to return the `AppFullSnapshot` from `app_registry` directly, instead of constructing `AppManifestListResponse` inline
4. Rewrite `_on_state_change()` to construct `StateChangedData` directly from event fields
5. Rewrite `_on_app_state_changed()` to construct `AppStatusChangedData` directly from `event.payload.data` fields with explicit `.value` coercion for `ResourceStatus` enums — preserve `hasattr(event, "payload")` guard
6. Rewrite `_on_service_status()` to construct `ServiceStatusData` directly from `event.payload.data` fields — preserve `hasattr(event, "payload")` guard
7. Rewrite `_on_ws_connected()` and `_on_ws_disconnected()` to construct `ConnectivityData` directly
8. Delete `_serialize_payload()` helper function
9. Remove all `from hassette.web.models import ...` lines
10. Update `tests/unit/core/test_runtime_query_service.py` — change `isinstance` assertions to domain types, update imports

## Test Strategy

**Updated tests:** `tests/unit/core/test_runtime_query_service.py`
- Update `isinstance` checks from `AppStatusResponse` → verify `AppStatusSnapshot` return type
- Update `isinstance` checks from `SystemStatusResponse` → verify `SystemStatus` return type
- Update imports from `hassette.web.models` to `hassette.core.domain_models` and `hassette.core.app_registry`
- Verify `get_system_status()` result has `services_running` field populated
- Verify WS event handlers produce domain model payloads (not web model payloads)

All 13 existing tests should pass with assertion type updates only — no behavioral changes.

## Review Guidance

- **Zero web.models imports**: After this WP, `grep "from hassette.web.models" src/hassette/core/runtime_query_service.py` must return nothing.
- **hasattr guards preserved**: `_on_app_state_changed` and `_on_service_status` must keep `if not hasattr(event, "payload"): return`.
- **Enum coercion**: Event handlers constructing `AppStatusChangedData` and `ServiceStatusData` must use `.value` on `ResourceStatus` enum fields, not pass enums directly.
- **_serialize_payload() deleted**: The triple-roundtrip (`asdict` → `json.dumps` → `json.loads` → `model_validate`) must be completely removed, not just unused.
- **services_running**: `get_system_status()` must populate `SystemStatus.services_running` from `self.hassette.children` — verify the field is not silently dropped.

## Activity Log

- 2026-03-30T16:00:00Z — system — lane=planned — WP created
- 2026-03-30T23:18:55Z — system — lane=doing — moved from planned
- 2026-03-30T23:39:26Z — system — lane=done — moved from doing
