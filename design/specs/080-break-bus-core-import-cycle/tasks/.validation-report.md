## Status: APPROVED

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1 | T01 | `InvokeHandler` and `ExecuteJob` import successfully from `hassette.commands`, and that module's only runtime `hassette.*` import is `hassette.types` (no runtime `bus`/`scheduler`/`events`/`core` import). |
| FR#2 | T01 | `grep -rn "core.commands" src/ tests/` returns zero hits and `src/hassette/core/commands.py` no longer exists. |
| FR#3 | T01 | No file under `src/hassette/bus/` has a runtime (non-`TYPE_CHECKING`) import of `hassette.core` or `hassette.core.*` (confirm via `python tools/check_module_boundaries.py` exiting 0 and manual check of `bus/invocation.py`). |
| FR#4 | T02 | `check_source("from hassette.core import Hassette\n", "bus")` returns a `bus-no-core` violation (asserted by `test_bus_import_of_core_flagged`). |
| FR#5 | T02 | `python tools/check_module_boundaries.py` exits 0 on the post-fix tree and `test_real_src_files_pass` passes. |
| AC#1 | T02 | `python tools/check_module_boundaries.py` exits 0 and its OK summary reports one additional rule versus before this task. |
| AC#2 | T02 | Reverting only the `invocation.py` import fix makes `python tools/check_module_boundaries.py` fail with a `bus-no-core` violation; restoring it returns the guard to green. |
| AC#3 | T01 | `grep -rn "core.commands" src/ tests/` returns no production or test hits. |
| AC#4 | T01 | `python -c "import hassette"` succeeds and the unit+integration suites pass with no failures. |
| AC#5 | T01 | `uv run pyright` reports no errors. |

## Coverage Gaps

None.

## Contradictions

None.

## Warnings

1. **T01 Verify for FR#3 references `python tools/check_module_boundaries.py` exiting 0 before the `bus-no-core` rule exists.** At the point T01 runs, the boundary tool does not yet have the `bus-no-core` rule (that is added in T02). The tool will exit 0 both before *and* after the fix at T01 time, making this check vacuously true as a proof of FR#3 specifically. The Verify item mitigates this by adding "and manual check of `bus/invocation.py`" — so the intent is captured. Still, an executor reading only T01 could mistake a pre-fix green exit for confirmation of FR#3 without performing the manual check. Consider making the manual inspection step the primary criterion in T01's FR#3 Verify item, with the tool exit as secondary context.

2. **context.md "Visual Artifacts" section value is "None."** This is valid per the validation rules (present-with-"None" is an accepted form) and correct for this design (no visual artifacts defined). Noted for completeness; not a defect.
