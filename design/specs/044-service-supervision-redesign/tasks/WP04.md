---
work_package_id: "WP04"
title: "Remove global restart config fields and update references"
lane: "planned"
depends_on: ["WP02", "WP03"]
---

## Objectives & Success Criteria

Remove the 5 global `service_restart_*` configuration fields from `HassConfig` and update all references throughout the codebase. After this WP, no code references these fields — all restart behavior is driven by per-service `RestartSpec`.

- `service_restart_max_attempts`, `service_restart_backoff_seconds`, `service_restart_backoff_multiplier`, `service_restart_max_backoff_seconds`, `service_restart_readiness_timeout_seconds` are removed from `HassConfig` in `src/hassette/config/config.py`
- `service_watcher_log_level` is preserved (orthogonal to restart policy)
- `WebsocketService` logging at line 151 no longer references `config.service_restart_max_attempts`
- `tests/integration/test_service_watcher.py` fixtures no longer reference these config fields (already handled by WP02's test rewrite, but verify)
- `tests/system/test_shutdown.py` — update any `config.model_copy()` calls that set these fields
- `tests/unit/test_config_log_level.py` — verify no breakage from field removal
- All tests pass with the fields removed

## Subtasks

1. Remove the 5 `service_restart_*` field definitions from `HassConfig` in `src/hassette/config/config.py`
2. Update `WebsocketService` logging at `src/hassette/core/websocket_service.py:151` — remove or replace the reference to `config.service_restart_max_attempts` (use the service's own `restart_spec.budget_intensity` for the log message instead)
3. Grep the full codebase for `service_restart_` to find any remaining references and update them
4. Update `tests/system/test_shutdown.py` if it sets any removed config fields via `model_copy()`
5. Verify `tests/unit/test_config_log_level.py` still passes (it tests `service_watcher_log_level` which is preserved)

## Test Strategy

No new tests — this WP removes dead code. Verification is that existing tests pass after removal.

Run the full test suite to confirm no references to removed fields remain. Any `AttributeError` on `config.service_restart_*` indicates a missed reference.

## Review Guidance

- Grep for `service_restart_max_attempts`, `service_restart_backoff`, `service_restart_readiness_timeout` across the entire codebase — zero hits expected after this WP
- Verify `service_watcher_log_level` is NOT removed
- Verify the WebsocketService log message update uses the service's own `restart_spec`, not a hardcoded value
