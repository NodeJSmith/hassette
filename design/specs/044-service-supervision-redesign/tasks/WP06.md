---
depends_on:
- WP02
- WP03
- WP04
lane: done
title: Update documentation for service supervision redesign
work_package_id: WP06
---

## Objectives & Success Criteria

All documentation reflects the new service supervision model: RestartSpec, restart types, sliding-window budget, and removed config fields.

- `CLAUDE.md` "Resource Hierarchy" section mentions RestartSpec and restart types
- `docs/` site service lifecycle page documents RestartSpec, restart types, budget behavior, and the three-layer error routing policy
- Migration note documents removal of global config fields
- Docstrings on RestartSpec, RestartType, RestartBudget, and key ServiceWatcher methods are complete

## Subtasks

1. Update `CLAUDE.md` — add RestartSpec mention to the "Resource Hierarchy" section, noting that Services declare a `restart_spec` class attribute for supervision behavior
2. Update the service lifecycle docs page in `docs/` to cover:
   - RestartSpec dataclass and its fields
   - RestartType enum (PERMANENT, TRANSIENT, TEMPORARY) with behavior for each
   - Sliding-window budget: how it works, automatic forgiveness, budget reset on recovery
   - Three-layer error routing: FatalError, fatal_error_names, non_retryable_error_names
   - Per-service declarations: table of all 8 services with their restart specs
   - EXHAUSTED_DEAD and EXHAUSTED_COOLING statuses
3. Add migration note to docs: the 5 global `service_restart_*` config fields are removed; restart behavior is now per-service via RestartSpec
4. Ensure docstrings exist on `RestartSpec`, `RestartType`, `RestartBudget`, and key ServiceWatcher methods (`restart_service`, `_cooldown_and_retry`, `_on_service_running`, `_reconcile_after_bus_recovery`)

## Test Strategy

N/A — documentation-only changes.

## Review Guidance

- Verify the docs page is accurate against the final implementation (not just the design doc — implementation may have minor deviations)
- Verify the migration note is clear about what was removed and what replaces it
- Verify CLAUDE.md update is minimal — just a mention in the existing section, not a rewrite
