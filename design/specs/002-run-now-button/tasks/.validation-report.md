# Plan Validation Report: Run Now Button for Scheduled Jobs

## Verdict: PASS

Zero coverage gaps, zero contradictions, and `context.md` has all five required sections with non-empty content. All identifiers use conforming `FR#N` / `AC#N` format.

---

## Requirement Inventory

**Total FRs: 11** — FR#1, FR#2, FR#3, FR#4, FR#5, FR#6, FR#7, FR#8, FR#9, FR#10, FR#11

**Total ACs: 10** — AC#1, AC#2, AC#3, AC#4, AC#5, AC#6, AC#7, AC#8, AC#9, AC#10

**Identifier format violations:** none found. A regex sweep of the design doc (`grep -nE 'FR[#-]?[0-9]+|AC[#-]?[0-9]+'` excluding conforming matches) returned no non-conforming identifiers.

---

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1 | T02 | "POST `/api/scheduler/jobs/{job_id}/trigger` with no body returns 202 with `JobTriggerResponse`" |
| FR#2 | T02 | "POST with unknown `job_id` returns 409 with descriptive message" |
| FR#3 | T02 | "POST with still-pending one-shot job returns 202 and calls `dequeue_job` before dispatch" |
| FR#4 | T02 | "POST with single-mode guard held returns 409; restart/queued modes return 202" |
| FR#5 | T02 | "Dispatched job executes through `run_job_with_guard()` with `trigger_mode=\"manual\"`" |
| FR#6 | T01 | "`ExecuteJob` constructed with `trigger_mode=\"manual\"` propagates to `ExecutionRecord.trigger_mode` via `build_record()`" |
| FR#7 | T02 | "WebSocket `execution_completed` fires for manual triggers (existing infrastructure, no new test)" |
| FR#8 | T03 | "\"Run Now\" button appears in the job detail panel on the handlers tab" |
| FR#9 | T04 | "Execution table rows display a \"manual\" badge for `trigger_mode=\"manual\"` executions" |
| FR#10 | T04 | "Execution detail panel shows `trigger_mode` value as its own line item when present" |
| FR#11 | T03 | "Button is disabled while a trigger request is in flight" |
| AC#1 | T02 | "Active recurring job trigger returns 202 and dispatches through `run_job_with_guard`" |
| AC#2 | T02 | "One-shot job not on heap returns 409" |
| AC#3 | T02 | "Still-pending one-shot executes and is dequeued" |
| AC#4 | T02 | "Guard pre-check correct per execution mode" |
| AC#5 | T01 | "Unit test confirms `trigger_mode='manual'` appears on the `ExecutionRecord` when passed through the full `run_job_with_guard()` → `run_job()` → `ExecuteJob` → `build_record()` chain" |
| AC#6 | T02 | "Execution appears in frontend via WS (existing infrastructure)" |
| AC#7 | T03 | "Button shows loading spinner during request and is disabled until complete" |
| AC#8 | T03 | "Error message displays inline below button on 409 and 500 responses" |
| AC#9 | T04 | "\"manual\" badge is visually distinguishable in execution rows" |
| AC#10 | T04 | "Detail panel renders trigger_mode independently of the context block" |

Every identifier in a task's `implements` frontmatter has a matching `- [ ]` Verify item in that same task. No identifier is claimed in `implements` without a corresponding Verify line, and no Verify item references an identifier absent from `implements`.

---

## Coverage Gaps

None. All 11 FRs and all 10 ACs are claimed by exactly one task's `implements` field, and each has a corresponding Verify checklist item in that task.

---

## Contradictions

None found. Cross-checked each task's Prompt/Focus text against the design doc's Architecture, Edge Cases, and Key Constraints sections:

- T01's field-threading description (`ExecuteJob`, `run_job_with_guard`, `run_job`, `build_record`) matches the design doc's "`trigger_mode` threading" subsection verbatim, including the parallel-vs-non-parallel lambda-capture distinction.
- T02's route handler step order (lookup → guard pre-check → one-shot dequeue → dispatch → 202 response) matches the design doc's Architecture → "New route" steps 1-5 exactly, including the dequeue-before-dispatch ordering rationale (guard.release() cancels current_task).
- T02's one-shot detection predicate (`job.trigger is None or job.trigger.next_run_time(job.next_run, now) is None`) matches FR#3 and the Edge Cases section word-for-word.
- T02's per-mode guard behavior (PARALLEL/RESTART/QUEUED skip pre-check, SINGLE only pre-checks) matches the "Guard pre-check detail" section.
- T03's button placement (composed into `extras` prop alongside `nextRunText`, not replacing it) matches the design doc's Frontend section.
- T04's badge placement/variant (`info`, positioned like the thread-leaked badge) and the independent (non-context-block) placement of `trigger_mode` in the detail panel match FR#9, FR#10, and AC#10.
- Spot-checked source line references cited in T01/T02 Focus sections (`commands.py` ExecuteJob dataclass, `scheduler_service.py` `run_job_with_guard`/`run_job`/`dequeue_job`, `command_executor.py` `build_record` ExecuteJob case) against the actual repository files — all references point to the correct code regions.

---

## Warnings

1. **Changed-Files list divergence, not a coverage gap.** The design doc's Impact → Changed Files lists `frontend/src/components/app-detail/job-detail.test.tsx` as **create** ("or modify if exists"). T03's Target Files instead lists `modify: frontend/src/components/app-detail/handlers-tab.test.tsx`, and T03's Focus section explains why: "There is no standalone `job-detail.test.tsx` — `JobDetail` is tested via `handlers-tab.test.tsx`." This is a planner correction based on investigating the actual test layout, not an omission — FR#8/FR#11/AC#7/AC#8 frontend test coverage is still fully accounted for. No action needed, but flagging since the file path in the design doc's Changed Files list is now stale relative to the tasks.

2. **No scope violations found.** No task's `implements` field lists an FR/AC identifier absent from the design doc.

3. **Visual Artifacts section says "None" despite a screenshot regeneration being in scope (T04).** This is not a contradiction — `context.md`'s "Visual Artifacts" section is for design mockups/wireframes informing implementation, and the design doc contains no such artifacts (only a screenshot *output* to regenerate after the change, which is a documentation deliverable, not an input design artifact). Noting for awareness only; no fix required.

## context.md Completeness

All required sections present and non-empty:
1. `## Problem & Motivation` — present, substantive.
2. `## Visual Artifacts` — present, contains "None." (valid — no artifacts to reference).
3. `## Key Decisions` — present, 8 enumerated decisions with rationale.
4. `## Constraints & Anti-Patterns` — present, 7 bulleted constraints.
5. `## Design Doc References` — present, points to specific design doc sections.
6. `## Convention Examples` (optional) — present with 5 real code snippets (not "None").

No missing or empty sections.
