---
work_package_id: "WP07"
title: "Documentation updates"
lane: "planned"
depends_on: ["WP03", "WP05"]
---

## Objectives & Success Criteria

The docs site explains `depends_on`, initialization ordering, shutdown ordering, and cycle detection. The `Resource.depends_on` docstring is clear. The dependency graph is documented with a mermaid diagram.

## Subtasks

1. Add a "Service Dependency Graph" section to `docs/pages/core-concepts/index.md`:
   - Explain `depends_on` ClassVar and how it controls initialization order
   - Explain shutdown ordering (reverse of initialization order)
   - Explain cycle detection at `__init__` time and the error message format
   - Include a mermaid diagram showing the framework's dependency graph (matching the design doc's graph)
   - Code example showing how a service declares its dependencies
   - Note that `depends_on` is scoped to Hassette-level direct children
2. Add a one-line docstring to `Resource.depends_on` in `src/hassette/resources/base.py`:
   ```python
   depends_on: ClassVar[list[type["Resource"]]] = []
   """Resource types that must be ready before this resource initializes."""
   ```

## Test Strategy

N/A — documentation-only changes. Verify `uv run mkdocs build` completes without errors.

## Review Guidance

- The mermaid diagram must match the actual `depends_on` declarations from WP03 — verify all edges are present
- The docs page must explain the `ready_event.wait()` distinction (coordinator gate vs. service dep) briefly, to prevent users from confusing the two mechanisms
- Code examples must use the actual pattern (`depends_on: ClassVar[list[type[Resource]]] = [SomeService]`), not a simplified pseudocode
- The docstring must not be a multi-line comment block — one line per project conventions
