---
task_id: "T08"
title: "Update docs, tooling, sync facade, and codegen"
status: "done"
depends_on: ["T06", "T07"]
implements: ["AC#10"]
---

## Summary

Migrate all documentation, doc snippets, tooling scripts, the sync facade, and codegen to use the new `Job`/`remove_*`/`removed_at` names. This is the mechanical rename sweep across files outside the core scheduler/web/frontend modules, ensuring nothing silently refers to the old API.

## Target Files

- modify: `src/hassette/scheduler/sync.py`
- modify: `codegen/src/hassette_codegen/sync_facade/generic.py`
- modify: `src/hassette/bus/sync.py`
- modify: `src/hassette/web/CLAUDE.md`
- modify: `docs/pages/core-concepts/scheduler/index.md`
- modify: `docs/pages/core-concepts/scheduler/methods.md`
- modify: `docs/pages/core-concepts/scheduler/management.md`
- modify: `docs/pages/core-concepts/scheduler/triggers.md`
- modify: `docs/pages/core-concepts/scheduler/execution-modes.md`
- modify: `docs/pages/web-ui/debug-handler.md`
- modify: `docs/pages/migration/scheduler.md`
- modify: `docs/pages/migration/checklist.md`
- modify: `docs/pages/core-concepts/internals/service-details.md`
- modify: `docs/pages/core-concepts/scheduler/snippets/`
- modify: `docs/pages/recipes/snippets/`
- modify: `tools/docs/gen_ref_pages.py`
- modify: `tools/docs/check_bare_symbols.py`
- modify: `tools/check_test_factories.py`
- modify: `tools/check_registration_signatures.py`
- modify: `scripts/seed_db.py`
- read: `design/specs/090-registered-manual-jobs/design.md` (Documentation Updates)
- modify: `tests/unit/di/`
- modify: `tests/integration/test_di_standalone.py`

## Prompt

**Sync facade (`src/hassette/scheduler/sync.py`):**
This is the sync wrapper generated by `codegen/src/hassette_codegen/sync_facade/generic.py`. It independently defines `cancel_job()`, `cancel_group()`, and references `ScheduledJob` throughout.
- Rename `cancel_job()` → `remove_job()`, `cancel_group()` → `remove_group()`.
- Rename all `ScheduledJob` references to `Job`.
- Update the codegen source (`generic.py`) so future regeneration produces the correct names.

**Bus sync facade (`src/hassette/bus/sync.py`):**
- Line 874 has a docstring cross-reference to `Scheduler.cancel_job` — update to `Scheduler.remove_job`.

**`src/hassette/web/CLAUDE.md`:**
- Update `SchedulerDep` description: `get_all_jobs()` reads the service registry (not the heap), `mark_job_cancelled()` is renamed to `mark_job_removed()`.

**Documentation pages** (see design doc > Documentation Updates for the complete list):
- `docs/pages/core-concepts/scheduler/index.md`: introduce manual-only registration and `Job`.
- `docs/pages/core-concepts/scheduler/methods.md`: document `register()` and updated return type.
- `docs/pages/core-concepts/scheduler/management.md`: replace cancellation terminology, document `submit()`, removal, statuses, groups, completed-job lifetime.
- `docs/pages/core-concepts/scheduler/triggers.md`: describe EntityTime waiting without sentinel scheduling.
- `docs/pages/core-concepts/scheduler/execution-modes.md`: explain manual submission uses same overlap modes.
- `docs/pages/web-ui/debug-handler.md`: rewrite the 409 paragraph — 409 now means removed/stopped-owner only.
- `docs/pages/migration/scheduler.md`, `docs/pages/migration/checklist.md`: migrate references.
- `docs/pages/core-concepts/internals/service-details.md`: update scheduler service descriptions.

**Doc snippet `.py` files:**
- All files under `docs/pages/core-concepts/scheduler/snippets/` and `docs/pages/recipes/snippets/`.
- These are outside `pyrightconfig.json`'s `include` list and have no test coverage.
- Rename `ScheduledJob` imports to `Job`, `.cancel()` to `.remove()`, `cancel_job()` to `remove_job()`, `cancel_group()` to `remove_group()`.

**Tooling scripts:**
- `tools/docs/gen_ref_pages.py`: update `"hassette.scheduler.classes"` reference.
- `tools/docs/check_bare_symbols.py`: update `"ScheduledJob"` reference.
- `tools/check_test_factories.py`: update references.
- `tools/check_registration_signatures.py`: update `cancel_job` reference.
- `scripts/seed_db.py`: update `ScheduledJobRegistration` usage and `cancelled_at` param construction to `removed_at`.

**DI test files:**
- `tests/unit/di/` and `tests/integration/test_di_standalone.py` define `ScheduledJobStub` — rename.

**Regenerate doc screenshots** if handlers/job views visibly changed:
```bash
uv run python scripts/capture_screenshots.py --only web_ui_handlers,web_ui_jobs
```

See design doc: Documentation Updates, Changed Files.

## Focus

- `sync.py` is generated — update both the generated file AND the codegen template. If only the generated file is updated, the next codegen run reverts it.
- Doc snippets under `docs/pages/` have no CI signal if they break — verify each one compiles conceptually after the rename.
- `scripts/seed_db.py` constructs `ScheduledJobRegistration` with `cancelled_at` params at lines ~223-249, 1090, 1149 — update all of them.
- `check_registration_signatures.py` validates method signatures — `cancel_job` is one of the checked methods.
- This task is intentionally mechanical — no behavioral changes, only renames and doc updates.

## Verify

- [ ] AC#10: `prek -a` passes. `uv run nox -s dev` passes (full unit+integration suite). `grep -rn 'ScheduledJob\|cancel_job\|cancel_group\|cancelled_at\|NO_OCCURRENCE' src/ docs/ tools/ scripts/ codegen/` returns zero matches outside of migration SQL files, comments explaining the rename, test assertions about the old names, and the internal `ScheduledJobRegistration` (persistence DTO) / `_ScheduledJobQueue` (internal heap class) names — accepted as out of scope for the public `ScheduledJob` -> `Job` rename; no task file or the design doc calls for renaming them.
