## Verdict: PASS

> PASS — all 17 FRs and all 20 ACs are covered by a task's `implements` field and have a corresponding, concrete Verify item in that task. No contradictions between task Prompt/Focus sections and the design doc. No non-locally-verifiable criteria. context.md has all five required sections non-empty, plus an optional, non-empty `## Convention Examples` section. No identifier format violations. No visual artifacts to cover (design doc and context.md both confirm none — this is a CLI/backend feature).

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1 | T03 | "A unit test asserts the precedence `server_url_flag` > `config.cli.server_url` > derived-from-`web_api`, including that a blank `config.cli.server_url` falls through to the derived branch." |
| FR#2 | T03 | "A unit test asserts `https://example.com/hassette` resolves to a `base_url` that, joined with `/api/health` by `httpx2`, yields `https://example.com/hassette/api/health`; a second asserts `http://[::1]:8126` round-trips with brackets intact." |
| FR#3 | T03 | "A unit test asserts a scheme-less URL raises the missing-scheme exception with the offending value in the message." |
| FR#4 | T03 | "A unit test asserts a URL whose path ends in `/api` raises, and that the message names the corrected form." |
| FR#5 | T03 | "A unit test asserts `https://example.com/hassette/` and `https://example.com/hassette` produce identical `base_url` values." |
| FR#6 | T04 | "A unit test asserts `httpx.Client` receives `verify=False` when `cli.verify_ssl` is false and `verify=True` by default." |
| FR#7 | T03 | "Unit tests walk each of the five precedence tiers, including `--token-file` overriding `cli.auth_token` and `cli.auth_token` overriding `web_api.auth_token`." |
| FR#8 | T03 | "Unit tests assert `resolve_cli_auth_token` returns `None` for a non-loopback target whose only credential is `web_api.auth_token`, and separately `<data_dir>/.web_api_token`, while returning the value for `cli.auth_token`; plus a test iterating `CREDENTIAL_SOURCES` asserting every entry declares a `scope` of `\"cli\"` or `\"server\"`." |
| FR#9 | T01 | "`is_loopback_host` in `src/hassette/utils/net_utils.py` classifies by parsing the host as an IP literal and reporting `is_loopback`, falling back to a fixed hostname set, with no DNS call anywhere in the function; `grep -rn \"_is_loopback_host\" src/hassette/core/` returns no match." |
| FR#10 | T04 | "A unit test asserts the transport receives a request for a non-loopback target with no resolvable credential, rather than the CLI exiting before the call." |
| FR#11 | T04 | "A unit test asserts the 401 message names `--token-file`, `cli.token_file`, `HASSETTE__CLI__AUTH_TOKEN`, and `trusted_proxies`, and that the `trusted_proxies` mention is qualified as applying to the remote instance." |
| FR#12 | T04 | "A unit test asserts a 302 response produces an error mentioning a redirect, forward auth, and a docs pointer." |
| FR#13 | T05 | "`uv run hassette run --help` lists `--ha-url` and `-u`, and lists neither `--base-url` nor `--url`; `grep -n 'base-url\|\"--url\"' src/hassette/cli/commands/run.py` returns no match." |
| FR#14 | T04 | "A unit test asserts a network error and an HTTP error against `https://example.com/hassette` each report that full base URL." |
| FR#15 | T03 | "A unit test asserts a token file containing a non-ASCII character raises rather than returning the value." |
| FR#16 | T04 | "Unit tests assert the target appears on a successful non-loopback request, is absent on a successful loopback request, and appears on a 401 against a non-loopback target **without** `--debug`." |
| FR#17 | T04 | "A unit test asserts a config-sourced `verify_ssl=false` emits the warning while the `--no-verify-ssl` flag does not." |
| AC#1 | T04 | "`uv run pytest tests/unit/cli/test_client.py -v` passes with `TestBaseUrl`'s four tests unmodified (`git diff` shows no change to that class)." |
| AC#2 | T03 | "`uv run pytest tests/unit/cli/test_target.py -v` passes the path-prefix composition test." |
| AC#3 | T03 | "`uv run pytest tests/unit/cli/test_target.py -v` passes the scheme-less and `/api`-suffix rejection tests with corrected-form assertions on the messages." |
| AC#4 | T03 | "`uv run pytest tests/unit/cli/test_target.py -v` passes the trailing-slash normalization test." |
| AC#5 | T04 | "The `verify=` assertions from FR#6 pass." |
| AC#6 | T03 | "`uv run pytest tests/unit/cli/test_target.py -v` passes the full credential-precedence chain." |
| AC#7 | T03 | "`uv run pytest tests/unit/cli/test_target.py -v` passes the suppression tests and the `CREDENTIAL_SOURCES` scope-declaration test." |
| AC#8 | T01 | "`uv run pytest tests/unit/utils/test_net_utils.py -v` passes with assertions that `localhost`, `LOCALHOST`, `127.0.0.1`, `127.0.0.53`, `::1`, and `::ffff:127.0.0.1` classify as loopback while `192.168.1.5`, `example.com`, and `0.0.0.0` do not." |
| AC#9 | T04 | "The request-issued assertion from FR#10 passes." |
| AC#10 | T04 | "The 401 message assertion from FR#11 passes, asserted on the qualifying phrase rather than substring presence alone." |
| AC#11 | T04 | "The 302 message assertion from FR#12 passes." |
| AC#12 | T05 | "`uv run pytest tests/unit/cli/ -v` passes, including the new flag-surface test and the existing `cmd_run(base_url=...)` keyword call at `tests/unit/cli/test_commands_run.py:60`." |
| AC#13 | T02 | "`uv run python scripts/export_schemas.py` followed by `uv run python tools/check_schemas_fresh.py` exits 0, and `uv run pytest tests/unit/test_config_models.py -v` passes including the new `CliConfig` default and env-resolution tests." |
| AC#14 | T06 | "`prek -a && prek pyright -a --stage pre-push` exits 0." |
| AC#15 | T06 | "`uv run mkdocs build --strict` exits 0 with the rewritten CLI configuration page, the new reverse-proxy section, and the `[hassette.cli]` row in place." |
| AC#16 | T03 | "`uv run pytest tests/unit/cli/test_target.py -v` passes the non-ASCII token test, asserting the raised error names the file path." |
| AC#17 | T04 | "The three target-surfacing assertions from FR#16 pass." |
| AC#18 | T04 | "The `verify_ssl` warning assertion from FR#17 passes." |
| AC#19 | T06 | "`grep -n 'base-url\|--url' docs/pages/cli/commands.md` returns no match for the removed `hassette run` flag spellings." |
| AC#20 | T04 | "The full-base-URL assertions from FR#14 pass." |

## Coverage Gaps

None.

## Contradictions

None. Each task's Prompt and Focus sections were checked against the design doc requirements they claim to implement:

- T01 (loopback classifier move) matches Key Constraints "Do not write a second loopback classifier" and FR#9's no-DNS, IP-literal-with-hostname-fallback behavior.
- T02 (`CliConfig` group) matches Architecture → Where the target lives, including the `model_config`-not-`Field()` placement rule for `ui.group_label`.
- T03 (target/credential resolution) matches FR#1–FR#5, FR#7, FR#8, FR#15 precedence order and scoping rules, and explicitly avoids `_build_ha_url`/`_parse_and_normalize_url` per Key Constraints.
- T04 (flags/context/client wiring) matches FR#6, FR#10–FR#12, FR#14, FR#16, FR#17, including the `-s` short-flag assignment, omission of `negative=[]` on `--no-verify-ssl`, and the unconditional (non-`--debug`-gated) HTTP-error target echo for non-loopback targets.
- T05 (flag rename) matches FR#13 exactly, including keeping the Python parameter name `base_url` while changing only the `Parameter(name=[...])` list.
- T06 (docs) matches the Documentation Updates section, including the proxy-agnostic-language and subdomain-only-example constraints.

## Non-Local Criteria

None. Every Verify item across all six tasks is a local `pytest`, `grep`, `prek`, `mkdocs build --strict`, or `uv run hassette --help` invocation runnable in the repo without observing CI, GitHub Actions, or any post-push/post-merge system.

## Warnings

None.

---

## Amendment (post-review, 2026-08-05)

This report captures the traceability state at the time the validator ran. Three
changes landed afterwards, from the plan review and two comb passes. FR/AC
coverage is unaffected — the same 17 FRs and 20 ACs map to the same tasks — but
three rows' quoted Verify text and one `depends_on` are now stale above:

- **T04 `depends_on`** is now `["T03", "T05"]`, not `["T03"]`. T04 and T05 both
  append to `tests/unit/cli/test_parse_args.py`; the added edge serializes that
  shared write target.
- **T04 and T05 Target Files** now include `tests/unit/cli/test_parse_args.py`,
  per `tests/unit/cli/CLAUDE.md`'s requirement that every new or changed CLI flag
  gets a cyclopts wiring test.
- **AC#2 and AC#8 wording** changed in design.md. AC#2 now specifies the resolver
  layer rather than "constructs a client." AC#8 now proves non-divergence via
  `grep -rn "_is_loopback_host" src/hassette/core/` returning no match, rather
  than promising a duplicated table run against `WebApiService`.

The verdict (PASS) and the coverage counts still hold.
