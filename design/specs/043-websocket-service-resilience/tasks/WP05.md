---
depends_on:
- WP04
lane: done
title: Fix downstream consumers and add documentation
work_package_id: WP05
---

## Objectives & Success Criteria

Fix `RuntimeQueryService.get_system_status()` to use `is_ready()` instead of `.status == RUNNING` for `ws_connected`. Make `StateProxy.on_disconnect()` idempotent for rapid disconnect/reconnect cycles. Update documentation for the new config fields and three-layer retry model. After this WP:

- `/health` endpoint returns `ws_connected: false` when WebSocket service is RUNNING but not ready (early-drop retry in progress)
- `StateProxy.on_disconnect()` is a no-op when already not-ready (no redundant cache clears or REST API fetches during early-drop cycles)
- All 8 new config fields are documented in the docs site
- The three-layer retry model is explained in a docs subsection
- `pyright` and `ruff` pass
- `uv run mkdocs serve` builds without warnings on changed pages

## Subtasks

1. Fix `get_system_status()` in `src/hassette/core/runtime_query_service.py` (line 217): Change `self.hassette.websocket_service.status == ResourceStatus.RUNNING` to `self.hassette.websocket_service.is_ready()` for the `ws_connected` field.

2. Make `StateProxy.on_disconnect()` idempotent in `src/hassette/core/state_proxy.py` (line 269): Add `if not self.is_ready(): return` guard at the top of `on_disconnect()`. When StateProxy is already not-ready (cache already cleared from a previous disconnect), skip the entire method body.

3. Write tests for the health endpoint fix: verify that when websocket_service status is RUNNING but `is_ready()` returns False, `get_system_status()` returns `ws_connected=False`.

4. Write test for StateProxy idempotency: call `on_disconnect()` twice in succession. Verify the cache-clear and subscription cancel only happen on the first call; the second call is a no-op.

5. Find the docs page that documents WebSocket configuration fields (grep `docs/` for existing `websocket_` config references). Add the 8 new fields with types, defaults, and descriptions.

6. Add a subsection to the docs explaining the three-layer retry model: what each layer handles, when to tune each layer, and the total recovery cap. Include a table similar to the design doc's three-layer table but written for end users (no code references).

7. Update `design.md` Documentation Updates section reference from "7 new configuration fields" to "8 new configuration fields" if not already done.

8. Evaluate `_ws_probe` in `tests/system/conftest.py` — now that the production code handles early-drops, determine whether the hold-probe is still needed as defense-in-depth or can be simplified. Document the decision in a code comment at the `_ws_probe` call site (retain or simplify, with rationale).

## Test Strategy

**New/updated tests:**

- `tests/integration/test_runtime_query_service.py` (or wherever runtime query tests live):
  - `test_system_status_ws_connected_reflects_readiness` — mock `websocket_service.is_ready()` to return False while status is RUNNING; verify `ws_connected` is False in response
  - `test_system_status_ws_connected_true_when_ready` — mock `is_ready()` True; verify `ws_connected` is True

- `tests/integration/test_state_proxy.py` (or existing state proxy test file):
  - `test_on_disconnect_idempotent` — call `on_disconnect()` when StateProxy is already not-ready; verify no cache clear, no subscription cancel, no REST API call
  - `test_on_disconnect_first_call_clears_cache` — call `on_disconnect()` when StateProxy is ready; verify cache is cleared and subscriptions cancelled (existing behavior preserved)

**Docs verification:**
- `uv run mkdocs serve` builds without warnings

## Review Guidance

- `get_system_status()` MUST use `is_ready()`, not `connected` — `connected` checks the raw WebSocket state, while `is_ready()` reflects the full service readiness including the early-drop retry state
- `StateProxy.on_disconnect()` guard MUST check `is_ready()`, not a custom flag — `is_ready()` is already set to False by `mark_not_ready()` which fires before the DISCONNECTED event
- Docs must explain the three-layer model at an end-user level — no references to class names, method names, or internal architecture. Focus on what operators need to know: what each layer does, what the config fields control, and when to change defaults
- Finding 7 from challenge: the inconsistency between HTTP health and WebSocket push must be resolved by this WP
