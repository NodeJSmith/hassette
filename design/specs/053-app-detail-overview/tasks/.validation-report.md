## Status: ISSUES_FOUND

> ISSUES_FOUND — 0 coverage gaps, 1 contradiction, 2 warnings. Review required before execution.

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1       | T02  | "Navigating to `/apps/{appKey}` (no tab segment) loads the overview tab" |
| FR#2       | T03  | "Error spotlight section lists handlers and jobs with failures or timeouts in the current time window" |
| FR#3       | T03  | "Error spotlight shows up to 3 expanded entries with 'show N more' for remaining" |
| FR#4       | T03  | "Each error spotlight entry links to the handlers tab with that handler/job pre-selected" |
| FR#5       | T03  | "Error spotlight section is absent when no handlers or jobs are failing" |
| FR#6       | T03  | "Handler health grid shows all handlers and jobs with status, run count, and last error info" |
| FR#7       | T03  | "Handler health grid orders failing items first" |
| FR#8       | T03  | "Each handler health grid entry links to the handlers tab with the handler/job pre-selected" |
| FR#9       | T04  | "Recent activity section shows invocations and executions across all handlers, merged and sorted by time" |
| FR#10      | T01  | "The endpoint `GET /telemetry/app/{app_key}/activity` returns recent invocations and executions merged and sorted by time, accepting `limit`, `since`, and `source_tier` parameters" |
| FR#11      | T04  | "Recent logs section shows app-scoped log entries" |
| FR#12      | T04  | "Overview updates in real time when invocation_completed or execution_completed WebSocket events arrive for this app" |
| FR#13      | T02  | "Tab bar order is overview, handlers, code, logs, config" |
| FR#14      | T02  | "Overview tab renders a mobile-appropriate layout below the mobile breakpoint" |
| AC#1       | T02  | "Navigating to `/apps/{appKey}` loads the overview tab" |
| AC#2       | T03  | "Failing handlers visible in error spotlight with error details and links" |
| AC#3       | T03  | "Error spotlight not rendered when no failures" |
| AC#4       | T03  | "All handlers/jobs visible in health grid with status indicators and links" |
| AC#5       | T04  | "Recent invocations/executions appear in the activity section from the new backend endpoint" |
| AC#6       | T04  | "Recent app-scoped logs appear in the logs section" |
| AC#7       | T04  | "New invocations/executions appear automatically via WebSocket without manual refresh" |
| AC#8       | T02  | "Tab bar shows overview as the first tab" |
| AC#9       | T02  | "Overview renders appropriately on mobile viewports" |

## Coverage Gaps

None.

## Contradictions

- T01 Prompt (method signature): `async def get_app_recent_activity(self, app_key: str, instance_index: int, ...)` makes `instance_index` a required `int`, but design doc Architecture section specifies the route query param as `instance_index: int | None` (nullable/optional). A non-nullable service method cannot correctly serve an endpoint where `instance_index` is omitted.

## Warnings

- T02 Verify FR#14: "Overview tab renders a mobile-appropriate layout below the mobile breakpoint" is not binary-verifiable — no concrete breakpoint value (e.g., `< 768px`) or observable layout behavior (e.g., "sections stack vertically") is stated. The same applies to T02 Verify AC#9: "Overview renders appropriately on mobile viewports."
- T02 Prompt step 6 says to render `<EmptyState>` placeholders for all three sections ("no content — just `<EmptyState>` placeholders"), but T03 Focus explicitly says "Error spotlight section is absent when nothing is failing (no 'no errors found' empty state)" and T04 applies similar logic. An `<EmptyState>` placeholder for the error spotlight in T02's skeleton directly contradicts the FR#5/T03 requirement that the section is absent when nothing is failing. The skeleton placeholder would need to be removed entirely (not replaced with real content) when T03 fills in the section.
