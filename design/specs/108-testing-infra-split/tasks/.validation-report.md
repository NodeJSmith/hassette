# Plan Validation Report: 108-testing-infra-split

## Verdict: PASS

Zero coverage gaps, zero contradictions, zero non-local verification criteria. `context.md` has all five required sections with non-empty content, plus an optional sixth section with content.

---

## Requirements Extracted

**Functional Requirements (9 total):** FR#1, FR#2, FR#3, FR#4, FR#5, FR#6, FR#7, FR#8, FR#9

**Acceptance Criteria (6 total):** AC#1, AC#2, AC#3, AC#4, AC#5, AC#6

**Identifier format:** All 15 identifiers conform to `^FR#\d+$` / `^AC#\d+$`. No format violations found.

---

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|---|---|---|
| FR#1 | T01 | "FR#1: `python -c \"from hassette.testing import AppTestHarness, RecordingApi, HassetteHarness, wait_for, build_harness, EventCapture, make_full_state_change_event\"` succeeds" |
| FR#2 | T01 | "FR#2: `python -c \"from hassette.testing import make_mock_hassette\"` raises `ImportError`" |
| FR#3 | T05 | "FR#3: `python -c \"import hassette.test_utils\"` raises `ModuleNotFoundError`" |
| FR#4 | T01 | "FR#4: `python -c \"import hassette.testing; assert len(hassette.testing.__all__) == 21\"` succeeds" |
| FR#5 | T02 | "FR#5: `python -c \"from tests.support.fixtures import hassette_harness, hassette_with_bus\"` succeeds (running from repo root, with `tests/` on the Python path)" |
| FR#6 | T02 | "FR#6: `grep -r \"from tests.support\" src/hassette/testing/` returns zero matches" |
| FR#6 | T05 | "FR#6: `grep -r \"from tests.support\" src/hassette/testing/` returns zero matches AND `tools/check_module_boundaries.py` `testing-isolation` rule passes" |
| FR#7 | T05 | "FR#7: `uv run nox -s wheel_smoke` passes (wheel contains `hassette/testing/`, not `hassette/test_utils/`)" |
| FR#8 | T01 | "FR#8: `python -c \"from hassette.testing.fixtures import dummy_cache, event_capture, build_harness\"` succeeds" |
| FR#9 | T02 | "FR#9: The file `tests/support/fixtures.py` contains the Tier 2 fixtures (`hassette_harness`, `hassette_with_bus`, etc.) and is importable" |
| AC#1 | T03 | "AC#1: `uv run nox -s dev` (or `uv run pytest -n 4`) exits with zero failures" |
| AC#2 | T05 | "AC#2: `prek -a` passes with zero errors" |
| AC#3 | T05 | "AC#3: `uv run nox -s wheel_smoke` exits 0" |
| AC#4 | T05 | "AC#4: `uv run pytest tests/unit/test_public_api_surface.py -v` passes, confirming `hassette.testing.__all__` matches the 21-symbol Tier 1 set" |
| AC#5 | T04 | "AC#5: `grep -r \"hassette.test_utils\" src/ tests/ tools/ docs/ scripts/ codegen/ .claude/ prek.toml ruff.toml` returns zero matches (excluding `src/hassette/test_utils/` itself, `design/` history files, and `CHANGELOG.md`)" |
| AC#6 | T05 | "AC#6: both the grep check and the AST-based `testing-isolation` rule confirm no `tests.support` imports in `src/hassette/testing/`" |

FR#6 is intentionally verified in two tasks (T02 as an in-progress build-time check, T05 as the final AST-enforced check). This is not a duplication problem — the design doc's "One-way dependency" constraint (Key Constraints) is explicitly enforced both mechanically during construction and again as a permanent lint rule (AC#6), matching the design's own architecture description.

Every FR and AC has at least one task whose `implements` field lists it and whose Verify section has a corresponding `- [ ] FR#N:` / `- [ ] AC#N:` item with matching text.

---

## Coverage Gaps

None. All 9 FRs and all 6 ACs are implemented and verified.

---

## Contradictions

None found. Cross-checked each task's Prompt/Focus against the design doc's Architecture, Module mapping table, Codemod strategy, and String reference cleanup sections:

- T01 module moves (whole-file and split/fold instructions) match the design doc's "Module mapping (authoritative)" table row-for-row (`app_harness.py`, `recording_api.py`, `api_call.py`, `config.py`, `exceptions.py`, `event_capture.py`, `harness.py`→`_harness.py`/`harness.py`, `helpers.py` split, `simulation.py`→`_simulation.py` with fold-ins, `fixtures.py` split, `time_control.py`, `sync_facade.py`, `reset.py`, `test_server.py`, `ws_mocks.py`).
- T01's `__all__` list matches the design doc's 21-symbol Tier 1 set exactly (same symbols, same order).
- T02 module moves (`factories.py`, `mock_hassette.py`, `web_*_helpers.py`, `sql_helpers.py`→`sql.py`, `uvicorn_server.py`→`uvicorn.py`, `resource_tracker.py`, `state_proxy_mocks.py`, `harness.py` `preserve_config`-only extraction, `helpers.py`/`fixtures.py` Tier 2 remainders) match the table.
- T03's import transformation rules match the design doc's "Codemod strategy" table (root-level Tier 1/Tier 2 imports, module-level imports, private-module imports, mixed-tier splitting).
- T03's `pytest_plugins` before/after matches the design doc's "pytest_plugins registration change" section verbatim.
- T04's file-by-file list matches the design doc's "String reference cleanup" table, plus the 3 gap-check additions the design doc itself flags in its Impact section HTML comment (`ast_utils.py`, `doc-accuracy-review/briefing-template.md`, `doc-coverage-review/REFERENCE.md`) — these are correctly routed to T04, not omitted or contradicted.
- T05's `test_public_api_surface.py` rewrite instructions match the design doc's "Existing Tests to Adapt" section (demote `make_mock_hassette`, promote `EventCapture`/`HassetteHarness`/`wait_for`/`build_harness`/`make_full_state_change_event`, flip `test_tier2_not_in_all` assertions, rewrite `test_tier2_importable`).
- T05's deletion-ordering note ("delete old package AFTER updating the test file and adding the nox session") is consistent with the design doc's non-goal of a shim period plus the requirement that `test_test_utils_removed` needs the package actually gone to pass.

---

## Non-Local Verification Criteria

None. Every Verify item across all 5 tasks is a `python -c`, `uv run pytest`, `uv run nox`, `prek -a`, or `grep` command runnable directly in the local repo checkout, matching the design doc's own AC definitions (which are themselves local commands).

---

## Vague Verification Criteria

None blocking. One item is borderline and noted as a warning below (T02's FR#9 criterion).

---

## Visual Artifact Coverage

Not applicable. The design doc's own context.md states "Visual Artifacts: None" — this is a backend/test-infrastructure restructuring with no UI-facing change, consistent with `design-completeness.md`'s exception for changes with no corresponding UI view.

---

## context.md Completeness

All required sections present and non-empty:

1. `## Problem & Motivation` — present, substantive (matches design doc's Problem section).
2. `## Visual Artifacts` — present, content is "None." (explicit non-empty statement, not a bare heading).
3. `## Key Decisions` — present, 8 numbered decisions.
4. `## Constraints & Anti-Patterns` — present, 7 bullet points.
5. `## Design Doc References` — present, 8 section pointers.
6. `## Convention Examples` (optional) — present, 3 code examples carried over from the design doc's own Convention Examples section.

---

## Warnings

1. **Test file count discrepancy (design doc vs. task/context.md) — already flagged, not a defect.** The design doc's Test Strategy and Impact sections both say "255 test files" import from `hassette.test_utils`. T03's Target Files section and `context.md`'s Constraints & Anti-Patterns section both correctly note the actual count is 261+ and instruct the codemod to cover all of them regardless of the design doc's stated number. This is self-consistent (context.md explicitly reconciles the discrepancy) and does not block the plan, but the design doc itself contains a stale count that should be corrected for future readers.

2. **T02's FR#9 Verify item is loosely worded relative to the others.** "The file `tests/support/fixtures.py` contains the Tier 2 fixtures (`hassette_harness`, `hassette_with_bus`, etc.) and is importable" mixes a source-inspection claim ("contains") with a runtime claim ("is importable") and uses "etc." rather than enumerating the full fixture list the Prompt section already names (`hassette_with_app_handler`, `hassette_with_file_watcher`, `hassette_with_mock_api`, `hassette_with_scheduler`, `hassette_with_state_proxy`, `run_hassette_startup_tasks`, `sync_executor`). It is still executor-checkable (grep for `def hassette_harness` etc. plus a python import), so it does not rise to a blocking "vague" finding, but tightening it to a concrete `python -c` import of all 9 named fixtures (mirroring FR#5's style in the same task) would remove ambiguity for the executor.

3. **FR#6 double-verification is intentional, not redundant scope creep.** Flagging for visibility only: both T02 and T05 list FR#6 in `implements`. This matches the design doc's description of the one-way-dependency invariant being checked twice (informally via grep during T02, then permanently via a new AST rule in T05 per AC#6) — not a scope violation, since both instances trace to design doc content (Key Constraints "One-way dependency" and AC#6's explicit AST-rule requirement).

No scope violations found — every FR#N/AC#N referenced in any task's `implements` field exists in the design doc's Functional Requirements or Acceptance Criteria sections.
