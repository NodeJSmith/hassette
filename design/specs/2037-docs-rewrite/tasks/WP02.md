---
work_package_id: "WP02"
title: "Rewrite home page and Getting Started section"
lane: "planned"
depends_on: ["WP01"]
---

## Objectives & Success Criteria

Five pages are rewritten or polished to professional quality: `docs/index.md`, `docs/pages/getting-started/index.md`, `docs/pages/getting-started/hassette-vs-ha-yaml.md`, and `docs/pages/getting-started/ha_token.md`. The dead CSS file `docs/_static/style.css` is deleted. `mkdocs build --strict` passes after all changes.

A new user landing on the home page can immediately understand what Hassette is, who it's for, and what problem it solves — without reading multiple pages. The Getting Started local setup page has no inline fenced code blocks (all examples are snippet-based). The `hassette-vs-ha-yaml.md` page stays in Getting Started.

## Subtasks

1. Read the nav audit document (`docs/nav-audit.md`) Section 1 and Section 2 for per-page recommendations affecting WP02's pages. Apply any accepted structural changes.

2. Read `docs/index.md` alongside `src/hassette/__init__.py` and `src/hassette/core/core.py` to verify accuracy of any feature claims on the home page. Rewrite the home page so it communicates: what Hassette is, who it's for, what problems it solves, and how to get started — without requiring the reader to visit other pages first.

3. Delete `docs/_static/style.css`. Check `docs/index.md:1` for any `.hero` class usage — if present, replace with a valid MkDocs Material `attr_list` approach (add `{ .hero }` attribute to the image tag and add a minimal inline style block in `mkdocs.yml` `extra_css:` pointing to a new `docs/_static/custom.css` with valid Material CSS, or remove the `.hero` class if it adds no value). Do NOT register the deleted file in `mkdocs.yml`.

4. Read `docs/pages/getting-started/index.md` alongside the actual installation instructions and `src/hassette/` source. Verify every step is accurate. Convert the inline typed-handler code example at approximately line 64–69 to a snippet file: create `docs/pages/getting-started/snippets/typed_handler.py` with the minimal `App` subclass wrapper and section markers, then replace the inline block with `--8<-- "pages/getting-started/snippets/typed_handler.py:typed-handler"`. Check all other inline fenced blocks in this page and convert any that remain.

5. Read `docs/pages/getting-started/hassette-vs-ha-yaml.md`. Rewrite or expand as needed so a new Python developer (no prior HA automation experience) understands the trade-offs. Verify no stale content against the current source. If the page currently has inline code blocks, convert them to snippets.

6. Read `docs/pages/getting-started/ha_token.md` (currently 21 lines). Expand if the brevity leaves a new user confused about what token they need and why. Verify image references resolve. If any code examples are inline, convert them to snippets.

7. Delete the dead snippet file `docs/pages/getting-started/snippets/hello_world.py` (it exists on disk but is not referenced by any page).

8. Run `python -m py_compile` on every new or modified snippet file in `docs/pages/getting-started/snippets/`.

9. Run `mkdocs build --strict`. Fix any errors or warnings before committing.

10. Commit all changes.

## Completion Report Requirements

- **Flags section**: list every `!!! warning "Needs human review"` admonition added, with page name and one-line description of the uncertainty.
- **Snippet changes**: list every new snippet file created and every inline block converted.
- **CSS note**: confirm `docs/_static/style.css` is deleted and not referenced in `mkdocs.yml`.

## Test Strategy

N/A — no Python application code changes. Verification:
- `mkdocs build --strict` passes (no errors, no warnings).
- `python -m py_compile docs/pages/getting-started/snippets/*.py` passes for all new/modified snippets.
- `docs/pages/getting-started/snippets/hello_world.py` no longer exists (deleted in Subtask 7).

## Review Guidance

Every inline fenced code block on the four pages must be gone — any remaining inline block is a BLOCKER.

The home page must clearly state what Hassette is and who it's for in the first visible section — a home page that leads with installation steps rather than positioning is a WARNING.

The typed-handler snippet file must be valid Python (imports + `App` subclass wrapper) — an invalid snippet that passes build only because `check_paths: true` validates path existence but not parse validity is a WARNING; `py_compile` must have been run.

`docs/_static/style.css` must not exist — its presence is a WARNING (dead code, wrong selectors).
