---
depends_on:
- WP01
lane: doing
title: Rewrite Web UI section
work_package_id: WP06
---

## Objectives & Success Criteria

Five Web UI pages are reviewed and polished: `docs/pages/web-ui/index.md`, `web-ui/dashboard.md`, `web-ui/apps.md`, `web-ui/sessions.md`, and `web-ui/logs.md`. All inline fenced code blocks are converted to snippet files. `mkdocs build --strict` passes.

The Web UI section accurately describes the current web interface. Thin pages (`logs.md` at 39 lines) are expanded to cover their topic. `web-ui/index.md` has inline blocks that need conversion.

## Subtasks

1. Read the nav audit (`design/audits/2026-04-11-docs-nav-audit/audit.md`) Section 2 for per-page recommendations on the five Web UI pages.

2. Read all five Web UI pages alongside the web UI source code. The frontend is a Preact SPA — locate component source in `frontend/src/`. The backend ASGI server lives in `src/hassette/`. Use both to verify that feature descriptions in the docs are accurate against the current implementation.

3. Rewrite or polish each page as needed. Pay particular attention to:
   - `web-ui/index.md`: has inline code blocks — convert all to snippets under `docs/pages/web-ui/snippets/`.
   - `web-ui/logs.md` (39 lines): expand if this page is too thin to be useful.
   - Any page that references UI features that have changed or been removed.

4. Convert all remaining inline fenced code blocks across all five pages to snippet files.

5. Run `python -m py_compile` on every new or modified `.py` snippet file.

6. Run `mkdocs build --strict`. Fix any errors or warnings before committing.

7. Commit all changes.

## Completion Report Requirements

- **Flags section**: list every `!!! warning "Needs human review"` admonition added.
- **Snippet changes**: list every new snippet file and every inline block converted.

## Test Strategy

N/A — no Python application code changes. Verification:
- `mkdocs build --strict` passes.
- `python -m py_compile` passes for all new/modified Python snippets.

## Review Guidance

Any description of web UI features that cannot be verified against source must be flagged — an unverified UI feature description presented as fact is a BLOCKER.

`web-ui/index.md` must have zero remaining inline fenced blocks — any remaining inline block is a BLOCKER.
