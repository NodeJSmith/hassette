---
depends_on:
- WP01
lane: done
title: Rewrite Advanced section
work_package_id: WP08
---

## Objectives & Success Criteria

Six Advanced pages are reviewed and polished: `docs/pages/advanced/index.md` (currently 9 lines — a placeholder), `advanced/dependency-injection.md`, `advanced/custom-states.md`, `advanced/state-registry.md`, `advanced/type-registry.md`, and `advanced/log-level-tuning.md`. The placeholder `advanced/index.md` is expanded to a useful section overview. Three dead snippet files are deleted (`hello_world.py` in Getting Started snippets is handled by WP02, not this WP). `mkdocs build --strict` passes.

## Subtasks

1. Read the nav audit (`design/audits/2026-04-11-docs-nav-audit/audit.md`) Section 2 for per-page recommendations on the six Advanced pages.

2. Delete the three dead snippet files in the Advanced section (note: `hello_world.py` in Getting Started snippets is handled by WP02):
   - `docs/pages/advanced/snippets/state-registry/basic_custom_state.py`
   - `docs/pages/advanced/snippets/state-registry/error_handling_examples.py`
   - `docs/pages/advanced/snippets/state-registry/integration_snippets.py`
   Verify that `mkdocs build` still passes after deletion (these files are unreferenced, so no `--8<--` include should break).

3. Rewrite `docs/pages/advanced/index.md` from its current 9-line placeholder into a genuine section overview: what "Advanced" topics are, who they're for, a brief description of each topic with a link, and guidance on which concepts are prerequisites (e.g., custom states require understanding the state registry).

4. Read `docs/pages/advanced/dependency-injection.md` (273 lines) alongside `src/hassette/event_handling/dependencies.py` and `src/hassette/event_handling/accessors.py`. Verify all extractor patterns, DI aliases (`D`), and builtin conversion rules. This page is already largely snippet-based — verify accuracy and expand prose where explanations are thin.
   - **Shared snippet note**: `dependency-injection/snippets/custom_type_converter.py` is shared with `advanced/type-registry.md`. If you modify it, note `type-registry.md` in the completion report.

5. Read `docs/pages/advanced/custom-states.md` (174 lines) and `docs/pages/advanced/state-registry.md` (219 lines) alongside `src/hassette/` for the state registry and type conversion implementations. Verify all patterns. Note any overlap between the two pages and ensure the narrative explains how they relate.
   - **Shared snippet note**: `state-registry/snippets/basic_custom_state_usage.py` is referenced from `custom-states.md` but lives in `state-registry/snippets/`. If you modify it, note `state-registry.md` in the completion report.

6. Read `docs/pages/advanced/type-registry.md` (359 lines) alongside `src/hassette/` for `TYPE_REGISTRY` and `TypeConverterEntry`. Verify all patterns. This page is heavily snippet-based; verify accuracy and expand prose where thin.
   - **Shared snippet note**: `dependency-injection/snippets/custom_type_converter.py` is shared with `dependency-injection.md`. If WP08 modifies it, note `dependency-injection.md` in the completion report (or coordinate with WP04 if it has not yet run).

7. Read `docs/pages/advanced/log-level-tuning.md` (101 lines). Verify the log level configuration instructions against the current codebase. Convert any inline code blocks to snippet files.

8. Run `python -m py_compile` on every new or modified `.py` snippet file.

9. Run `mkdocs build --strict`. Fix any errors or warnings before committing.

10. Commit all changes.

## Completion Report Requirements

- **Flags section**: list every `!!! warning "Needs human review"` admonition added.
- **Dead snippet deletions**: confirm all three Advanced dead snippet files are deleted (`hello_world.py` in Getting Started was deleted by WP02).
- **Shared snippet notes**: explicitly note any modifications to shared snippet files and which other pages are affected.

## Test Strategy

N/A — no Python application code changes. Verification:
- `mkdocs build --strict` passes.
- `python -m py_compile` passes for all new/modified Python snippets.
- The three Advanced dead snippet files no longer exist on disk.

## Review Guidance

`advanced/index.md` must no longer be 9 lines — a placeholder-quality overview page in a published docs site is a WARNING.

Deletion of the three dead snippet files must be confirmed — files that were never referenced being left on disk is a WARNING (pollutes the tree). (`hello_world.py` in Getting Started is handled by WP02, not WP08.)

Shared snippet modifications without completion-report notes are a WARNING.

Any DI pattern, accessor, or condition that doesn't match the current `event_handling/` source is a BLOCKER.
