---
work_package_id: "WP05"
title: "Rewrite Core Concepts: API, States, Persistent Storage, Database & Telemetry, Configuration"
lane: "planned"
depends_on: ["WP01"]
---

## Objectives & Success Criteria

Eleven Core Concepts pages are reviewed, rewritten where needed, and made snippet-compliant: the four API pages, the States page, Persistent Storage, Database & Telemetry, and the four Configuration pages. The heaviest-work pages are `persistent-storage.md` (424 lines, zero snippet includes, ~13 inline code blocks) and `database-telemetry.md` (4 inline blocks, mostly prose). All inline fenced code blocks are converted to snippet files. `mkdocs build --strict` passes.

## Subtasks

1. Read the nav audit (`docs/nav-audit.md`) Section 2 for per-page recommendations on all 11 pages in scope.

2. Read `docs/pages/core-concepts/api/index.md`, `api/entities.md`, `api/services.md`, and `api/utilities.md` alongside `src/hassette/api/`. Verify all API methods (`get_state`, `get_states`, `call_service`, `set_state`, `fire_event`, history, logbook, template methods). The four pages are currently thin-to-medium (50–72 lines each); expand any that are genuinely too short. Convert remaining inline blocks to snippet files.

3. Read `docs/pages/core-concepts/states/index.md` (65 lines) alongside `src/hassette/state_manager/`. Verify domain access pattern (`self.states.light`), generic access (`self.states[CustomState]`), and direct entity lookup (`self.states.get("light.kitchen")`). These pages are snippet-based; verify accuracy and expand prose if thin.

4. Read `docs/pages/core-concepts/persistent-storage.md` (424 lines, no snippet includes). This is the most work-intensive page in this WP. Read `src/hassette/` to understand the persistent storage implementation. Rewrite the page with accurate content. Extract all ~13 inline code blocks into snippet files under `docs/pages/core-concepts/snippets/persistent-storage/` (create this directory). Use section markers for partial-file embedding.

5. Read `docs/pages/core-concepts/database-telemetry.md` (115 lines, 4 inline blocks). Read `src/hassette/` for the database and telemetry implementation (check `design/specs/2033-telemetry-source-tier/design.md` for recent changes). Verify accuracy, update stale sections, convert inline blocks to snippets under `docs/pages/core-concepts/snippets/database-telemetry/`.

6. Read `docs/pages/core-concepts/configuration/index.md` (26 lines — thin), `configuration/auth.md` (30 lines — thin), `configuration/global.md` (229 lines), and `configuration/applications.md` (82 lines) alongside `src/hassette/config/`. Expand thin pages if they lack content a user needs. Verify config option names and types. Convert remaining inline blocks to snippets.
   - **Shared snippet note**: `apps/snippets/app_config_definition.py` and `apps/snippets/app_config.toml` are shared with `apps/configuration.md` (WP04). If WP04 has already run and modified these files, verify they still work for `configuration/applications.md`. If you need to modify them, note `apps/configuration.md` in the completion report.
   - **Shared snippet note**: `configuration/snippets/file_discovery.md` is shared with `getting-started/index.md` (WP02). If WP02 modified it, verify it still works here. If you need to modify it, note `getting-started/index.md` in the completion report.

7. Run `python -m py_compile` on every new or modified `.py` snippet file touched in this WP.

8. Run `mkdocs build --strict`. Fix any errors or warnings before committing.

9. Commit all changes.

## Completion Report Requirements

- **Flags section**: list every `!!! warning "Needs human review"` admonition added.
- **Snippet changes**: list every new snippet file and every inline block converted.
- **Shared snippet notes**: explicitly note any modifications to `app_config_definition.py`, `app_config.toml`, or `file_discovery.md`.

## Test Strategy

N/A — no Python application code changes. Verification:
- `mkdocs build --strict` passes.
- `python -m py_compile` passes for all new/modified Python snippets.

## Review Guidance

`persistent-storage.md` must have zero inline fenced code blocks after this WP — it is the largest zero-snippet page and the conversion is a BLOCKER.

API method signatures in snippet files must match current source — stale signatures are a BLOCKER.

Shared snippet modifications without completion-report notes are a WARNING.

Config option names in prose and snippets must match current `HassetteConfig` and app config classes — stale option names are a BLOCKER.
