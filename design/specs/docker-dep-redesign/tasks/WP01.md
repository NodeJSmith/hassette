---
work_package_id: "WP01"
title: "Add constraints file generation to Dockerfile"
lane: "planned"
plan_section: "Dockerfile changes"
depends_on: []
---

## Objectives & Success Criteria

The Dockerfile produces a `/app/constraints.txt` in the final image that pins all hassette dependencies plus hassette itself. A stale lockfile causes a build failure. `git` is removed from the final stage.

- `docker build` succeeds and the resulting image contains `/app/constraints.txt`
- The constraints file contains pinned versions (e.g., `pydantic==2.12.3`) and a `hassette==<version>` line
- `RUN uv lock --check` fails the build if `uv.lock` is stale relative to `pyproject.toml`
- `git` is not available in the final image (`docker run <image> which git` returns non-zero)

## Subtasks

1. Add `RUN uv lock --check` after copying `pyproject.toml` and `uv.lock` but before `uv sync` in the builder stage of `Dockerfile`
2. Add `RUN uv export --no-hashes --frozen --no-dev --no-editable --no-emit-project > /app/constraints.txt` after the final `uv sync` in the builder stage
3. Append `hassette==<version>` to the constraints file: `RUN echo "hassette==$(uv version --short)" >> /app/constraints.txt`
4. Remove `git \` from the `apt-get install` list in the final stage (line ~56)
5. Add `constraints.txt` to `.dockerignore` with a comment explaining it's generated at build time

## Test Strategy

N/A — Dockerfile changes are verified by the existing Docker CI workflow (`docker-tests.yml`) which builds the image. The constraints file presence and content will be tested in WP03's integration tests. The `uv lock --check` guard is a build-time check that prevents stale lockfiles from being shipped.

## Review Guidance

- Verify `uv lock --check` is positioned after `COPY` of `pyproject.toml`/`uv.lock` but before any `uv sync`
- Verify `uv export` runs after the final `uv sync` (so the venv is fully populated)
- Verify `uv version --short` outputs the hassette project version (not the uv CLI version) — this was confirmed experimentally
- Verify `git` is not in the final stage's `apt-get install` list
- Confirm the constraints file is included in the `COPY --from=builder` step (it's in `/app` which is already copied)

## Avoid

- Do not remove the `uv sync` steps from the builder stage — those are build-time and correct; only runtime `uv sync` is being eliminated
- Do not use `uv pip freeze` for constraints generation — `uv export` from the lockfile is the canonical approach

## Activity Log

- 2026-04-04T00:00:00Z — system — lane=planned — WP created
