## Status: APPROVED

## Traceability Matrix
| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1 | T01 | `AppManifest` has an `autostart: bool` field defaulting to `True`; constructing a manifest without `autostart` yields `autostart is True`. |
| FR#2 | T01 | An `enabled=true, autostart=false` manifest appears in `registry.manifests` and in `get_full_snapshot().manifests`. |
| FR#3 | T02 | After `bootstrap_apps()`, an enabled+autostart=false app has zero running instances and is absent from `registry.apps`. |
| FR#4 | T02 | `start_app(app_key)` for that app produces a running instance (no config edit). |
| FR#5 | T02 | A reload `ChangeSet` whose `new_apps` contains an autostart=false app leaves it unstarted. |
| FR#6 | T02 | A reload does not start an autostart=false app that is not currently running even when its config/file changed. |
| FR#7 | T02 | A reload that changes an unrelated app leaves an already-running autostart=false app running. |
| FR#8 | T02 | A reload of a running autostart=false app whose config changed reloads it and leaves it running. |
| FR#9 | T01 | `get_full_snapshot()` reports `status == "stopped"` (not `"disabled"`) for an enabled+autostart=false manifest with no running instances/failures. |
| FR#10 | T01 | `get_full_snapshot()` sets `autostart` on each `AppManifestInfo` matching the manifest's value. |
| FR#10 | T03 | `AppManifestResponse` has an `autostart` field, and the mapper sets it from `AppManifestInfo.autostart` (mapper test asserts both `True` and `False` carry through). |
| FR#11 | T04 | The apps table renders a visible "no autostart" marker on rows where `autostart === false`, and no marker when `autostart === true` (frontend test asserts both). |
| FR#12 | T02 | `enabled=false` apps are still skipped at boot and report `disabled` status (unchanged). |
| FR#13 | T01 | A manifest dict with no `autostart` key parses to `autostart is True`. |
| FR#13 | T02 | An app with no `autostart` key still auto-starts at boot (existing fixture apps continue to start). |
| AC#1 | T01 | A unit test confirms `autostart` round-trips through config parsing and absent-key defaults to `True`. |
| AC#2 | T02 | Integration test confirms the autostart-off app is absent from `registry.apps` after bootstrap. |
| AC#3 | T01 | A unit test confirms an enabled+autostart=false manifest's snapshot entry has `status == "stopped"` and `autostart is False`. |
| AC#4 | T02 | Integration test confirms `start_app` starts the autostart-off app. |
| AC#5 | T02 | Integration test confirms `new_apps={autostart-off key}` reload leaves it unstarted. |
| AC#6 | T02 | Integration test confirms an unrelated reload leaves a manually-started autostart-off app running. |
| AC#7 | T02 | Integration test confirms autostart-absent apps auto-start at boot. |
| AC#8 | T02 | Integration test confirms `disabled_app` stays absent + `disabled` status. |
| AC#9 | T03 | The `/apps` manifest response includes `autostart` per app (mapper/model tests confirm), and the `hassette app` CLI list renders an `Autostart` column. |
| AC#9 | T04 | `generated-types.ts` includes `autostart` on the manifest type, `AppRow` carries it, and the UI marker renders for autostart=false apps; a screenshot of the marker is captured for the PR. |

## Coverage Gaps
None.

## Contradictions
None.

## Warnings
- **FR#13 claimed by two tasks (T01 and T02):** Both tasks implement FR#13 at different layers (config parsing in T01; lifecycle boot behavior in T02). Both have distinct Verify items. This is intentional and correct — no action needed.
- **FR#10 claimed by two tasks (T01 and T03):** T01 covers `AppManifestInfo`/`get_full_snapshot`; T03 covers `AppManifestResponse`/REST mapper. Both have distinct Verify items. Intentional — no action needed.
- **AC#9 claimed by two tasks (T03 and T04):** T03 covers the API/CLI half; T04 covers the UI/screenshot half. Both are required by the design doc, which references FR#10 and FR#11 in AC#9's scope. Intentional — no action needed.
- **AC#3 cross-asserted in T02 Prompt but not in T02 implements:** T02's Prompt mentions "AC#2/AC#3 cross-check" as an integration-level confirmation, but AC#3 is owned by T01 and is not listed in T02's `implements`. This is a documentation style choice, not a gap — the ownership is unambiguous.
