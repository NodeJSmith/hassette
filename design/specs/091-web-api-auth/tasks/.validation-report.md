## Verdict: PASS

> PASS — all 21 FRs and 19 ACs are covered by at least one task with a matching Verify item, no
> contradictions between task Prompt/Focus sections and the design doc, no non-local verification
> criteria, and context.md has all required sections with non-empty content.

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1 | T05 | "Integration test confirms `GET /api/apps` (a representative non-exempt route) with no credential returns 401, and each of `GET /api/health/live`, `GET /api/health/ready`, `POST /api/auth/session` returns a non-401 response with no credential; a request to `GET /api/docs` with no credential returns 401 (confirming it is no longer exempt)." |
| FR#1 | T07 | (implements FR#1 via AC#1's WS portion — see AC#1 row for T07) |
| FR#1 | T14 | "`docs/pages/web-ui/index.md` no longer states the API is unauthenticated by default; it documents auth-on-by-default and the default-deny behavior (verifiable via `grep -i \"no authentication\"` returning no match in the rewritten section, and `grep -i \"trusted_proxies\"` returning a match)." |
| FR#2 | T03 | "Unit test confirms a peer address matching a `trusted_proxies` IP, CIDR, or resolved-hostname entry is reported as trusted by the matcher function." |
| FR#2 | T14 | "`docs/pages/web-ui/index.md` documents `trusted_proxies` as the recommended path for a forward-auth gateway setup, including a concrete reverse-proxy TLS-termination config snippet." |
| FR#3 | T03 | "The peer-match function's signature accepts only an address string (verified by inspection/type signature — no headers parameter exists); unit test confirms passing a non-matching address with no other input still returns not-trusted (there is no header input to spoof)." |
| FR#4 | T08 | "Unit test confirms `WebApiService.serve()` constructs `uvicorn.Config` with `proxy_headers=False` (inspect the call, e.g. via mock/spy on `uvicorn.Config`)." |
| FR#5 | T08 | "Unit test confirms `on_initialize()` schedules a periodic job via `self.scheduler.run_every(...)` that calls T03's refresh function, in addition to calling it once at startup." |
| FR#6 | T04 | "Unit test confirms a bearer-token check against the correct token succeeds and against an incorrect token fails, using `secrets.compare_digest` (not `==` — inspect the implementation)." |
| FR#7 | T04 | "Unit test confirms a cookie minted for a given token can be verified successfully against that same token, and that minting/verifying does not depend on any server-side state..." |
| FR#7 | T13 | "Playwright e2e test confirms a cookie injected via `context.add_cookies` authenticates both a REST dashboard load and a WebSocket connection against the real running stack, without driving the login form." |
| FR#8 | T04 | "Unit test confirms a cookie whose embedded issuance timestamp exceeds `session_ttl` is rejected by the verify function, and one within the TTL is accepted." |
| FR#9 | T02 | "Unit test confirms that with no configured token and no existing file, a token is generated via `secrets.token_urlsafe(32)`, persisted atomically... with mode `0600`... and an INFO log line fires." |
| FR#9 | T14 | "`docs/pages/getting-started/docker/index.md` documents the generated-token-on-first-start flow, and `docs/pages/getting-started/docker/troubleshooting.md` includes a \"401 after upgrade\" entry pointing at `docker logs` for the token." |
| FR#10 | T02 | "Unit test confirms a corrupt/truncated existing token file results in a fresh token being generated (not a crash), with an ERROR-level log line making the regeneration visible." |
| FR#11 | T07 | "Integration test confirms an unauthenticated WebSocket connection attempt receives close code `1008` and `accept()` is never called (verified by confirming no data can be sent/received on the connection before the close)." |
| FR#12 | T12 | "Component/unit test confirms `use-websocket.ts`'s `onclose` handler, on receiving `event.code === 1008`, does not call `scheduleReconnect()` and instead redirects to the login route; confirms other close codes still trigger the existing reconnect behavior unchanged." |
| FR#13 | T08 | "Unit test confirms `WebApiService` raises a startup error naming both `auth_enabled` and `host` when `auth_enabled=False` and `host=\"0.0.0.0\"`." |
| FR#14 | T08 | "Unit test confirms a WARNING log line naming TLS is emitted when `host=\"0.0.0.0\"` and `trusted_proxies=()`, and confirms no such warning fires when `trusted_proxies` is non-empty." |
| FR#15 | T01 | "Config load with `cors_origins=(\"*\",)` raises a `ValidationError` (unit test in `tests/unit/config/test_web_api_config.py`); config load with `cors_origins=(\"http://localhost:3000\",)` succeeds." |
| FR#16 | T10 | "Integration test confirms a successful `start`/`stop`/`reload`/`trigger`/log-level-change request produces an INFO log line naming the action and source IP, visible via `GET /api/logs/recent`." |
| FR#17 | T10 | "Integration test confirms a burst of failed-auth requests exceeding the threshold from one source within the window produces exactly one coalesced WARN log line, not one per attempt." |
| FR#18 | T09 | "Unit test confirms `HassetteCLIClient` reads the token from `HASSETTE__WEB_API__AUTH_TOKEN` when set, falls back to the token file when it isn't, and attaches `Authorization: Bearer <token>` to outgoing requests; confirms no CLI subcommand defines a bare `--token <value>` argument." |
| FR#18 | T14 | "`docs/pages/cli/configuration.md` documents `HASSETTE__WEB_API__AUTH_TOKEN` and the CLI's credential-resolution order." |
| FR#19 | T01 | "`auth_token` is declared as `SecretStr \| None` on `WebApiConfig`; a `WebApiConfig` instance with `auth_token` set renders it masked (not plaintext) via `repr()`/`str()`." |
| FR#20 | T04 | "Unit test confirms the Secure-flag decision function returns `True` only when the peer is trusted (per T03's matcher) AND `X-Forwarded-Proto` says `https`; returns `False` when the peer is untrusted regardless of the header value, and confirms it calls T03's matcher rather than a separate comparison." |
| FR#21 | T02 | "Unit test exercises each of the three resolution branches (explicit config, existing file, freshly generated) and asserts each produces a distinct INFO log message, not a shared generic one." |
| AC#1 | T05 | "Integration test confirms a mutation endpoint (`POST /api/apps/{app_key}/start`), `GET /api/apps/{app_key}/source`, and `GET /api/config`, each with no credential, all return 401. (The WS-upgrade portion of AC#1 is verified separately in T07.)" |
| AC#1 | T07 | "Integration test (WS portion) confirms the WS upgrade with no credential results in close code `1008`, extending `tests/integration/web_api/test_ws_endpoint.py`." |
| AC#2 | T11 | "Test confirms a wrong bearer token returns 401 and a correct one returns 200." |
| AC#3 | T11 | "Test confirms a correct session cookie returns 200." |
| AC#4 | T03 | "Unit test confirms a peer address matching a `trusted_proxies` IP or CIDR entry is trusted by the matcher, and a non-matching address is not." |
| AC#4 | T11 | "Test confirms a client address matching a `trusted_proxies` IP/CIDR entry returns 200 with no credential." |
| AC#5 | T11 | "Test confirms a client address matching a `trusted_proxies` hostname entry returns 200, and confirms the trusted set updates after a simulated periodic-refresh tick." |
| AC#6 | T11 | "Test confirms a spoofed `X-Forwarded-For` header from an untrusted client address is rejected (401), not treated as trusted." |
| AC#7 | T06 | "Integration test confirms `GET /api/health/live`, `GET /api/health/ready` are reachable with zero credentials, and `POST /api/auth/session` with a correct token in the body (no prior credential) returns a successful response with a session cookie set." |
| AC#8 | T08 | "Unit test confirms starting with `auth_enabled=false` and `host=\"0.0.0.0\"` fails at startup with an error naming both settings (same test as FR#13, phrased at the AC's exact scenario)." |
| AC#9 | T08 | "Unit test confirms the WARNING fires for empty `trusted_proxies` with non-loopback host, and does not fire when `trusted_proxies` is non-empty (same test as FR#14, phrased at the AC's exact scenario)." |
| AC#10 | T01 | "Unit test confirms `WebApiConfig(cors_origins=(\"*\",))` raises at construction/validation time." |
| AC#11 | T01 | "New system test in `tests/system/test_web_api.py` confirms `GET /api/config` (authenticated) never contains the plaintext `auth_token` value in its response body, mirroring the existing HA-token test at lines 75-93." |
| AC#12 | T02 | "Unit test confirms a corrupted/truncated token file at resolution time results in a fresh token, an ERROR log line, and the function returning successfully (not raising)." |
| AC#13 | T12 | "Frontend component test confirms the WS client stops reconnecting and navigates to the login view on close code 1008, rather than retrying indefinitely." |
| AC#14 | T09 | "`hassette status` (or an equivalent CLI command) successfully authenticates using the token file or `HASSETTE__WEB_API__AUTH_TOKEN`, verified via a unit test exercising the credential-resolution + header-attachment path; `--help` output / source inspection confirms no subcommand accepts a literal token argument." |
| AC#15 | T11 | "Test confirms a cookie minted more than `session_ttl` seconds ago is rejected, and one within the TTL is accepted." |
| AC#16 | T10 | "Same integration test as FR#16, phrased at the AC's exact scenario (a `start`/`stop`/`reload`/`trigger`/log-level-change request from an authenticated caller)." |
| AC#17 | T10 | "Same integration test as FR#17, phrased at the AC's exact scenario (a burst of failed-auth requests from one source within a window produces exactly one coalesced WARN)." |
| AC#18 | T11 | "Test confirms a trusted peer with `X-Forwarded-Proto: https` gets a `Secure`-flagged cookie from `POST /api/auth/session`, and a non-trusted peer with the same spoofed header does not." |
| AC#19 | T02 | "Unit test asserts on the exact log output distinguishing all three branches (explicit config vs. existing file vs. freshly generated)." |

## Coverage Gaps

None. All 21 FRs (FR#1-FR#21) and all 19 ACs (AC#1-AC#19) appear in at least one task's `implements`
field, and every claimed identifier has a corresponding `- [ ]` Verify item in that same task.

## Contradictions

None. Every task's Prompt and Focus section read consistently with the design doc sections they cite
(Architecture → Credential model, Cookie Secure flag, Middleware and routing, WebSocket auth, Startup
guards, CORS validator, Misuse-visibility logging, CLI). Notable places checked closely, all
consistent:

- T01's `session_ttl` default of 3600s and T10's 10-attempts/5-minute coalescing threshold are both
  concrete choices where the design doc only specifies "short default" / gives an illustrative
  example ("12 failed auth attempts... in the last 5 minutes") — both tasks pick a concrete value
  and document the choice, which the design doc explicitly permits ("Pick concrete threshold/window
  values consistent with that example").
- T05's registration of the auth middleware "inside `CORSMiddleware`" matches design.md's Architecture
  → Middleware and routing section exactly, including carrying forward the doc's own flagged
  uncertainty (Open Questions) as a task-level empirical verification step rather than asserting it
  blindly.
- T13 flags and corrects an inaccurate citation in design.md (a nonexistent `browser_context_args`
  fixture) rather than silently building against a fixture that doesn't exist — this is a task
  correctly deviating from a stale line in the design doc's Existing Tests to Adapt section, not a
  contradiction with the design's actual functional requirements (FR#7).

## Non-Local Criteria

None. Every Verify item across all 14 tasks resolves to a unit test, integration test, e2e test, or a
local `grep` check against docs content (T14) — all executable and observable within the local repo
by the task executor. No item depends on CI status, GitHub Actions, post-merge behavior, or any
external review process.

## Warnings

- design.md contains three informal references to *GitHub issue #1117's own* AC numbering, formatted
  with a space (`AC #4` at line 29, `AC #5` at lines 22 and 216) rather than this design doc's
  `AC#N` convention. These are prose references to the originating issue's acceptance criteria (external
  to this design doc), not identifiers meant to be part of this doc's own FR#/AC# traceability set —
  none of them correspond to a numbered item in this doc's own `## Acceptance Criteria` section, and no
  task claims to implement them as if they were local ACs. Not a coverage or traceability problem; noted
  only because the string pattern technically deviates from `^AC#\d+$`.
- T14 (documentation) claims `implements: ["FR#1", "FR#2", "FR#9", "FR#18"]`, all of which are
  primarily implemented by earlier code tasks (T05/T07, T03, T02, T09 respectively) and only
  *documented* by T14. This is consistent with `.claude/rules/design-completeness.md`'s requirement
  that user-facing behavior changes ship with docs in the same PR, and each FR's Verify item in T14 is
  phrased as a documentation-content check (grep-verifiable), not a re-claim of the underlying
  behavior — not flagged as a scope violation, but noted since a reviewer scanning `implements` fields
  alone might initially read it as duplicate ownership rather than doc coverage of already-implemented
  behavior.
