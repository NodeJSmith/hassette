## Verdict: PASS

> PASS — all 23 FRs (FR#1–FR#23) and all 22 ACs (AC#1–AC#22) are covered by at least one task with a
> matching Verify item, no contradictions found between task Prompt/Focus sections and the current
> design doc, no non-local verification criteria, and context.md has all five required sections
> non-empty (plus a populated optional sixth). Two non-blocking warnings noted below (stale summary
> text in context.md, and pre-existing external-issue AC references in design.md).

This validation was performed independently against the files currently on disk (design.md and all
15 task files were read in full). The pre-existing `.validation-report.md` in `tasks/` was read only
after independent analysis was complete, to cross-check; its traceability claims were corroborated,
not assumed.

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1 | T05 | "Integration test confirms `GET /api/apps` ... with no credential returns 401 when `auth_enabled=True`, and each of `GET /api/health/live`, `GET /api/health/ready`, `POST /api/auth/session` returns a non-401 response with no credential; a request to `GET /api/docs` with no credential returns 401 ...; the same non-exempt route ... returns a non-401 response when `auth_enabled=False`." |
| FR#1 | T07 | (WS portion, via AC#1 — see AC#1/T07 row) |
| FR#1 | T14 | "`docs/pages/web-ui/index.md` no longer states the API is unauthenticated by default; it documents auth-on-by-default and the default-deny behavior (verifiable via `grep -i "no authentication"` returning no match ..., and `grep -i "trusted_proxies"` returning a match)." |
| FR#2 | T03 | "Unit test confirms a peer address matching a `trusted_proxies` IP, CIDR, or resolved-hostname entry is reported as trusted by the matcher function; confirms `trusted_proxies=("0.0.0.0/0",)` and `("::/0",)` both raise at parse time." |
| FR#2 | T14 | "`docs/pages/web-ui/index.md` documents `trusted_proxies` as the recommended path for a forward-auth gateway setup, including a concrete reverse-proxy TLS-termination config snippet." |
| FR#3 | T03 | "The peer-match function's signature accepts only an address string ...; unit test confirms passing a non-matching address with no other input still returns not-trusted." |
| FR#4 | T08 | "Unit test confirms `WebApiService.serve()` constructs `uvicorn.Config` with `proxy_headers=False`." |
| FR#5 | T08 | "Unit test confirms `on_initialize()` schedules a periodic job via `self.scheduler.run_every(...)` that calls T03's refresh function, in addition to calling it once at startup." |
| FR#6 | T04 | "Unit test confirms a bearer-token check against the correct token succeeds and against an incorrect token fails, using `secrets.compare_digest`." |
| FR#7 | T04 | "Unit test confirms a cookie minted for a given token can be verified successfully against that same token, and that minting/verifying does not depend on any server-side state." |
| FR#7 | T13 | "Playwright e2e test ... confirms a cookie injected via `context.add_cookies` authenticates both a REST dashboard load and a WebSocket connection against the real running stack, without driving the login form." |
| FR#8 | T04 | "Unit test confirms a cookie whose embedded issuance timestamp exceeds `session_ttl` is rejected by the verify function, and one within the TTL is accepted." |
| FR#9 | T02 | "Unit test confirms that with no configured token and no existing file, a token is generated ..., persisted atomically ... with mode `0600` ..., and an INFO log line fires containing a ready-to-use URL." |
| FR#9 | T14 | "`docs/pages/getting-started/docker/index.md` documents the generated-token-on-first-start flow, and `.../troubleshooting.md` includes a "401 after upgrade" entry pointing at `docker logs`." |
| FR#10 | T02 | "Unit test confirms a corrupt/truncated existing token file results in a fresh token being generated (not a crash), with an ERROR-level log line making the regeneration visible." |
| FR#11 | T07 | "Integration test confirms an unauthenticated WebSocket connection attempt receives close code `1008` and `accept()` is never called." |
| FR#12 | T12 | "Component/unit test confirms `use-websocket.ts`'s `onclose` handler, on receiving `event.code === 1008`, does not call `scheduleReconnect()` and instead redirects to the login route; confirms other close codes still trigger the existing reconnect behavior unchanged. Additionally, a test in `app.test.tsx` confirms rendering `App()` at `/login` does not mount `WebSocketEffect`, `TelemetryHealthEffect`, `Sidebar`, or `StatusBar`." |
| FR#13 | T08 | "Unit test confirms `WebApiService` raises a startup error naming both `auth_enabled` and `host` when `auth_enabled=False` and `host="0.0.0.0"`; confirms no error when `host="127.0.0.1"` or `"localhost"`." |
| FR#14 | T08 | "Unit test confirms a WARNING log line naming TLS is emitted when `host="0.0.0.0"` and `trusted_proxies=()`, and confirms no such warning fires when `trusted_proxies` is non-empty or when `host="localhost"`." |
| FR#15 | T01 | "Config load with `cors_origins=("*",)` raises a `ValidationError` ...; config load with `cors_origins=("http://localhost:3000",)` succeeds." |
| FR#16 | T10 | "Integration test confirms a successful `start`/`stop`/`reload`/`trigger`/log-level-change request produces an INFO log line naming the action and source IP, visible via `GET /api/logs/recent`." |
| FR#17 | T05 | "Integration test confirms a burst of failed-auth requests from one source within the window produces exactly one coalesced WARN log line, not one per attempt, visible via `GET /api/logs/recent`." |
| FR#18 | T09 | "Unit test confirms `HassetteCLIClient` resolves the token from `config.web_api.auth_token` (via `HASSETTE__WEB_API__AUTH_TOKEN`), falls back to the token file, and attaches `Authorization: Bearer <token>`; confirms no CLI subcommand defines a bare `--token <value>` argument." |
| FR#18 | T14 | "`docs/pages/cli/configuration.md` documents `HASSETTE__WEB_API__AUTH_TOKEN` and the CLI's credential-resolution order." |
| FR#19 | T01 | "`auth_token` is declared as `SecretStr \| None` on `WebApiConfig`; a `WebApiConfig` instance with `auth_token` set renders it masked (not plaintext) via `repr()`/`str()`." |
| FR#20 | T04 | "Unit test confirms the Secure-flag decision function returns `True` only when the peer is trusted (per T03's matcher) AND `X-Forwarded-Proto` says `https`; returns `False` when the peer is untrusted regardless of header value; confirms it calls T03's matcher rather than a separate comparison." |
| FR#21 | T02 | "Unit test exercises each of the three resolution branches ... and asserts each produces a distinct INFO log message, not a shared generic one." |
| FR#22 | T04 | "Unit test confirms the renewal predicate returns `False` for a freshly minted cookie, `True` for one past half of `session_ttl`, and `False` again for one already past full `session_ttl`." |
| FR#22 | T05 | "Integration test confirms a request carrying a cookie past its half-life comes back with a `Set-Cookie` header whose new value verifies successfully; a fresh cookie returns no `Set-Cookie`; bearer/trusted-proxy-authenticated requests return no `Set-Cookie`." |
| FR#23 | T05 | "Integration test confirms `GET /` and a representative path under `/assets` return their content with no credential while `auth_enabled=True`, and that `GET /api/config` with no credential still returns 401." |
| AC#1 | T05 | "Integration test confirms a mutation endpoint (`POST /api/apps/{app_key}/start`), `GET /api/apps/{app_key}/source`, and `GET /api/config`, each with no credential, all return 401. (The WS-upgrade portion of AC#1 is verified separately in T07.)" |
| AC#1 | T07 | "Integration test (WS portion) confirms the WS upgrade with no credential results in close code `1008`, extending `tests/integration/web_api/test_ws_endpoint.py`." |
| AC#2 | T11 | "Test confirms a wrong bearer token returns 401 and a correct one returns 200." |
| AC#3 | T11 | "Test confirms a correct session cookie returns 200." |
| AC#4 | T03 | "Unit test confirms a peer address matching a `trusted_proxies` IP or CIDR entry is trusted by the matcher, and a non-matching address is not." |
| AC#4 | T11 | "Test confirms a client address matching a `trusted_proxies` IP/CIDR entry returns 200 with no credential." |
| AC#5 | T11 | "Test confirms a client address matching a `trusted_proxies` hostname entry returns 200, and confirms the trusted set updates after a simulated periodic-refresh tick." |
| AC#6 | T11 | "Test confirms a spoofed `X-Forwarded-For` header from an untrusted client address is rejected (401), not treated as trusted." |
| AC#7 | T06 | "Integration test confirms `GET /api/health/live`, `GET /api/health/ready` are reachable with zero credentials, and `POST /api/auth/session` with a correct token in the body ... returns a successful response with a session cookie set." |
| AC#8 | T08 | "Unit test confirms starting with `auth_enabled=false` and `host="0.0.0.0"` fails at startup with an error naming both settings." |
| AC#9 | T08 | "Unit test confirms the WARNING fires for empty `trusted_proxies` with non-loopback host, and does not fire when `trusted_proxies` is non-empty." |
| AC#10 | T01 | "Unit test confirms `WebApiConfig(cors_origins=("*",))` raises at construction/validation time." |
| AC#11 | T01 | "New system test in `tests/system/test_web_api.py` confirms `GET /api/config` (authenticated) never contains the plaintext `auth_token` value ...; the full existing system-test suite still passes unchanged after `auth_enabled`'s default flips to `True`." |
| AC#12 | T02 | "Unit test confirms a corrupted/truncated token file at resolution time results in a fresh token, an ERROR log line, and the function returning successfully (not raising)." |
| AC#13 | T12 | "Frontend component test confirms the WS client stops reconnecting and navigates to the login view on close code 1008, rather than retrying indefinitely." |
| AC#14 | T09 | "`hassette status` ... successfully authenticates using the token file or `HASSETTE__WEB_API__AUTH_TOKEN` ...; `--help` output / source inspection confirms no subcommand accepts a literal token argument." |
| AC#15 | T11 | "Test confirms a cookie minted more than `session_ttl` seconds ago is rejected, and one within the TTL is accepted." |
| AC#16 | T10 | "Same integration test as FR#16, phrased at the AC's exact scenario." |
| AC#17 | T05 | "Same integration test as FR#17, phrased at the AC's exact scenario." |
| AC#18 | T11 | "Test confirms a trusted peer with `X-Forwarded-Proto: https` gets a `Secure`-flagged cookie from `POST /api/auth/session`, and a non-trusted peer with the same spoofed header does not." |
| AC#19 | T02 | "Unit test asserts on the exact log output distinguishing all three branches (explicit config vs. existing file vs. freshly generated)." |
| AC#20 | T05 | "Integration test confirms a cookie past its half-life is renewed on the response, a fresh one is not, and bearer/trusted-proxy requests never mint one." |
| AC#21 | T05 | "Integration test confirms `GET /` and an `/assets` path are reachable with no credential while `GET /api/config` still returns 401." |
| AC#22 | T05 | "The same test confirms a burst of wrong-token `POST /api/auth/session` requests produces the same coalesced WARN — proving the counter keys off the outgoing 401 rather than the middleware's own reject branch." |

## Coverage Gaps

None. All 23 FRs (FR#1–FR#23) and all 22 ACs (AC#1–AC#22) appear in at least one task's `implements`
field, and every claimed identifier in every task has a corresponding `- [ ]` Verify item in that same
task's Verify section (checked task-by-task, not sampled).

Specifically checked for the two failure modes this revision could plausibly have introduced:

- **Ownership moves**: FR#17/AC#17 (coalesced failed-auth WARN) moved from T10 to T05 in this
  revision. Verified T10's current `implements` is exactly `["FR#16", "AC#16"]` with no residual
  FR#17/AC#17 mention anywhere in its Prompt/Focus/Verify (its Summary explicitly states the move and
  why: "FR#17's coalesced failed-auth WARN is not part of this task — it moved into T05's middleware").
  Verified T05's `implements` includes `FR#17`, `AC#17`, and `AC#22` (the new login-endpoint
  brute-force AC), each with a matching Verify bullet. No orphaned claim, no double-claim.
- **New identifiers from the revision** (FR#22, FR#23, AC#20, AC#21, AC#22) are each claimed by a task
  (T04 and/or T05) with a Verify item present — none are silently unimplemented despite being recent
  additions.

## Contradictions

None found. Checked every task's Prompt and Focus section against the specific design.md
FR#/AC#/Architecture subsections it cites, with particular attention to sections most likely to have
drifted during the append-only revision (Credential model, Middleware and routing, Misuse-visibility
logging, Startup guards):

- T05's `/api/` prefix scoping (FR#23), sliding-renewal composition (FR#22), and 401-keyed
  failed-auth counting (FR#17, including counting the exempt login route) all match design.md's
  Architecture → Middleware and routing / Misuse-visibility logging sections precisely, including the
  detail that the counter has "no shared tracker object" with T10's success-logging.
  T10's Summary correctly disclaims ownership of FR#17 rather than silently keeping a stale claim.
- T01's `session_ttl` default (3600) and docstring instructions match FR#22's sliding-renewal framing
  ("this bounds the lifetime of a single cookie value, not the length of a working session") — no
  leftover text implying a fixed hourly re-login, which would have contradicted FR#22.
- T04's Secure-flag function (FR#20) and T05's reuse of it for sliding renewal both correctly cite
  reusing the same trusted-peer check rather than re-deriving it — consistent with design.md's
  Architecture → Cookie Secure flag section.
- T09's credential resolution correctly reads via `HassetteConfig`/pydantic-settings rather than
  `os.environ` directly, matching context.md's Constraints & Anti-Patterns list (a correction this
  revision made per the prior report — reverified against the current file text, still present).
- T13's use of `live_server_ws` (not `live_server`) matches T07/T08's WS backend
  (`ws="websockets-sansio"`); no stale reference to a `live_server` default remains.

## Non-Local Criteria

None. Every Verify item across all 15 tasks resolves to a unit test, integration test, e2e/Playwright
test, or a local `grep`/inspection check against docs content (T14) or tooling invocation (T15) — all
executable and observable within the local repo by a task executor. No item depends on CI status,
GitHub Actions, post-merge behavior, or any external review process.

## Warnings

- **context.md "Design Doc References" section is stale post-revision.** It states `## Functional
  Requirements — FR#1 through FR#21, the authoritative numbered requirements` and `## Acceptance
  Criteria — AC#1 through AC#19, each mapped to one or more FRs` (lines ~117, ~121). The current
  design.md has FR#1 through **FR#23** and AC#1 through **AC#22**. This is inconsistent with the same
  file's own Key Decisions section, which *was* updated to reference FR#22 (sliding renewal, Key
  Decision 5) and the `/api/` prefix scoping that is FR#23 (Key Decision 11) — so the substantive
  content is current, only this one summary line's numeric range was not bumped. Low risk (an executor
  reads FR#/AC# text from design.md directly, not from this range citation), but worth a one-line fix
  since a reader skimming context.md alone would undercount the requirement set by 2 FRs and 3 ACs.
- design.md contains two informal references to *GitHub issue #1117's own* AC numbering, formatted
  with a space — `AC #5` (design.md:22) and `AC #4` (design.md:29), plus a third at design.md:233
  ("issue #1117 AC #5"). These are prose references to the originating GitHub issue's acceptance
  criteria (external to this design doc), not identifiers in this doc's own `## Acceptance Criteria`
  section — none of them correspond to a numbered item in this doc's own AC list, and no task claims
  to implement them as local ACs. Not a coverage or traceability problem; noted only because the
  string pattern technically deviates from `^AC#\d+$` and a naive grep for `AC#` would miss them
  (which is arguably correct behavior here, since they're not this doc's identifiers).
- T14 claims `implements: ["FR#1", "FR#2", "FR#9", "FR#18"]`, each of which is primarily implemented
  by an earlier code task (T05/T07, T03, T02, T09/T14 respectively) and only *documented* by T14. Each
  FR's Verify item in T14 is phrased as a documentation-content check (grep-verifiable against docs
  prose), not a re-claim of the underlying runtime behavior. Consistent with
  `.claude/rules/design-completeness.md`'s same-PR docs requirement; not a scope violation, but a
  reviewer scanning bare `implements` fields across tasks could initially misread this as duplicate
  ownership rather than doc coverage of already-implemented behavior.
