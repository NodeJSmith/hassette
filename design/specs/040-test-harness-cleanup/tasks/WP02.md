---
depends_on:
- WP01
lane: done
title: Replace _HassetteMock with _TestableHassette and add proxy properties
work_package_id: WP02
---

## Objectives & Success Criteria

- `_HassetteMock` (90 lines) is deleted from `harness.py`.
- `_TestableHassette(Hassette)` exists with exactly 2 overrides: `_should_skip_dependency_check → True` and `wait_for_ready → True`.
- `HassetteHarness.__init__` creates `_TestableHassette(config=self.config)` instead of `_HassetteMock(config=self.config)`.
- `HassetteHarness` has proxy properties for: `task_bucket`, `send_event`, `shutdown_event`, `api`, `states` (in addition to existing `bus`, `scheduler`, `state_proxy`, `bus_service`, `scheduler_service`, `app_handler`).
- All existing tests pass — the harness's internal lifecycle (`start()`, `stop()`, starters) works identically with the real `Hassette` subclass.

## Subtasks

1. Add `_TestableHassette` class to `src/hassette/test_utils/harness.py`, immediately before `HassetteHarness`. Two method overrides only. Import `Hassette` from `hassette.core.core`.

2. Delete the entire `_HassetteMock` class (lines 90–181).

3. Update `HassetteHarness.__init__`: change `self.hassette = _HassetteMock(config=self.config)` to `self.hassette = _TestableHassette(config=self.config)`.

4. Add proxy properties to `HassetteHarness`:
   - `task_bucket` → `self.hassette.task_bucket`
   - `send_event` → `self.hassette.send_event` (method delegation, not property — use a wrapper async method)
   - `shutdown_event` → `self.hassette.shutdown_event`
   - `api` → `self.hassette.api` (with None guard matching existing pattern)
   - `states` → `self.hassette.states` (with None guard)

5. Verify that `HassetteHarness.start()` works with `_TestableHassette` — the 45 private attribute writes (`_api_service`, `_bus`, `_event_stream_service`, etc.) work because `_TestableHassette` inherits all null slots from `Hassette.__init__`.

6. Remove any imports of `_HassetteMock` if they exist outside `harness.py`.

## Test Strategy

- **Existing test suite**: Run `timeout 300 uv run nox -s dev -- -n 2` — all tests pass.
- **Structural test**: Update `tests/unit/test_harness_dependency_consistency.py` if it references `_HassetteMock` — replace with `_TestableHassette`.
- No new test files needed — the existing test suite exercises every harness lifecycle path. The proxy properties are verified transitively by the fixture migration in WP03.

## Review Guidance

- Verify `_HassetteMock` is fully deleted — no remnants, no dead imports.
- Verify `_TestableHassette` has exactly 2 overrides and no attribute declarations (those come from `Hassette.__init__` via inheritance).
- Verify the `send_event` proxy delegates correctly — it's an async method on `Hassette`, not a property. Tests that monkey-patch it (e.g., `test_service_watcher.py:120`) will be handled in WP03.
- Verify `HassetteHarness.__init__`'s `context.set_global_hassette()` call still works — it should, because `_TestableHassette.__init__` (via `Hassette.__init__`) does NOT call `context.set_global_hassette()` (that moved to `wire_services()` in WP01).
