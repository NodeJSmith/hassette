---
depends_on:
- WP02
lane: doing
title: Migrate fixtures to yield HassetteHarness and update all integration test files
work_package_id: WP03
---

## Objectives & Success Criteria

- All 8 fixtures in `fixtures.py` that currently yield `cast("Hassette", harness.hassette)` yield `harness` directly with return type `AsyncIterator[HassetteHarness]`.
- All integration test files (15 files) and the root `tests/conftest.py` use `HassetteHarness` type annotations and public accessor properties — no private attribute access (`._bus`, `._scheduler`, `._state_proxy`, `._app_handler`, `._bus_service`) on fixture parameters.
- `test_service_watcher.py`'s `send_event` monkey-patch assigns on `harness.hassette.send_event` instead of `harness.send_event`.
- Verified by grep: `grep -rn '\._[a-z]' tests/integration/ --include='*.py'` returns no matches on harness fixture parameters (excluding `hassette_instance` in `test_core.py` and `conftest.py` setup code).
- All existing tests pass.

## Subtasks

1. Update 8 fixtures in `src/hassette/test_utils/fixtures.py`:
   - `hassette_with_nothing`: return type → `AsyncIterator[HassetteHarness]`, yield `harness`
   - `hassette_with_bus`: same
   - `hassette_with_scheduler`: same
   - `hassette_with_file_watcher`: same
   - `hassette_with_app_handler`: same
   - `hassette_with_app_handler_custom_config`: same
   - `hassette_with_state_proxy`: same
   - `hassette_with_state_registry`: same
   - Remove `from typing import cast` import if no longer used. Remove `Hassette` from TYPE_CHECKING imports if no longer used.

2. Update `tests/conftest.py:208-213` — `bucket_fixture`: change parameter type `hassette_with_nothing: Hassette` → `hassette_with_nothing: HassetteHarness`. Access `.task_bucket` works via the new proxy property. Update import.

3. Update `tests/integration/conftest.py` — the 4 autouse cleanup fixtures still reference private attrs on the old `Hassette` fixture type. These fixtures are intentionally left unchanged in this WP because WP04 deletes and replaces them entirely. Only update `hassette_instance` type annotations if needed (it stays as real `Hassette`).

4. Migrate each integration test file (mechanical substitutions):
   - `test_bus.py`: `hassette_with_bus._bus` → `hassette_with_bus.bus`; local `bus` fixture → `return hassette_with_bus.bus`; type annotations
   - `test_scheduler.py`: `hassette_with_scheduler._scheduler` → `hassette_with_scheduler.scheduler`
   - `test_state_proxy.py`: `hassette._state_proxy` → `hassette.state_proxy`; `hassette._bus` → `hassette.bus`
   - `test_states.py`: `hassette.send_event` works via proxy
   - `test_bus_error_handler.py`: private attribute access → public accessors
   - `test_scheduler_error_handler.py`: same
   - `test_apps.py`: `.task_bucket` works via proxy
   - `test_apps_env.py`: type annotations
   - `test_file_watcher.py`: private access → public accessors
   - `test_framework_telemetry.py`: `._bus_service` access — update to `hassette.bus_service`
   - `test_hot_reload.py`: private access → public accessors
   - `test_lifecycle_propagation.py`: private access → public accessors
   - `test_source_capture_integration.py`: private access → public accessors
   - `test_service_watcher.py`: `hassette.send_event = mock_send` → `hassette.hassette.send_event = mock_send`; `hassette.shutdown_event` works via proxy
   - `test_core.py`: uses `hassette_instance` (real `Hassette`) — private accesses are excluded from migration. Only update if it also uses harness fixtures.

5. Update imports in each file: remove `from hassette import Hassette` / `from hassette.core.core import Hassette` when only used for type annotations; add `from hassette.test_utils.harness import HassetteHarness` or `from hassette.test_utils import HassetteHarness`.

## Test Strategy

- **Full suite**: `timeout 300 uv run nox -s dev -- -n 2` after completing all files.
- **Grep verification**: `grep -rn '\.\(_bus\|_scheduler\|_state_proxy\|_app_handler\|_bus_service\)' tests/integration/ --include='*.py'` — only matches should be in `conftest.py` (cleanup fixtures, replaced in WP04) and `test_core.py` (excluded — uses `hassette_instance`).
- No new test files — existing tests exercise all migrated paths.

## Review Guidance

- Verify every `cast("Hassette", harness.hassette)` is replaced with `harness`.
- Verify `test_service_watcher.py`'s monkey-patch targets `harness.hassette.send_event`, not `harness.send_event` — the proxy property is read-only; assignment on it sets a local attribute on `HassetteHarness` instead of reaching the inner coordinator.
- Verify `test_core.py` private accesses on `hassette_instance` are NOT migrated — they're legitimate structural tests.
- Check that no file imports `Hassette` solely for a fixture type annotation that should now be `HassetteHarness`.
