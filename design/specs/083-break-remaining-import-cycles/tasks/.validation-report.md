## Status: APPROVED

> All 8 FRs and 7 ACs are covered by at least one task with a matching Verify item; context.md has all five required sections non-empty; no contradictions found.

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1 | T01 | `SchedulerServiceProtocol` defined in `types/types.py` with all seven members and exported from `types/__init__.py` |
| FR#2 | T01 | `StateReader` defined in `types/types.py` with all four members and exported from `types/__init__.py` |
| FR#3 | T03 | `grep -n "hassette.core" src/hassette/scheduler/scheduler.py` shows no runtime import; `Scheduler.scheduler_service` typed `SchedulerServiceProtocol`; pyright passes |
| FR#4 | T04 | `grep -n "hassette.core" src/hassette/state_manager/state_manager.py` shows no runtime import; `DomainStates`/`StateManager` typed against `StateReader` in both `.py` and `.pyi`; pyright passes |
| FR#5 | T02 | `grep -n "hassette.task_bucket" src/hassette/resources/base.py` shows no runtime import; `# lazy-import:` line at 145 is gone |
| FR#6 | T02 | Existing resource/task-bucket tests pass — non-`TaskBucket` `Resource` still gets its own default `TaskBucket`, and a `TaskBucket` is still its own `task_bucket` |
| FR#7 | T03 | `RULES` contains `scheduler-no-core` forbidding `hassette.core` for the `scheduler` layer |
| FR#7 | T04 | `RULES` contains `state_manager-no-core` forbidding `hassette.core` for the `state_manager` layer |
| FR#8 | T02 | `tools/check_module_boundaries.py` `RULES` contains `resources-no-task_bucket`; it forbids `hassette.task_bucket` for the `resources` layer |
| AC#1 | T04 | `python tools/check_module_boundaries.py` reports zero violations and summary names 8 import rules |
| AC#2 | T02 | `python tools/check_lazy_imports.py` passes and `grep -rn "lazy-import" src/hassette/` no longer lists `resources/base.py` |
| AC#3 | T03 | `scheduler/scheduler.py` contains no runtime `hassette.core` import; `check_module_boundaries.py` reports zero violations |
| AC#3 | T04 | `state_manager/state_manager.py` contains no runtime `hassette.core` import |
| AC#4 | T01 | `pyright` passes with no new errors and no `# pyright: ignore` added; conformance test passes for both concrete classes |
| AC#5 | T04 | `tests/unit/state_manager/test_domain_states_statereader.py` passes — `DomainStates` works against a dict-backed fake `StateReader` |
| AC#6 | T02 | `check_source` test asserts that reverting to a runtime `hassette.task_bucket` import in a `resources` file is flagged by `resources-no-task_bucket` |
| AC#6 | T03 | `check_source` test asserts that re-adding `from hassette.core.scheduler_service import SchedulerService` in a `scheduler` file is flagged by `scheduler-no-core` |
| AC#6 | T04 | Flipped `test_state_manager_import_of_core_*` asserts `from hassette.core.state_proxy import StateProxy` in a `state_manager` file is flagged by `state_manager-no-core` |
| AC#7 | T02 | Test confirms non-`TaskBucket` `Resource` receives its own bucket, a `TaskBucket` is its own bucket, and the guarded `RuntimeError` fires when the factory is unregistered |

## Coverage Gaps

None.

## Contradictions

None.

## Warnings

None.
