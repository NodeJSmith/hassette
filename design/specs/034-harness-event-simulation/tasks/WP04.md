---
depends_on:
- WP03
lane: done
title: Structural drift test and documentation updates
work_package_id: WP04
---

## Objectives & Success Criteria

Add the structural regression test that prevents future Bus/harness drift, and update testing documentation with representative simulation examples. After this WP:

- A unit test enumerates all `Bus.on_*` subscription methods and asserts `AppTestHarness.simulate_*` counterparts exist
- Adding a new `on_*` to Bus without a `simulate_*` causes an immediate test failure
- Testing docs include examples of typed DI testing with `simulate_state_change`, `simulate_call_service`, and `simulate_hassette_service_status`
- `mkdocs serve` builds cleanly

## Subtasks

1. Create `tests/unit/test_harness_coverage.py` with `test_all_bus_subscriptions_have_simulate_counterparts` — use `Bus.__dict__` filtered against `Resource.__dict__` to get only subscription methods (not lifecycle hooks like `on_initialize`/`on_shutdown`)
2. Update `docs/pages/testing/index.md` with representative examples:
   - State change with `D.StateNew[BinarySensorState]` typed DI handler
   - Service call with `D.Domain` typed DI handler
   - Hassette service failure simulation
3. Verify `mkdocs serve` builds without errors

## Test Strategy

`tests/unit/test_harness_coverage.py`:
- `test_all_bus_subscriptions_have_simulate_counterparts` — introspects `Bus.__dict__` for `on_*` methods not in `Resource.__dict__`, asserts matching `simulate_*` on `AppTestHarness`. Synchronous test, no fixtures needed.

## Review Guidance

- Verify drift test uses `Bus.__dict__` minus `Resource.__dict__` — NOT `dir(Bus)` with `startswith` exclusions
- Verify drift test would fail if a hypothetical `on_new_event` were added to Bus without a simulate counterpart
- Verify documentation examples are accurate — code snippets should work if copy-pasted into a test file
- Verify no exhaustive per-method documentation — representative examples only
