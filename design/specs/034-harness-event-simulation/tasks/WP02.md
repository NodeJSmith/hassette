---
depends_on:
- WP01
lane: done
title: Extract SimulationMixin and TimeControlMixin from AppTestHarness
work_package_id: WP02
---

## Objectives & Success Criteria

Decompose `app_harness.py` (1081 lines) into three focused modules per the design doc's file decomposition table. After this WP:

- `app_harness.py` contains setup/teardown, `__aenter__`/`__aexit__`, config validation, property accessors, state seeding (~450 lines)
- `simulation.py` contains `SimulationMixin` with all existing `simulate_*` methods + `_drain_task_bucket` (~250 lines initially, grows in WP03)
- `time_control.py` contains `TimeControlMixin` with `freeze_time`, `advance_time`, `trigger_due_jobs`, `_TestClock` (~200 lines)
- `_SimulationHost` Protocol is defined in `simulation.py` under `TYPE_CHECKING` declaring `_harness`, `_app`, `bus`, `_drain_task_bucket`
- `_require_harness()` is extracted as a shared method (on Protocol or SimulationMixin base)
- `AppTestHarness` inherits from both mixins, Pyright validates the Protocol contract
- All existing tests pass unchanged — zero behavioral change

## Subtasks

1. Create `src/hassette/test_utils/simulation.py` with `_SimulationHost` Protocol (under `TYPE_CHECKING`) declaring the attributes both mixins need: `_harness`, `_app`, `bus`, `_require_harness()`, and `_drain_task_bucket()`
2. Move `_drain_task_bucket`, `_raise_drain_timeout`, `simulate_state_change`, `simulate_attribute_change`, `simulate_call_service` from `app_harness.py` to `SimulationMixin` in `simulation.py`
3. Extract `_require_harness()` method — replace the 5+ inline `if harness is None: raise RuntimeError(...)` guards with calls to this method
4. Create `src/hassette/test_utils/time_control.py` with `TimeControlMixin` — move `_TestClock`, `freeze_time`, `advance_time`, `trigger_due_jobs`, `_stop_time_patchers`, `_release_freeze_time` from `app_harness.py`
5. Update `app_harness.py` to inherit from `SimulationMixin` and `TimeControlMixin` — update imports, remove moved methods
6. Run full test suite to confirm zero behavioral change

## Test Strategy

No new tests — this is a pure refactoring WP. All existing tests in `tests/integration/test_app_test_harness.py` must pass unchanged. Run `uv run pyright` to confirm Protocol satisfaction.

## Review Guidance

- Verify no behavioral changes — only code movement
- Verify `_SimulationHost` Protocol covers all attributes accessed by both mixins
- Verify `_require_harness()` replaces all inline guard patterns
- Verify MRO is correct — `AppTestHarness(SimulationMixin, TimeControlMixin)` or equivalent
- Verify Pyright passes with no new suppressions
- Check that `app_harness.py` is ~450 lines, `simulation.py` ~250 lines (grows in WP03 to ~500), `time_control.py` ~200 lines — per the design doc's decomposition table
