---
depends_on: []
lane: doing
title: Derive harness startup order from dependency graph
work_package_id: WP01
---

## Objectives & Success Criteria

Stream 1: Replace the hardcoded `_STARTUP_ORDER` list with a string-based topological sort over `_DEPENDENCIES`. Remove the `service_watcher` ghost entry from all metadata tables. Delete dead code: `start_resource` function, `_tasks` list and cancellation loop in `stop()`. Document `_exit_stack` as the canonical cleanup registry.

Success:
- No `_STARTUP_ORDER` list exists in `harness.py`
- `service_watcher` does not appear in `_DEPENDENCIES`, `_COMPONENT_CLASS_MAP`, or `_starters`
- `start_resource()` function is deleted
- `self._tasks` list and its cancellation loop in `stop()` are deleted
- A structural test asserts `_starters.keys() == set(_COMPONENT_CLASS_MAP.keys()) | set(harness_only_components)`
- The existing bidirectional consistency test is updated and passes alongside the new structural test
- All 2394 tests pass; Pyright clean

## Subtasks

1. Implement a string-based topological sort function in `src/hassette/test_utils/harness.py` that takes `dict[str, set[str]]` and returns `list[str]` — do NOT reuse `service_utils.topological_sort` (which operates on `type[Resource]`)
2. Remove the `service_watcher` entry from `_DEPENDENCIES`, `_COMPONENT_CLASS_MAP`, and any comments referencing it; remove the now-dead `from hassette.core.service_watcher import ServiceWatcher` import; add a comment to the `_COMPONENT_CLASS_MAP` omitted-entries block documenting the removal rationale
3. Replace `_STARTUP_ORDER` with a module-level computed value: `_STARTUP_ORDER = _topological_sort(_DEPENDENCIES)` — then update `start()` to iterate the computed order
4. Delete the `start_resource()` standalone function (confirmed: no callers outside `harness.py`, and it's unused inside too)
5. Delete `self._tasks: list[tuple[str, asyncio.Task[Any]]] = []` from `__init__` and the cancellation loop over `self._tasks` in `stop()`; add a docstring comment on `self._exit_stack` noting it as the canonical cleanup registry for starters that need background-task cleanup
6. Update `tests/unit/test_harness_dependency_consistency.py`: update imports (no more `_STARTUP_ORDER`), update the bidirectional test to work with the derived `_DEPENDENCIES`, add a structural test asserting `_starters.keys() == set(_COMPONENT_CLASS_MAP.keys()) | set(harness_only_components)` with `harness_only_components = {"api_mock", "file_watcher", "state_registry"}` explicitly enumerated

## Test Strategy

- **Structural test**: `tests/unit/test_harness_dependency_consistency.py::test_starters_match_component_map` — asserts key-set equality between `_starters` and `_COMPONENT_CLASS_MAP` plus `harness_only_components`
- **Updated bidirectional test**: `tests/unit/test_harness_dependency_consistency.py::test_harness_dependencies_match_real` — verifies forward (harness ⊆ real `depends_on`) and reverse (real ⊆ harness) for components in `_COMPONENT_CLASS_MAP`
- **Topological sort unit tests**: `tests/unit/test_harness_dependency_consistency.py::test_topological_sort_empty`, `test_topological_sort_linear`, `test_topological_sort_diamond`, `test_topological_sort_cycle_raises` — verify the string-based topo sort handles empty, linear, diamond DAG, and cycle detection
- Existing test suite (2394 tests) must pass unchanged

## Review Guidance

- Verify that the topological sort produces the same effective ordering as the old `_STARTUP_ORDER` for the current `_DEPENDENCIES` graph — order differences are acceptable only if the dependency constraints are still satisfied
- Verify `service_watcher` is fully removed from all tables, not just `_STARTUP_ORDER`
- Confirm `start_resource` has no remaining callers via grep before accepting deletion
- Confirm `_tasks` list is never populated by any starter before accepting deletion
- The `_exit_stack` comment must be concise (one line) — not a multi-paragraph docstring
