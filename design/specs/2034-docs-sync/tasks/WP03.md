---
work_package_id: "WP03"
title: "Update RecordingApi error messages to remove SimpleTestServer references"
lane: "done"
depends_on: []
---

## Objectives & Success Criteria

All `SimpleTestServer` references in `src/hassette/test_utils/recording_api.py` error messages and docstrings are replaced with generic guidance. The `_not_implemented()` helper and `__getattr__` fallback produce messages like: "RecordingApi.{method}() is not implemented. Seed state via AppTestHarness.set_state() for read methods, or use a full integration test for methods requiring a live HA connection." All existing tests pass after the change.

## Subtasks

1. In `src/hassette/test_utils/recording_api.py`, find the `_not_implemented()` helper function and replace the `SimpleTestServer` reference with generic guidance: "RecordingApi.{method_name}() is not implemented. Seed state via AppTestHarness.set_state() for read methods, or use a full integration test for methods requiring a live HA connection."
2. Find the `__getattr__` method and update its `NotImplementedError` message similarly — replace the `SimpleTestServer` reference.
3. Update the module docstring (top of file) and `RecordingApi` class docstring to remove `SimpleTestServer` references from prose. Replace with generic phrasing (e.g., "use a full integration test" instead of "use SimpleTestServer").
4. Find all individual method docstrings that say "Not implemented. Use SimpleTestServer for full coverage." and replace with "Not implemented — raises NotImplementedError." (the detailed message comes from `_not_implemented()`). Use content as the primary locator, not line numbers — they may have shifted.
5. Run existing tests to confirm nothing breaks: `uv run pytest tests/ -k "recording" -v`

## Test Strategy

No new tests needed — this is a message-text change. Existing `RecordingApi` tests verify the `NotImplementedError` is raised (they don't assert on message content). Run `uv run pytest tests/ -k "recording_api or RecordingApi" -v` to confirm no regressions.

## Review Guidance

- No instance of "SimpleTestServer" should remain in `recording_api.py` after this WP.
- The replacement messages must reference `AppTestHarness.set_state()` as the primary alternative for read methods.
- Docstring changes must be minimal — only replace `SimpleTestServer` references, don't rewrite surrounding prose.
- Check that tests still pass — some tests may assert on `NotImplementedError` being raised (not message content).
