---
work_package_id: "WP04"
title: "Refresh all Web UI screenshots from demo repo"
lane: "done"
depends_on: ["WP02"]
---

## Objectives & Success Criteria

All four Web UI screenshots are refreshed to reflect the current color system, dashboard polish, and responsive layout. If the demo repo is unavailable, screenshots may be deferred to a follow-up commit per the design doc's risk section — in that case, skip this WP and file an issue.
- `docs/_static/web_ui_dashboard.png`
- `docs/_static/web_ui_apps.png`
- `docs/_static/web_ui_sessions.png`
- `docs/_static/web_ui_logs.png`

Screenshots show non-zero telemetry data (persisted handler/job counts, invocations) — not a fresh empty-state dashboard.

## Subtasks

1. **Precondition**: Ensure the hassette-examples demo repo has completed at least one prior session. Run the demo once, let it run for ~30s to accumulate some telemetry, stop it, then restart. This ensures persisted handler/job counts are non-zero.
2. Follow the demo screenshot workflow from `reference_demo_screenshots.md`:
   - Stop production hassette container
   - Start demo HA + hassette from `/home/jessica/source/hassette-examples/docker-compose.yml`
   - Wait for apps to load (~15s)
3. Take screenshots via Playwright MCP:
   - Resize viewport: 1400x900
   - Navigate to `/` (Dashboard) — screenshot → `web_ui_dashboard.png`
   - Navigate to `/apps` (Apps) — expand multi-instance apps before capture → `web_ui_apps.png`
   - Navigate to `/sessions` (Sessions) — screenshot → `web_ui_sessions.png`
   - Navigate to `/logs` (Logs) — screenshot → `web_ui_logs.png`
4. Copy screenshots to `docs/_static/`, replacing existing files.
5. Tear down demo and restore production hassette.
6. Visually verify each screenshot shows non-empty data, correct color system, and no loading/error states.

## Test Strategy

N/A — no testable code changes. Verification is visual inspection of the 4 screenshot files.

## Review Guidance

- Screenshots must show non-zero telemetry data — a dashboard with 0 handlers/jobs contradicts the "persisted registrations" text added in WP02.
- Viewport must be 1400x900 — not the default 800x600 which looks cramped.
- Routes are at root (`/`, `/apps`, `/logs`, `/sessions`) — NOT `/ui/` (the SPA routes from root).
- Production hassette must be restored after capture — check that the demo containers are torn down.
- Screenshots must not show loading spinners, error states, or "Connected" indicator in disconnected state.

## Visual Verification

| Page | Setup | Verify |
|------|-------|--------|
| Dashboard (`/`) | Ensure at least one completed prior session exists. Set viewport to 1400x900. Wait for data to load (no spinners). | KPI strip shows non-zero handler/job counts. App health grid populated. Color system matches post-679fe4f overhaul. No loading states. |
| Apps (`/apps`) | Expand any multi-instance apps. Wait for status badges to render. | All apps visible with status badges. Multi-instance apps expanded. No truncation or overflow. |
| Sessions (`/sessions`) | Ensure at least 2 sessions visible (one completed, one running). | Session table has at least 2 rows. Status badges colored correctly. Duration column populated. |
| Logs (`/logs`) | Wait for log entries to stream in. | Log entries visible with source location. Filter controls present. No empty state. |
