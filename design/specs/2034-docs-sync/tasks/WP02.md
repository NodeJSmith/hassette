---
work_package_id: "WP02"
title: "Update existing docs pages and nav for bus, telemetry, dashboard, sessions"
lane: "planned"
depends_on: ["WP01"]
---

## Objectives & Success Criteria

Eight existing doc pages and `mkdocs.yml` are updated to reflect recent code changes. Specifically: bus `name=` parameter documented with "Registration Identity" subsection, telemetry page has Registration Persistence and Source Tier content, dashboard/sessions pages reflect persisted registrations, Web UI overview mentions mobile-responsive design, cross-references to the testing page are added in `hassette-vs-ha-yaml.md`, `advanced/index.md`, and `appdaemon-comparison.md`, and `mkdocs.yml` has the Testing section between Web UI and Advanced. `uv run mkdocs build` succeeds.

## Subtasks

1. **`mkdocs.yml`** — Add Testing nav section between Web UI and Advanced:
   ```
   - Testing:
       - Testing Your Apps: pages/testing/index.md
   ```
2. **`docs/pages/core-concepts/bus/index.md`** — Add "Registration Identity" subsection after "Rate Control" (as a new `## Registration Identity` section, not nested under Rate Control). Explain:
   - The `name=` optional parameter on all subscription methods
   - Its purpose: stable natural key for listener identity, used for disambiguation when multiple listeners have the same predicate signature
   - The duplicate-listener collision `ValueError`
   - Forward link to "Registration Persistence" in `database-telemetry.md`
3. **`docs/pages/core-concepts/database-telemetry.md`** — Add three updates:
   - New "Registration Persistence" subsection (after "Monitoring Telemetry Health"): listener/job registrations persist across sessions via upsert semantics, old registrations marked with `retired_at`. Registration history visible in web UI dashboard.
   - New "Source Tier" paragraph (can be part of "What Is Collected" or its own subsection): "Framework-internal handlers (telemetry, WebSocket, scheduler services) are recorded with `source_tier='framework'` and excluded from Dashboard KPIs. Handler and Job counts show only your app registrations."
   - Update "Degraded Mode" section: clarify that all telemetry including persisted registrations is unavailable when the database is degraded (same SQLite file).
4. **`docs/pages/web-ui/dashboard.md`** — Update KPI strip table: note that Handler and Job counts reflect persisted registrations (survive restarts). Add inline note alongside Handlers KPI card explaining framework-tier exclusion.
5. **`docs/pages/web-ui/sessions.md`** — Add note that listener/job registrations now persist across sessions; the dashboard shows historical registrations even after restarts.
6. **`docs/pages/web-ui/index.md`** — Add mobile-responsive mention to the intro paragraph (after "Hassette ships with a dashboard...").
7. **`docs/pages/advanced/index.md`** — Add testing page link to the list: `- **[Testing Your Apps](../testing/index.md)** — test your automations with AppTestHarness, event simulation, and time control.`
8. **`docs/pages/getting-started/hassette-vs-ha-yaml.md`** — Revise line 35 from "Built-in testing and debugging tools from Python's ecosystem" to reference Hassette-native test utilities (e.g., "Built-in [testing harness](../testing/index.md) — test your apps with `AppTestHarness`, event simulation, and time control").
9. **`docs/pages/appdaemon-comparison.md`** — Make "Test each app incrementally" at line 1014 a link to the testing page. Add `- **[Testing Your Apps](testing/index.md)**` to the Next Steps block at line 1035.

## Test Strategy

N/A — no testable code changes. Verification is that `uv run mkdocs build` succeeds without errors and all internal links resolve.

## Review Guidance

- The "Registration Identity" subsection must NOT be nested under "Rate Control" in bus/index.md — it's a peer section.
- The `name=` documentation must include the forward link to database-telemetry.md's Registration Persistence subsection.
- The Source Tier paragraph must explicitly state that framework handlers are excluded from Dashboard KPIs.
- The degraded mode update must clarify that persisted registrations are also unavailable (same SQLite file).
- All cross-reference links must use correct relative paths and resolve without 404.
- The `hassette-vs-ha-yaml.md` anchor text must reference Hassette-native utilities, not "Python's ecosystem."
