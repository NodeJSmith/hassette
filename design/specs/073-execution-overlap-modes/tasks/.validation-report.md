## Status: APPROVED

## Traceability Matrix
| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1 | T01 | `ExecutionMode` StrEnum exists with exactly single/restart/queued/parallel, exported alongside other enums |
| FR#2 | T02 | All four async bus methods and sync facades accept `mode=` via Options TypedDict |
| FR#2 | T05 | Bus concept page documents `mode` and four modes on registration methods, with tested snippets |
| FR#3 | T02 | App-tier without `mode` resolves to single; framework-tier to parallel |
| FR#3 | T05 | Page and docstrings document tier-aware default and that explicit `mode=` wins |
| FR#4 | T01 | Unit test: `single` guard drops second trigger while first runs (exactly one run) |
| FR#5 | T01 | `single` drop increments `suppressed` and emits DEBUG log (asserted via counter) |
| FR#5 | T05 | Page states suppressed/dropped events log at DEBUG |
| FR#6 | T01 | Unit test: `restart` cancels running invocation and starts new one |
| FR#7 | T01 | Cancelled invocation observes `CancelledError`; no exception escapes guard |
| FR#8 | T01 | Unit test: `queued` runs N triggers in arrival order, one at a time |
| FR#9 | T01 | `queued` at cap drops newest trigger, preserves existing queue |
| FR#10 | T01 | `queued` cap drop increments `dropped` and emits DEBUG log |
| FR#11 | T01 | Unit test: `parallel` runs M triggers concurrently (pass-through) |
| FR#12 | T02 | Invalid `mode` string raises clear error at registration time |
| FR#13 | T01 | `restart` A→B→C never has two running invocations concurrently |
| FR#14 | T03 | `003.sql` adds mode column; upsert writes it; summary endpoint returns persisted mode |
| FR#15 | T01 | Guard exposes live `suppressed`/`dropped` integer counts |
| FR#15 | T03 | Summary endpoint returns live counts from in-memory guards by db_id; no counter columns in DB |
| FR#15 | T05 | Page documents suppressed/dropped counts are live-only diagnostics shown in UI |
| FR#16 | T03 | `restart`-cancelled invocation appears in executions table with status='cancelled' |
| FR#17 | T01 | `release()` cancels tracked task, clears deque, pending factories do not run |
| FR#17 | T02 | Cancelling/replacing a listener calls guard `release()`; no in-flight task or queued factory leaks |
| FR#18 | T04 | Handler row and listener detail pane both display listener's `mode` |
| FR#19 | T04 | Listener detail pane shows suppressed/dropped cells only when non-zero |
| FR#20 | T02 | Handler with `debounce` + `mode="single"` debounces starts and suppresses overlap |
| FR#20 | T05 | Page documents `mode` composition with debounce/throttle |
| FR#21 | T02 | `once=True` handler fires at most once regardless of mode |
| FR#21 | T05 | Page documents `mode` composition with `once=True` |
| FR#22 | T02 | Duration-hold handler applies guard at hold-expiry dispatch, not trigger arrival |
| FR#22 | T05 | Page documents `mode` composition with duration-hold |
| AC#1 | T03 | Registering with each of four modes persists mode and it is queryable via API |
| AC#2 | T02 | Test shows app→single, framework→parallel defaults |
| AC#3 | T02 | Integration test: `single` yields exactly one execution on double-fire, with DEBUG drop |
| AC#4 | T02 | Integration test: `restart` cancels first and runs second; dispatching bucket does not error |
| AC#5 | T01 | Test asserts no two concurrent running invocations across rapid `restart` A→B→C |
| AC#6 | T02 | Integration test: `queued` runs N triggers in order |
| AC#7 | T02 | Integration test: `queued` at cap drops newest, runs rest, counts drop |
| AC#8 | T02 | Integration test: `parallel` runs M triggers concurrently |
| AC#9 | T02 | Integration test: invalid `mode` raises at registration |
| AC#10 | T03 | After drops, web API reflects suppressed/dropped counts |
| AC#11 | T03 | Integration test confirms `restart` cancellation row is status='cancelled' |
| AC#12 | T01 | Test asserts `release()` on `queued` guard with pending items retains no reference and runs none |
| AC#13 | T04 | App-detail UI renders mode chip and suppressed/dropped counts (demo stack + e2e) |
| AC#14 | T02 | Integration test: `debounce` + `mode="single"` compose correctly |
| AC#15 | T02 | Integration test: `once=True` fires at most once in any mode |
| AC#16 | T02 | Integration test: duration-hold + `mode="single"` guards at hold-expiry |

## Coverage Gaps
None.

## Contradictions
None.

## Warnings
- Identifier counts confirmed: 22 FRs (FR#1–FR#22) and 16 ACs (AC#1–AC#16), all 38 accounted for. All identifiers match the `^FR#\d+$` / `^AC#\d+$` format.
- T03's `implements` list does not include FR#17, but FR#17 (release on cancel) is fully covered by T01 (`release()` mechanism) and T02 (`remove_listener` wiring). The web/telemetry task correctly omits it. Not a gap.
- T05 (docs) lists FR identifiers in `implements` but no AC identifiers — correct, since documentation has no binary acceptance criteria of its own; its extra Verify line (`doc-persona-review` passing verdict) is a non-FR/AC quality gate.
- context.md sections 1–5 (`## Problem & Motivation`, `## Visual Artifacts`, `## Key Decisions`, `## Constraints & Anti-Patterns`, `## Design Doc References`) are all present and non-empty. `## Convention Examples` (optional) is present and populated.
- Dependency chain is linear and consistent: T01 (no deps) → T02 → {T03, T05}; T03 → T04. No cycles; every `depends_on` target exists.
