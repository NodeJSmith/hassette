## Status: APPROVED

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|---|---|---|
| FR#1 | T01 | Rendered output contains `class {Domain}EntitySyncFacade(BaseEntitySyncFacade[{Domain}State, str])` block |
| FR#1 | T04 | A docs page documents that each domain entity exposes a typed sync facade, with at least one concrete domain-action example beyond `turn_on`/`turn_off`/`toggle` |
| FR#2 | T01 | Facade defines one method per service with matching `method_name` and keyword parameter signature |
| FR#2 | T04 | Docs show a domain-specific sync action with parameters; any `run_sync` guidance for entity domain methods updated to facade |
| FR#3 | T01 | Entity class renders a `sync` property annotated `-> "{Domain}EntitySyncFacade"` that constructs and caches the domain facade |
| FR#4 | T01 | Each facade method body calls `self.entity.api.sync.call_service(...)` with no return-type annotation |
| FR#5 | T01 | Facade inherits from `BaseEntitySyncFacade` and does not redeclare `entity`/`__init__` |
| FR#6 | T02 | `src/hassette/models/entities/__init__.py` exports every `{Domain}EntitySyncFacade` (regenerated, not hand-edited) |
| FR#7 | T01 | `test_no_services_returns_none` still passes (domain with no services renders nothing) |
| AC#1 | T02 | `from hassette.models.entities import CoverEntitySyncFacade, ClimateEntitySyncFacade, LightEntitySyncFacade` succeeds |
| AC#2 | T03 | `CoverEntity(...).sync`, `ClimateEntity(...).sync`, and `LightEntity(...).sync` are instances of their respective `{Domain}EntitySyncFacade`, not `BaseEntitySyncFacade` |
| AC#3 | T03 | `CoverEntity(...).sync.open_cover()` calls `api.sync.call_service` once with `service="open_cover"` and the correct domain/target |
| AC#4 | T03 | `ClimateEntity(...).sync.set_temperature(temperature=21.0)` calls `api.sync.call_service` with `service="set_temperature"` and temperature passed through |
| AC#5 | T03 | `LightEntity(...).sync.turn_on(brightness=128)` and `.turn_off()` route through inherited base facade methods; `test_entity_sync_turn_on_registers` remains green |
| AC#6 | T02 | `cd codegen && uv run hassette-codegen generate --ha-core-path ../ha-core --check` exits 0 |
| AC#7 | T02 | `uv run pyright` exits 0 with no new errors in regenerated entity files |
| AC#8 | T01 | `cd codegen && uv run pytest tests/test_entity_generator.py -q --rootdir=.` passes, including new facade-structure assertions and `test_output_compiles` |

## Coverage Gaps

None.

## Contradictions

None.

## Warnings

1. **T04 claims FR#1 and FR#2 with documentation-layer Verify items.** FR#1 and FR#2 as written in the design doc describe generated code behavior ("each generated module emits a `{DomainTitle}EntitySyncFacade` class," "the sync facade class defines one synchronous method per domain-specific service"). T04 reinterprets these as "a docs page documents that..." — a proxy claim at a different layer. The intent is reasonable (design-completeness requires docs), but FR#1 and FR#2 are already fully covered at the code level by T01. T04's claims are additive and not harmful, but they dilute the precision of the traceability matrix. Consider whether T04 should instead reference only the documentation completeness requirement from the design's `## Documentation Updates` section rather than code-level FRs.

2. **`## Visual Artifacts` in context.md contains only "None."** The section is technically present and the content is the conventional placeholder for no visual artifacts. This satisfies the non-empty requirement — no action needed. Noted for completeness.
