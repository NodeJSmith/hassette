## Verdict: PASS

> PASS — all 14 FRs and all 14 ACs are covered by exactly one task each, no contradictions found between task Prompt/Focus sections and the design doc, no non-local verification criteria, all Verify items are concrete/binary-checkable, and context.md has all five required sections with non-empty content (Convention Examples present with code, which is valid).

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1 | T01 | "ActionButtons with `instance={{ index: 1, name: "office" }}` clicking Start calls `POST /apps/{appKey}/instances/1/start`" |
| FR#2 | T01 | "ActionButtons with `instance` clicking Stop calls instance-level stop route" |
| FR#3 | T01 | "ActionButtons with `instance` clicking Reload calls instance-level reload route" |
| FR#4 | T01 | "ActionButtons without `instance` calls app-level routes (existing behavior unchanged)" |
| FR#5 | T01 | "Stop dialog title and description both include instance name when `instance` is provided" |
| FR#6 | T01 | "`data-testid` includes instance index and `aria-label` includes instance name when `instance` present" |
| FR#7 | T01 | "Toast text includes instance name when `instance` present" |
| FR#8 | T02 | "app-detail-header passes `instance` to ActionButtons when viewing a single instance of a multi-instance app" |
| FR#9 | T02 | "apps-table-row passes `instance` to ActionButtons in instance sub-rows and `confirmStop` on both app-level and instance sub-rows" |
| FR#10 | T03 | "`hassette app start my_app` sends `POST /api/apps/my_app/start`" |
| FR#11 | T03 | "`hassette app start my_app --instance 1` sends `POST /api/apps/my_app/instances/1/start`" |
| FR#12 | T03 | "`stop` and `reload` follow the same routing pattern" |
| FR#13 | T03 | "Success message includes instance name when `--instance` provided; uses app-level text otherwise" |
| FR#14 | T03 | "`stop` and `reload` prompt for confirmation; `--yes` bypasses; `start` does not prompt" |
| AC#1 | T01 | "Component tests confirm instance-level endpoint calls with `instance` prop" |
| AC#2 | T01 | "Component tests confirm app-level endpoint calls without `instance` prop" |
| AC#3 | T01 | "Component test confirms dialog title and description include instance name" |
| AC#4 | T01 | "Component test confirms instance-scoped testid and aria-label" |
| AC#5 | T01 | "Component test confirms instance-aware toast text" |
| AC#6 | T03 | "CLI test confirms `start` without `--instance` hits app-level route" |
| AC#7 | T03 | "CLI test confirms `start` with `--instance 1` hits instance route" |
| AC#8 | T03 | "CLI tests confirm `stop` and `reload` follow same routing" |
| AC#9 | T03 | "CLI test confirms confirmation prompt and `--yes` bypass" |
| AC#10 | T03 | "CLI test confirms instance-aware success message" |
| AC#11 | T02 | "Component test confirms apps-table-row passes confirmStop on both row types" |
| AC#12 | T02 | "E2E test navigates to multi-instance app instance view and verifies action buttons fire instance-level routes" |
| AC#13 | T04 | "`prek -a` passes (lint + type check) with zero errors" |
| AC#14 | T04 | "`cd frontend && npm run build` succeeds with regenerated types" |

Total FRs in design.md: 14 (FR#1–FR#14). Total ACs in design.md: 14 (AC#1–AC#14). Every identifier appears in exactly one task's `implements` field, and each has a corresponding `- [ ]` Verify item in that task.

## Coverage Gaps

None.

## Contradictions

None. Cross-checked each task's Prompt/Focus against the design doc sections they claim to implement:

- T01 matches `## Architecture → Frontend` (paired `instance` object shape, `performAction`/`buildButtonSpecs`/`ActionButton`/`StopConfirmDialog` changes, toast/testid/aria-label wording) verbatim.
- T02 matches `## Architecture → Frontend` wiring guidance (`manifest.instance_count > 1 && !showParentOverview` guard, `confirmStop` on both row types, app-level row explicitly excluded from `instance`, matching Non-Goals) and `## Smoke Test → Frontend` for the E2E scenario.
- T03 matches `## Architecture → CLI` (client `post()` method modeled on `get()`, command signatures, confirmation/`--yes` behavior, message construction) and the `## Convention Examples` CLI pattern.
- T04 matches `## Architecture → Schema regeneration` and `## Documentation Updates` (three tables in `docs/pages/cli/commands.md`, action-appropriate `--instance` wording, `--yes` only for `stop`/`reload`).

## Non-Local Criteria

None. AC#13 (`prek -a`) and AC#14 (`npm run build`) are both locally executable commands, not CI/post-merge signals.

## Warnings

- T02's Target Files list includes `modify: frontend/src/pages/app-detail.header.test.tsx`, but this file does not appear in the design doc's `## Impact → Changed Files` list. T02's own Focus section notes "likely no changes needed unless the test starts asserting on props," so this is a soft inconsistency (task lists a file the design doesn't formally track as changed) rather than a scope violation — flagging for awareness only, does not block approval.
- No visual artifacts (mockups/screenshots) are referenced in design.md or context.md (`## Visual Artifacts` is explicitly "None."), so the Visual Artifact Coverage check does not apply.
- Design.md has no `## Operational Lifecycle` section, so the Operational Lifecycle Coverage check does not apply.
- All FR/AC identifiers in both design.md and all four task files conform to `^FR#\d+$` / `^AC#\d+$` — no format violations found.
