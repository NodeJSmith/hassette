---
work_package_id: "WP01"
title: "Core service split — RuntimeQueryService, TelemetryQueryService skeleton, dead code deletion"
lane: "done"
plan_section: "Service Split"
depends_on: []
---

## Objectives & Success Criteria

Create the two new service files, wire them into `core/core.py`, delete all dead code (BusService/SchedulerService stubs, entity route file), and add the three new DI aliases alongside the existing `DataSyncDep` (which is kept alive here and removed in WP01b once routes are migrated). Core unit tests pass.

- `src/hassette/core/data_sync_service.py` is deleted
- `src/hassette/core/runtime_query_service.py` exists; `RuntimeQueryService(Resource)` contains only the kept methods from the design doc (app status, manifests, events, logs, system status, WS management)
- `src/hassette/core/telemetry_query_service.py` exists; `TelemetryQueryService(Resource)` with all 9 stub methods returning `[]` or `None`; `on_initialize` waits on `DatabaseService` then marks ready
- `BusService.get_all_listener_metrics()` and `get_listener_metrics_by_owner()` deleted from `core/bus_service.py`
- `SchedulerService.get_execution_history()` deleted from `core/scheduler_service.py`
- `web/routes/entities.py` deleted; its `include_router` call removed from `src/hassette/web/app.py`
- `core/core.py`: `_data_sync_service` → `_runtime_query_service`; `_telemetry_query_service` added as child; properties `runtime_query_service` and `telemetry_query_service` exposed; old `data_sync_service` property removed
- `web/ui/context.py`: `alert_context()` accepts `RuntimeQueryService` instead of `DataSyncService`
- `web/dependencies.py`: `RuntimeDep`, `TelemetryDep`, `SchedulerDep` added with `get_runtime()`, `get_telemetry()`, `get_scheduler()` getter functions; `DataSyncDep` kept intact for now (removed in WP01b)
- `tests/unit/core/test_data_sync_service.py` renamed to `test_runtime_query_service.py`; tests for dropped methods removed; remaining tests updated to reference `RuntimeQueryService`
- `tests/unit/core/test_web_ui_watcher.py`: `hassette.data_sync_service` → `hassette.runtime_query_service`
- `uv run pytest tests/unit/core/ -n auto` passes
- `uv run pyright src/hassette/core/` passes
- Note: web route files still reference `DataSyncDep` — this is expected and resolved in WP01b

## Subtasks

1. Create `runtime_query_service.py`: copy `data_sync_service.py`, rename class to `RuntimeQueryService`, delete all dropped methods and their imports. Confirm `_event_buffer`, `_ws_clients`, WS event handlers, and lifecycle hooks are untouched.
2. Create `telemetry_query_service.py`: `Resource` subclass; `on_initialize` calls `await self.hassette.wait_for_ready([self.hassette.database_service])` then `self.mark_ready(...)`; all 9 method stubs with correct signatures and return types (`list[dict]` or `dict | None`), all returning `[]` or `None`.
3. Delete `BusService.get_all_listener_metrics()` and `get_listener_metrics_by_owner()` from `core/bus_service.py`. Delete any tests that cover only those stub methods.
4. Delete `SchedulerService.get_execution_history()` from `core/scheduler_service.py`. Delete any tests that cover only that stub method.
5. Delete `web/routes/entities.py`. Remove its `include_router` call from `src/hassette/web/app.py` (import on line ~15, `include_router` call on line ~51). No nav.html change needed — no entities link exists in the sidebar.
6. Update `core/core.py`: rename `_data_sync_service` → `_runtime_query_service`; expose `runtime_query_service` property; add `_telemetry_query_service = self.add_child(TelemetryQueryService)` and `telemetry_query_service` property; remove old `data_sync_service` property.
7. Update `web/ui/context.py`: change `alert_context()` signature to accept `RuntimeQueryService` instead of `DataSyncService`.
8. Update `web/dependencies.py`: add `get_runtime()`, `get_telemetry()`, `get_scheduler()` getter functions and `RuntimeDep`, `TelemetryDep`, `SchedulerDep` type aliases. Leave `DataSyncDep` in place — it will be removed in WP01b after routes are migrated.
9. Update core unit tests: rename `tests/unit/core/test_data_sync_service.py` → `test_runtime_query_service.py`; remove tests for deleted methods (entity state, listener metrics, scheduler, owner maps); update all `DataSyncService` references to `RuntimeQueryService`. Update `tests/unit/core/test_web_ui_watcher.py`: change `hassette.data_sync_service` → `hassette.runtime_query_service`.
10. Run `uv run pytest tests/unit/core/ -n auto` and `uv run pyright src/hassette/core/`; fix failures.

## Test Strategy

**Approach**: update-existing — core unit tests only in this WP.

- `tests/unit/core/test_runtime_query_service.py` (renamed): kept methods retain existing tests; deleted method tests removed
- `tests/unit/core/test_web_ui_watcher.py`: updated in-place (2 lines)
- Web layer tests (`tests/e2e/`, web route tests) are **not** run in this WP — they still depend on `DataSyncDep` and will be fixed in WP01b

## Review Guidance

Verify:
- No `DataSyncService` or `data_sync_service` references remain in `src/hassette/core/` (grep to confirm; web/ still has them — expected)
- No `get_all_listener_metrics`, `get_listener_metrics_by_owner`, `get_execution_history` remain in `src/` (grep to confirm)
- `TelemetryQueryService.on_initialize` calls `wait_for_ready([self.hassette.database_service])` before `mark_ready`
- `RuntimeDep`, `TelemetryDep`, `SchedulerDep` exist in `web/dependencies.py`; `DataSyncDep` still present (intentional)
- `core.py` exposes `runtime_query_service` and `telemetry_query_service` properties; `data_sync_service` property is gone

## Activity Log
- 2026-03-14T12:13:23Z — system — lane=doing — moved from planned
- 2026-03-14T12:31:43Z — system — lane=done — moved from doing
