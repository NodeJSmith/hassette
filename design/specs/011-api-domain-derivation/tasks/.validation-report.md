# Plan Validation Report: 011-api-domain-derivation

## Verdict: FAIL (one contradiction — trivially fixable, one-line correction)

Zero coverage gaps, zero non-local criteria, zero vague criteria, context.md complete. One contradiction found: T03 mislabels a Verify item as `AC#9` when its content actually describes `AC#11`.

**Note on prior report in this file:** An earlier version of this report (superseded by this run) flagged a critical serviceless-domain regression tied to `BaseEntity` generic methods forwarding a derived (and often nonexistent) HA service domain. That finding was against an older `design.md` (7 FRs, `FR#6`/`FR#7` covering `BaseEntity`/`BaseEntitySyncFacade` `**data` forwarding, Non-Goals stating the serviceless-domain gap was deliberately left open). The `design.md` and all three task files read for **this** run are a revised version (6 FRs) that resolves that exact issue by removing `BaseEntity`/`BaseEntitySyncFacade`'s generic `turn_on`/`turn_off`/`toggle` methods entirely (cherry-picked from #1320), so there is no longer a fallback path that could derive a domain for a serviceless entity. This was independently verified against the live source tree (see §4, item 2) — `src/hassette/models/entities/base.py` has zero `def turn_on|def turn_off|def toggle` matches today. The old finding no longer applies to the current plan.

---

## 1. Identifier Extraction

### design.md — Functional Requirements (format `^FR#\d+$`)

| ID | Format valid | Text |
|---|---|---|
| FR#1 | yes | domain derivation when `domain is None` |
| FR#2 | yes | explicit `domain` string override |
| FR#3 | yes | `toggle_service` → `toggle` rename (Api, ApiSyncFacade, RecordingApi, RecordingApiSyncFacade, BaseEntity, BaseEntitySyncFacade) |
| FR#4 | yes | `turn_off`/`toggle` accept `**data: Any` |
| FR#5 | yes | `RecordingApi.turn_off`/`toggle` capture `**data` in `ApiCall.kwargs` |
| FR#6 | yes | `BaseEntity`/`BaseEntitySyncFacade` no longer expose generic `turn_on`/`turn_off`/`toggle` |

### design.md — Acceptance Criteria (format `^AC#\d+$`)

| ID | Format valid | Text |
|---|---|---|
| AC#1 | yes | `turn_on("light.kitchen")` → domain `"light"` |
| AC#2 | yes | `turn_on("light.kitchen", domain="homeassistant")` → domain `"homeassistant"` |
| AC#3 | yes | `Api.toggle` exists; `Api.toggle_service` does not |
| AC#4 | yes | `turn_off("switch.fan", transition=2)` forwards `transition=2` |
| AC#5 | yes | `toggle("light.kitchen", transition=1)` forwards `transition=1` |
| AC#6 | yes | `RecordingApi.toggle` records method name `"toggle"` |
| AC#7 | yes | `RecordingApi.turn_off(..., brightness=0)` captures `brightness=0` |
| AC#8 | yes | `BaseEntity` has no `turn_on`/`turn_off`/`toggle` |
| AC#9 | yes | All existing tests pass after signature changes (no regressions) |
| AC#10 | yes | `prek -a` passes cleanly |
| AC#11 | yes | Doc snippets type-check via Pyright (CI-tested) |

All 6 FR and 11 AC identifiers match their required regex. No malformed IDs found.

### Task files — declared claims

| Task | `implements` (header) | Verify checklist IDs |
|---|---|---|
| T01 | FR#1–6, AC#1–8, AC#10 | FR#1–6, AC#1–8, AC#10 (exact match to header) |
| T02 | FR#1–6, AC#1–9 | FR#1–6, AC#1–9 (exact match to header) |
| T03 | FR#1, FR#3, FR#4, AC#3, AC#9, AC#10, AC#11 | FR#1, FR#3, FR#4, AC#3, AC#9, AC#10, AC#11 (exact ID match to header, but the `AC#9` Verify line's *content* is wrong — see Contradictions) |

Each task's Verify checklist is a 1:1 ID match with its own `implements` header — no task claims an ID it doesn't verify, or verifies an ID it doesn't claim.

---

## 2. Traceability Matrix

| FR/AC | T01 | T02 | T03 | Verified by (concrete check) |
|---|---|---|---|---|
| FR#1 | ✓ | ✓ | ✓ | `call_service` receives derived `domain="light"`; new unit tests; doc examples drop `domain=` |
| FR#2 | ✓ | ✓ | — | explicit `domain="homeassistant"` override test |
| FR#3 | ✓ | ✓ | ✓ | rename verified via `hasattr` (T01), grep in tests (T02), grep in docs (T03) |
| FR#4 | ✓ | ✓ | ✓ | `**data` forwarding tests; docs show `**data` param |
| FR#5 | ✓ | ✓ | — | `RecordingApi.toggle`/`turn_off` capture kwargs |
| FR#6 | ✓ | ✓ | — | read-only verification that `BaseEntity` already lacks these methods (independently confirmed live — §4) |
| AC#1 | ✓ | ✓ | — | |
| AC#2 | ✓ | ✓ | — | |
| AC#3 | ✓ | ✓ | ✓ | `hasattr` check, grep tests, grep docs |
| AC#4 | ✓ | ✓ | — | |
| AC#5 | ✓ | ✓ | — | |
| AC#6 | ✓ | ✓ | — | |
| AC#7 | ✓ | ✓ | — | |
| AC#8 | ✓ | ✓ | — | |
| AC#9 | — | ✓ | ✓ (mislabeled) | T02: `ptest` run across all 7 touched test files. T03's `AC#9` entry is mislabeled — see Contradictions. |
| AC#10 | ✓ | — | ✓ | `prek -a` run in both T01 and T03 |
| AC#11 | — | — | ✓ | snippet Pyright check via `prek -a` |

No FR or AC is left without at least one task claiming and concretely verifying it.

---

## 3. Coverage Gaps

**None.** Every FR#1–6 and AC#1–11 appears in at least one task's `implements` list with a matching, concrete Verify item.

Extra diligence performed beyond the task files' own text:
- Grepped the full repo for `toggle_service` and `domain="homeassistant"` / `"domain": "homeassistant"` occurrences outside the files named across the three tasks. Found one additional hit outside scope: `src/hassette/bus/bus.py` (lines 1192, 1230, 1268) — confirmed this is `on_call_service(domain="homeassistant", service="restart"/"start"/"stop", ...)`, i.e. HA's *system* event domain for bus listeners (`homeassistant.restart` etc. are real, unrelated events), not related to `Api.turn_on/turn_off/toggle`. Correctly out of scope; not a gap.
- Confirmed no other `domain="homeassistant"` literal or dict-style `"domain": "homeassistant"` exists in `src/` or `tests/` beyond what T01/T02 already enumerate line-by-line (verified exact line matches in `test_recording_api.py` and `test_recording_sync_facade.py`).

---

## 4. Contradictions

### C1 — T03 mislabels a Verify item as `AC#9` (should be `AC#11`, already present separately)

**File:** `tasks/T03-docs-examples.md` — header `implements` list and final Verify bullet before the closing.

Design doc definitions:
- **AC#9** — "All existing tests pass after the signature changes (no regressions)"
- **AC#11** — "Doc snippets type-check via Pyright (CI-tested)"

T03's Verify checklist reads:
> `- [ ] AC#9: All snippet files pass Pyright type checking via prek -a`

That is the definition of **AC#11**, not AC#9. T03 does no test-execution work (it only touches docs/snippets), so it has no basis to claim AC#9 ("all existing tests pass") — that claim is legitimately owned by T02, which runs `ptest` across the affected test files. T03 lists `AC#9` in its header `implements`, then attaches AC#11's description to the Verify line — a direct content mismatch against design.md's own definition of AC#9.

**Fix:** Remove `AC#9` from T03's `implements` header and delete the mislabeled Verify bullet. T03 already separately and correctly has an `AC#11` line one bullet below with the right description — the `AC#9` line is redundant in intent and wrong in identifier.

This is the only contradiction found. It causes no real-world coverage gap (AC#9 is genuinely and correctly covered by T02), but it fails the "task prompt conflicts with design doc" bar for PASS, so the verdict is FAIL until corrected.

### 2. Independent verification that FR#6/AC#8's premise holds today

Both design.md and T01/T02 assert `BaseEntity`/`BaseEntitySyncFacade` already lack `turn_on`/`turn_off`/`toggle` due to a prior cherry-picked commit (#1320), and treat this as read-only verification rather than new work. Confirmed against the live worktree:
```
$ grep -n "def turn_on\|def turn_off\|def toggle" src/hassette/models/entities/base.py
(no output — file is 90 lines, no matches)
$ git log --oneline --all | grep -i 1320
b7f829b2 chore: update design and tasks for expanded scope (#1320, entity-services-generation)
```
The design's premise is accurate; FR#6/AC#8 are correctly scoped as verification-only in T01/T02, not new implementation.

### 3. Current source state matches design's "before" picture exactly

Confirmed all four target implementations still have the pre-change signatures the design/tasks assume:
```
src/hassette/api/api.py:575:      def turn_off(self, entity_id: str | StrEnum, domain: str = "homeassistant") -> ...
src/hassette/api/api.py:593:      def toggle_service(self, entity_id: str | StrEnum, domain: str = "homeassistant") -> ...
src/hassette/api/sync.py:238,250: same pattern, `toggle_service`
src/hassette/test_utils/recording_api.py:426,437: same pattern, `toggle_service`
src/hassette/test_utils/sync_facade.py:187,194: same pattern, `toggle_service`
```
No drift between the design's stated starting point and the actual codebase.

---

## 5. Non-Local Criteria

**None found.** Every Verify item in every task references a file already listed in that same task's Target Files section (as `modify` or `read`). Spot-checked:
- T01 Verify FR#6 (BaseEntity check) → `src/hassette/models/entities/base.py` listed as a `read` target.
- T02 Verify FR#6 → `test_sync_entity_facade.py` / `test_entity_coroutine_conversion.py` listed as `read` targets.
- T03 Verify items → all reference files already in its own Target Files list.

---

## 6. Vague Criteria

**None found.** All Verify items specify concrete method calls, entity_ids, expected kwargs/values, or exact commands (`prek -a`, `ptest <files> -n 4`, `grep -r "toggle_service" ...`, `hasattr` checks). No criteria phrased as "works correctly" or similar without a concrete, checkable condition.

---

## 7. context.md Completeness

All expected sections present and substantive:
- Problem & Motivation ✓ — matches design.md's Problem section
- Visual Artifacts ✓ — "None" (correct; no UI surface in this change)
- Key Decisions ✓ — 5 numbered decisions, matches design.md Architecture/Replacement Targets
- Constraints & Anti-Patterns ✓ — 6 items, matches design.md Key Constraints/Non-Goals
- Design Doc References ✓ — 5 section pointers into design.md
- Convention Examples ✓ (bonus section) — 3 code snippets, matches design.md's own Convention Examples

No invented decisions, no dropped constraints.

---

## 8. Warnings (non-blocking)

1. **Line-number drift (~2–5 lines) in several doc citations**, most likely from edits made after the design/task line numbers were captured. Content descriptions remain accurate; several Verify items already fall back to `grep` rather than exact line numbers, so this won't mislead an implementer. Spot-checked and confirmed harmless:
   - `docs/pages/core-concepts/api/methods.md`: T03 cites "line 195-196" for the warning admonition (actual: ~197-198); "line 215" for the `turn_on` heading (actual: 217); "line 230-233" for the deprecation warning (actual: ~232-235); "line 235"/"248" for `turn_off`/`toggle_service` headings (actual: ~237/250).
   - `docs/pages/testing/harness.md`: T03 cites "Line 188 and 201" for `toggle_service` references — line 188 in the current file does **not** contain `toggle_service` (it lists `turn_on, turn_off, call_service, set_state, fire_event`); only line 201 has the actual reference. Harmless: FR#3's verify criterion (`grep -r "toggle_service" docs/` returns nothing) is still satisfied once line 201 is fixed.

2. All other line references checked across `tests/unit/test_recording_api.py`, `tests/unit/test_recording_sync_facade.py`, `tests/unit/test_api_coroutine_conversion.py`, `tests/unit/test_forgotten_await_completeness.py`, `tests/pyright_probes/forgotten_await_probe.py`, `docs/pages/getting-started/snippets/first_automation_step3.py`/`step4.py`, `docs/pages/getting-started/first-automation.md`, `docs/pages/troubleshooting.md`, and the read-only files (`examples/climate_controller.py`, `examples/motion_lights.py`, migration snippet) matched exactly.

---

## Summary

- **Coverage gaps:** 0
- **Contradictions:** 1 (T03's `AC#9` Verify item is mislabeled — content belongs to `AC#11`)
- **Non-local criteria:** 0
- **Vague criteria:** 0
- **context.md:** complete

**Recommendation:** Fix C1 (drop `AC#9` from T03's `implements` header and delete the mislabeled Verify bullet — a one-line and one-bullet edit) and this plan is ready to execute. No other changes needed.
