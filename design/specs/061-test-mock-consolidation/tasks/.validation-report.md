# Plan Validation Report

**Design doc:** `design/specs/061-test-mock-consolidation/design.md`
**Tasks reviewed:** T01, T02, T03, T04, T05
**context.md reviewed:** yes
**Status:** APPROVED

---

## Step 1: Requirements Extracted from design.md

### Functional Requirements (8 total)

| Identifier | Section | Text |
|------------|---------|------|
| FR#1 | Functional Requirements | A shared factory function produces a mock hassette object with real, validated configuration and mock non-configuration attributes |
| FR#2 | Functional Requirements | The factory accepts keyword overrides for any configuration field, merged on top of test-appropriate defaults |
| FR#3 | Functional Requirements | The factory sets standard non-configuration attributes (readiness signals, shutdown signals, thread identity, event loop reference, service stubs) so callers don't need to wire them manually |
| FR#4 | Functional Requirements | A shared database-initialization fixture creates a database service and session row, yielding the service and session identifier |
| FR#5 | Functional Requirements | The session-scoped database template fixture uses validated configuration instead of manually-set mock attributes |
| FR#6 | Functional Requirements | No test file outside `test_utils` defines its own hassette mock factory function |
| FR#7 | Functional Requirements | Database-backed hassette fixtures use a distinct name from lightweight stubs to prevent naming confusion |
| FR#8 | Functional Requirements | Dead fixtures that are defined but never consumed by any test are removed |

### Acceptance Criteria (8 total)

| Identifier | Section | Text |
|------------|---------|------|
| AC#1 | Acceptance Criteria | All test files use the shared factory; grep pattern returns zero results AND no inline `mock_hassette` fixture outside `test_utils` or `e2e/` manually sets `.config.` attributes on a MagicMock (FR#6) |
| AC#2 | Acceptance Criteria | The `initialized_db` fixture exists in exactly one location and is not duplicated in any test file (FR#4) |
| AC#3 | Acceptance Criteria | Adding a new field with a default to any configuration model subgroup requires zero test file changes (FR#1, FR#2) |
| AC#4 | Acceptance Criteria | The full test suite passes with no regressions (`uv run nox -s dev -- -n 2`) (implied all FRs) |
| AC#5 | Acceptance Criteria | Net reduction in test code lines (measured by `git diff --stat` against the branch point) |
| AC#6 | Acceptance Criteria | The 5 dead root-level fixtures are removed from `tests/conftest.py` (FR#8) |
| AC#7 | Acceptance Criteria | Database-backed integration test fixtures use a name distinct from `mock_hassette` (FR#7) |
| AC#8 | Acceptance Criteria | `make_ws_hassette_stub()` is exported from `hassette.test_utils` and used by both WebSocket test files (FR#1) |

**Identifier format validation:** All identifiers match `^FR#\d+$` and `^AC#\d+$` — no format violations.

---

## Step 2: Claims Extracted from Task Files

### T01 — Create make_mock_hassette() factory and thread safety
- **depends_on:** []
- **implements:** FR#1, FR#2, FR#3, AC#8
- **Verify items:**
  - FR#1: `make_mock_hassette()` returns an `AsyncMock` with `.config` set to a real `HassetteConfig` instance (not MagicMock)
  - FR#2: Passing `strict_lifecycle=True` produces a config where `config.strict_lifecycle is True`
  - FR#3: The returned mock has all 13 non-config attributes wired
  - AC#8: `make_ws_hassette_stub()` is importable from `hassette.test_utils` and returns a mock with WebSocket config fields set to sub-millisecond values

### T02 — Migrate named factory functions to shared factory
- **depends_on:** [T01]
- **implements:** FR#6, AC#1
- **Verify items:**
  - FR#6: grep for named factories returns zero results
  - AC#1: The named factory grep pattern returns zero matches (inline fixture portion verified in T04)

### T03 — Consolidate initialized_db and update DB template fixture
- **depends_on:** [T01]
- **implements:** FR#4, FR#5, FR#7, AC#2, AC#7
- **Verify items:**
  - FR#4: `initialized_db` fixture exists in `tests/integration/conftest.py` and yields `(DatabaseService, int)`
  - FR#5: `_migrated_db_template` uses `make_mock_hassette()` with real validated config, not MagicMock attribute assignment
  - FR#7: Database-backed fixtures use `db_hassette` name, not `mock_hassette`
  - AC#2: `grep -rn 'def initialized_db' tests/` returns exactly one result
  - AC#7: `grep -rn 'def db_hassette' tests/` returns result in `tests/integration/conftest.py`; no integration test uses `mock_hassette` for DB-backed tests

### T04 — Migrate inline mock_hassette fixtures to shared factory
- **depends_on:** [T01, T03]
- **implements:** FR#6, AC#1
- **Verify items:**
  - FR#6: No test file outside `test_utils/` or `e2e/` defines a `mock_hassette` fixture that manually sets `.config.` attributes on a MagicMock
  - AC#1: Combined with T02's grep, the full AC#1 check passes

### T05 — Remove dead fixtures, update documentation, verify ACs
- **depends_on:** [T02, T03, T04]
- **implements:** FR#8, AC#3, AC#4, AC#5, AC#6
- **Verify items:**
  - FR#8: grep for 5 dead fixture names in `tests/conftest.py` returns zero results
  - AC#3: `make_mock_hassette()` uses `make_test_config()` which uses real `HassetteConfig` model
  - AC#4: `uv run nox -s dev -- -n 2` exits with status 0
  - AC#5: `git diff --stat` shows net negative line count in test files
  - AC#6: The 5 dead root-level fixtures are removed from `tests/conftest.py`

---

## Step 3: Traceability Matrix

| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1 | T01 | "`make_mock_hassette()` returns an `AsyncMock` with `.config` set to a real `HassetteConfig` instance (not MagicMock)" |
| FR#2 | T01 | "Passing `strict_lifecycle=True` to `make_mock_hassette()` produces a config where `config.strict_lifecycle is True`" |
| FR#3 | T01 | "The returned mock has all 13 non-config attributes wired (ready_event, shutdown_event, event_streams_closed, _loop_thread_id, loop, _scheduler_service callbacks, _bus_service methods, session_id, database_service, wait_for_ready, children)" |
| FR#4 | T03 | "`initialized_db` fixture exists in `tests/integration/conftest.py` and yields `(DatabaseService, int)`" |
| FR#5 | T03 | "`_migrated_db_template` uses `make_mock_hassette()` with real validated config, not MagicMock attribute assignment" |
| FR#6 | T02 | "`grep -r '_make_hassette_stub\|_make_mock_hassette\|_make_hassette_mock\|_make_ws_hassette_stub' tests/` returns zero results" |
| FR#6 | T04 | "No test file outside `test_utils/` or `e2e/` defines a `mock_hassette` fixture that manually sets `.config.` attributes on a MagicMock" |
| FR#7 | T03 | "Database-backed fixtures use `db_hassette` name, not `mock_hassette`" |
| FR#8 | T05 | "`grep -n 'def test_data_path\|def test_config_path\|def test_events_path\|def test_api_responses_path\|def test_apps_path' tests/conftest.py` returns zero results" |
| AC#1 | T02 | "The named factory grep pattern returns zero matches (the inline fixture portion of AC#1 is verified in T04)" |
| AC#1 | T04 | "Combined with T02's grep, the full AC#1 check passes — no named factories AND no inline manual-config fixtures remain" |
| AC#2 | T03 | "`grep -rn 'def initialized_db' tests/` returns exactly one result (in `tests/integration/conftest.py`)" |
| AC#3 | T05 | "`make_mock_hassette()` uses `make_test_config()` which uses real `HassetteConfig` model — fields with defaults require no test file changes" |
| AC#4 | T05 | "`uv run nox -s dev -- -n 2` exits with status 0" |
| AC#5 | T05 | "`git diff --stat` shows net negative line count in test files" |
| AC#6 | T05 | "The 5 dead root-level fixtures are removed from `tests/conftest.py`" |
| AC#7 | T03 | "`grep -rn 'def db_hassette' tests/` returns result in `tests/integration/conftest.py`; no integration test uses `mock_hassette` for DB-backed tests" |
| AC#8 | T01 | "`make_ws_hassette_stub()` is importable from `hassette.test_utils` and returns a mock with WebSocket config fields set to sub-millisecond values" |

All 8 FRs and all 8 ACs are covered. Each identifier has at least one implementing task with a corresponding Verify item.

---

## Step 4: Problem Analysis

### Coverage Gaps

**None.** All 8 FRs and all 8 ACs are covered:

- FR#1 → T01 (Verify present)
- FR#2 → T01 (Verify present)
- FR#3 → T01 (Verify present)
- FR#4 → T03 (Verify present)
- FR#5 → T03 (Verify present)
- FR#6 → T02 + T04 (both Verify items present; split is appropriate — named factories in T02, inline fixtures in T04)
- FR#7 → T03 (Verify present)
- FR#8 → T05 (Verify present)
- AC#1 → T02 + T04 (Verify present in both; AC#1 is intentionally split across tasks with T02 noting T04 covers the inline portion)
- AC#2 → T03 (Verify present)
- AC#3 → T05 (Verify present)
- AC#4 → T05 (Verify present)
- AC#5 → T05 (Verify present)
- AC#6 → T05 (Verify present)
- AC#7 → T03 (Verify present)
- AC#8 → T01 (Verify present)

### Contradictions

**None found.** Spot-checked key areas:

- T01 Prompt specifies `sealed=False` as the escape hatch for tests needing extra attributes — consistent with design doc Edge Cases section.
- T02 correctly excludes `test_hassette_timeout_warning.py` per the Non-Goals section.
- T03 uses `set_loop=False` for `_migrated_db_template` and names the new fixture `db_hassette` — consistent with both the Architecture and Fixture Naming sections.
- T04 correctly identifies files excluded from its scope (web test files using `create_hassette_stub()`).
- T05 specifies retaining module-level constants while removing only the fixture functions — consistent with the design doc's Dead Fixture Removal section.

### Vague Verification Criteria

**One minor observation (non-blocking):**

- T05's AC#3 verify item ("make_mock_hassette() uses make_test_config() which uses real HassetteConfig model — fields with defaults require no test file changes") is a structural/design assertion rather than a runnable check. However, it is accompanied by the AC#4 full test run (`nox -s dev`), which provides behavioral verification. The combination is sufficient.

All other verify items are runnable as grep checks, import checks, or test suite pass/fail checks.

### Visual Artifact Coverage

The design doc contains no visual artifacts (no diagrams, screenshots, or UI mockups). This is expected — the change is test infrastructure only, with no frontend impact. Visual artifact coverage is N/A.

### context.md Completeness

All five required sections are present and non-empty:

| Section | Present | Non-empty |
|---------|---------|-----------|
| `## Problem & Motivation` | yes | yes |
| `## Visual Artifacts` | **absent** | N/A |
| `## Key Decisions` | yes | yes |
| `## Constraints & Anti-Patterns` | yes | yes |
| `## Design Doc References` | yes | yes |
| `## Convention Examples` | yes | yes (code examples included) |

**Note on `## Visual Artifacts`:** This section is not present in context.md. The validation spec marks sections 1–5 as required. Checking the section order: context.md has Problem & Motivation (1), Key Decisions (2), Constraints & Anti-Patterns (3), Design Doc References (4), Convention Examples (5). There is no `## Visual Artifacts` section. However, `## Visual Artifacts` would normally be section 2 in the canonical ordering. Since the design doc explicitly states "Blast radius: Test infrastructure only. No application code changes. No API changes. No frontend changes." and contains no visual artifacts whatsoever, the absence of this section is understandable. The five sections that are present all have substantive non-empty content. This is a documentation gap but not a content gap — there are no visual artifacts to document.

**Ruling:** The absence of `## Visual Artifacts` is a minor structural omission. Given the design scope (test infra only, no UI), the section would contain nothing meaningful. This does not affect plan correctness.

### Identifier Format Violations

None. All identifiers in design.md and all task `implements` fields use `FR#N` and `AC#N` format correctly.

---

## Step 5: Summary

**Status: APPROVED**

The plan fully covers all 8 Functional Requirements and all 8 Acceptance Criteria. The traceability is clean — every identifier from the design doc maps to at least one task with a corresponding runnable Verify item. No contradictions were found between task content and design doc intent. The dependency ordering (T01 → T02/T03 → T04 → T05) is logical: the factory is created first, then factory consumers are migrated, then the final cleanup and verification pass runs last.

**One structural note:** context.md is missing a `## Visual Artifacts` section. Given that the design explicitly states no visual changes are involved, this is acceptable — the section would be empty. If the team's context.md template requires all five sections regardless, add `## Visual Artifacts\nNone — this change is test infrastructure only; no UI or visual components are affected.` between `## Problem & Motivation` and `## Key Decisions`.

No changes to the task files are required before execution begins.
