# Plan Validation Report: WS Log Notify-and-Fetch

**Verdict: PASS**

## Step 1: Requirements Extraction

### Functional Requirements (10 total, all format-valid `^FR#\d+$`)

| ID | Text (summary) | Section |
|---|---|---|
| FR#1 | `log_hint` WS message: only `type`+`timestamp`, gated on `subscribe_logs`, bypasses `min_log_level` | Functional Requirements |
| FR#2 | `CorrelationFilter`/`_extract_correlation_attrs` stop stamping/extracting `execution_kind`, `listener_id`, `job_id` | Functional Requirements |
| FR#3 | `LogEntry` dataclass drops the three linking fields | Functional Requirements |
| FR#4 | New `GET /logs/since/{since_id}` returns `id > since_id`, ordered `id ASC`, separate from `/logs/recent` | Functional Requirements |
| FR#5 | REST `get_log_records` remains SoT for full enriched shape | Functional Requirements |
| FR#6 | Frontend debounces + fetches enriched batch on `log_hint` using `lastSeenId`, preserving filter set | Functional Requirements |
| FR#7 | Bursty `log_hint`s within debounce window coalesce to one fetch | Functional Requirements |
| FR#8 | On WS reconnect, frontend fetches from `lastSeenId` to fill gap | Functional Requirements |
| FR#9 | `LogEntryResponse` includes `id` field | Functional Requirements |
| FR#10 | `get_log_records_since` orders `lr.id ASC`, separate function from `get_log_records` | Functional Requirements |

### Acceptance Criteria (10 total, all format-valid `^AC#\d+$`)

| ID | Text (summary) | Maps to FR |
|---|---|---|
| AC#1 | `LogWsMessage` → `LogHintWsMessage` (type+timestamp only) | FR#1 |
| AC#2 | `_RECORD_FIELDS` excludes linking fields | FR#2, FR#3 |
| AC#3 | `/logs/since/{N}` filters correctly with full linking metadata; `/logs/recent` unchanged | FR#4, FR#5 |
| AC#4 | Frontend log table shows execution linking metadata sourced from REST | FR#6 |
| AC#5 | Burst of 10+ hints within 100ms → 1 REST fetch | FR#7 |
| AC#6 | Post-reconnect entries appear automatically; cursor survives restarts | FR#8 |
| AC#7 | Regenerated schemas reflect `LogHintWsMessage` and REST model changes | — |
| AC#8 | All backend/frontend tests pass | — |
| AC#9 | Cursor regression detection + one-time reset notice | FR#8 |
| AC#10 | Catch-up loop bounded: max 5 full-page fetches, ≥100ms spacing, backoff on non-2xx | FR#10 |

No identifier format violations found.

## Step 2: Task File Claims

All 6 task files (`T01`–`T06`) have well-formed frontmatter (`task_id`, `title`, `depends_on`, `implements`). Dependency graph:

```
T01 (no deps)
 ├─ T02 (depends_on: T01)
 ├─ T03 (depends_on: T01)
 └─ T04 (depends_on: T01)
       └─ T05 (depends_on: T03, T04)
             └─ T06 (depends_on: T01, T02, T03, T04, T05)
```

Sequencing is sound: model changes (T01) gate everything; T05 correctly waits on both the REST endpoint (T03) and the WS hint plumbing (T04); T06 (schema regen + final test pass) correctly waits on all prior tasks.

`context.md` has all 5 required sections (`Problem & Motivation`, `Visual Artifacts`, `Key Decisions`, `Constraints & Anti-Patterns`, `Design Doc References`) plus the optional `Convention Examples` section. Complete.

## Step 3: Traceability Matrix

| Req | Implementing Task(s) | Verify Present? |
|---|---|---|
| FR#1 | T01 | Yes |
| FR#2 | T02 | Yes |
| FR#3 | T02 | Yes |
| FR#4 | T03 | Yes |
| FR#5 | T03 | Yes |
| FR#6 | T04 (partial: hint receipt), T05 (fetch/merge/filters) | Yes (both) |
| FR#7 | T04 (partial: counter increments), T05 (debounce/coalesce) | Yes (both) |
| FR#8 | T05 | Yes |
| FR#9 | T01 | Yes |
| FR#10 | T03 | Yes |
| AC#1 | T01 | Yes |
| AC#2 | T02 | Yes |
| AC#3 | T03 | Yes |
| AC#4 | T05 | Yes |
| AC#5 | T05 | Yes |
| AC#6 | T05 | Yes |
| AC#7 | T01 (partial: schema regen right after model change), T06 (final regen + freshness check) | Yes (both) |
| AC#8 | T06 | Yes |
| AC#9 | T05 | Yes |
| AC#10 | T05 | Yes |

**Coverage: 10/10 FRs covered, 10/10 ACs covered. Zero gaps.**

FR#6/FR#7 and AC#7 are each claimed by two tasks. In every case the task files themselves acknowledge the split explicitly (T04's Verify notes: "coalescing happens downstream in T05's debounce, not here"; T01 does the schema regen as its own closing step while T06 re-runs it as the final consistency + freshness gate after all other tasks land). This is a legitimate staged-implementation split, not duplicate/conflicting ownership — flagged as a note, not a defect.

## Step 4: Problems

### Coverage Gaps
None. All FR#1–10 and AC#1–10 map to at least one task with a corresponding Verify checklist item.

### Contradictions
None found. Spot-checked T01's file/line references against the actual source:
- `src/hassette/web/models.py:183` → `class LogEntryResponse(BaseModel):` ✓ matches
- `src/hassette/web/models.py:266` → `class LogWsMessage(BaseModel):` ✓ matches (task says "266-269")
- `src/hassette/web/models.py:321` → `WsServerMessage = Annotated[` ✓ matches
- `src/hassette/web/routes/ws.py:70-77` → `_send_from_queue`'s `if message.get("type") == "log":` filter block ✓ matches, including the `min_log_level` sub-check structure the task describes

Design doc's Non-Goals section, Key Constraints, and Behavioral Invariants are all consistent with the task Focus sections (e.g., T02 correctly preserves `seq` stamping and DB-persisted correlation fields; T03 correctly treats `get_log_records_since` as a wholly separate function rather than an overload, matching design's explicit "not a mode switch" language).

### Vague Verification Criteria
None. All Verify items are either grep-able facts (field presence/absence, ordering clause, gating logic) or command-based (schema regen, test suite run) with explicit pass conditions.

### Non-Local Verification Criteria
None blocking. T06's AC#8 item ("full backend/frontend test suite passes") is a whole-repo check, but it's explicit about which commands to run (`uv run nox -s dev`, `cd frontend && npm run test`) and is scoped correctly to the final consistency-gate task (T06), not to an earlier task where it would be premature.

### Visual Artifact Coverage
Design doc has no UI screenshots/mockups; `context.md`'s `## Visual Artifacts` section correctly states "None." Consistent.

### context.md Completeness
Complete — all 5 required sections present, plus optional `Convention Examples`. Content accurately reflects design.md (verified against `## Architecture`, `## Edge Cases`, `## Key Constraints`, `## Replacement Targets`, `## Test Strategy` sections).

### Identifier Format Violations
None — every FR/AC identifier in both design.md and the task files matches `^FR#\d+$` / `^AC#\d+$`.

## Verdict

**PASS** — zero coverage gaps, zero contradictions, zero non-local verification criteria, context.md complete. The two areas of dual-task claims (FR#6/FR#7 split across T04/T05, AC#7 split across T01/T06) are explicitly self-documented staging decisions within the task files themselves, not unacknowledged overlap, and do not block.
