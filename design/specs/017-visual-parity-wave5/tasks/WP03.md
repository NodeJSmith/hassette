---
work_package_id: "WP03"
title: "Fix instance metadata PID display to use owner_id"
lane: "done"
plan_section: "Fix 3: Instance metadata PID display (GAP-AD2)"
depends_on: []
---

## Objectives & Success Criteria

- `AppInstance` TypeScript interface includes `owner_id: string | null`
- App detail page shows `"Instance N · PID <owner_id>"` instead of `"Instance N · PID <instance_name>"`
- PID portion is omitted when `owner_id` is null
- Existing tests pass; new test cases cover PID display

## Subtasks

1. Read `frontend/src/api/endpoints.ts` and `frontend/src/pages/app-detail.tsx` to confirm current state
2. In `frontend/src/api/endpoints.ts`:
   - Add `owner_id: string | null;` to the `AppInstance` interface (after `error_traceback` field, line 29)
3. Write test (in existing test file or create `frontend/src/pages/app-detail.test.tsx` if needed — check existing test infrastructure):
   - `test_instance_meta_shows_owner_id_as_pid` — render with `currentInstance.owner_id="MyApp.office_button.0"`, assert text "PID MyApp.office_button.0" present
   - `test_instance_meta_omits_pid_when_owner_id_null` — render with `owner_id=null`, assert no "PID" text
4. In `frontend/src/pages/app-detail.tsx`:
   - Change line 71 from `{currentInstance?.instance_name && <> &middot; PID {currentInstance.instance_name}</>}` to `{currentInstance?.owner_id && <> &middot; PID {currentInstance.owner_id}</>}`
5. Update the `createInstance` helper function in `frontend/src/components/apps/manifest-list.test.tsx` (lines 33–44) that constructs `AppInstance` objects — add `owner_id: null` (or a test value like `"TestApp.test.0"`) to the default return value to satisfy the new field. Also update any other test helpers or inline `AppInstance` objects found via grep
6. Run tests to verify all pass

## Test Strategy

**File:** Depends on existing test infrastructure — if `app-detail.test.tsx` doesn't exist, test via the manifest-list test fixtures that already construct `AppInstance` objects. The type addition in `endpoints.ts` will cause build errors in any test that constructs `AppInstance` without `owner_id` — fix those first.

TDD: write/update tests first, then implement the display change.

## Review Guidance

- Verify `owner_id` is used for the PID value, NOT `instance_name`
- Verify graceful null handling — no "PID null" or "PID undefined" in the UI
- Verify the `AppInstance` type change doesn't break any other component that constructs or consumes this interface (check `manifest-list.test.tsx`, `manifest-row.tsx`)
- Verify no backend changes were made — `owner_id` is already in the wire format

## Visual Verification

| Page | Setup | Verify |
|------|-------|--------|
| App detail page for a running app | Navigate to any running app's detail page | Instance metadata line shows "Instance 0 · PID <actual_owner_id>" with a real identifier, not a class name |

## Activity Log

- 2026-03-24T17:30:00Z — system — lane=planned — WP created
- 2026-03-24T23:04:42Z — system — lane=doing — moved from planned
- 2026-03-24T23:15:07Z — system — lane=done — moved from doing
