---
work_package_id: "WP04"
title: "Add multi-column sort with live-streaming pause to log table"
lane: "done"
plan_section: "Fix 4: Multi-column sort on log table (GAP-AD11)"
depends_on: []
---

## Objectives & Success Criteria

- Level, App, and Message columns in the log table are sortable (in addition to existing Timestamp sort)
- Sort comparator is extracted to a named `sortEntries()` function at the top of the file
- `aria-sort` attribute appears only on the active sort column
- Each new sort button has a `data-testid` attribute (`sort-level`, `sort-app`, `sort-message`)
- When a non-timestamp sort is active, WebSocket log entries stop accumulating and a "Live updates paused" indicator appears
- Live streaming resumes when the user switches back to timestamp sort or clicks "Resume"
- Default sort remains Timestamp descending
- Existing `log-table.test.tsx` tests pass; new tests cover multi-column sort and pause behavior

## Subtasks

1. Read `frontend/src/components/shared/log-table.tsx` and `frontend/src/components/shared/log-table.test.tsx` to confirm current state
2. Write tests in `frontend/src/components/shared/log-table.test.tsx`:
   - `test_sort_by_level` — click Level sort button, verify entries are ordered by severity index
   - `test_sort_by_app` — click App sort button, verify entries are ordered alphabetically by app_key
   - `test_sort_by_message` — click Message sort button, verify entries are ordered alphabetically
   - `test_sort_toggle_direction` — click same column twice, verify order reverses
   - `test_aria_sort_only_on_active_column` — after clicking Level sort, verify `aria-sort` is on Level `<th>` only, not on Timestamp
   - `test_live_paused_indicator_on_non_timestamp_sort` — click Level sort, verify "Live updates paused" text appears
   - `test_live_resumes_on_timestamp_sort` — click Level sort then Timestamp sort, verify paused indicator disappears
3. Extract `sortEntries(entries: LogEntry[], column: SortColumn, asc: boolean): LogEntry[]` function above the `LogTable` component:
   - Define `type SortColumn = "timestamp" | "level" | "app" | "message"`
   - Define `const LEVEL_INDEX: Record<string, number>` mapping each level to its severity index
   - Implement the comparator: Level uses `LEVEL_INDEX`, Timestamp uses numeric subtraction, App and Message use `localeCompare`
4. Replace `sortAsc` signal with `sortConfig` signal: `useRef(signal<{ column: SortColumn; asc: boolean }>({ column: "timestamp", asc: false })).current`
5. Update the sort pipeline (line 76–78) to call `sortEntries(filtered, sortConfig.value.column, sortConfig.value.asc)`
6. Add `<button class="ht-sortable">` wrappers to Level, App, and Message `<th>` elements:
   - Each button gets `data-testid="sort-{column}"` and an `onClick` handler that updates `sortConfig`
   - Clicking the same column toggles `asc`; clicking a different column sets that column with `asc: false`
   - The App sort button must be conditionally rendered only when `showAppColumn` is true (matching the existing `{showAppColumn && <th>}` pattern). When `showAppColumn={false}` (app-scoped logs), the App sort button should not appear
7. Update `aria-sort` to appear only when `sortConfig.value.column` matches the column
8. Add live-streaming pause:
   - Derive `const livePaused = sortConfig.value.column !== "timestamp"`
   - Gate the WS entry inclusion: when `livePaused`, skip the `wsEntries` merge (show only `initialEntries`). Note: WS entries still accumulate in the ring buffer (`createLogStore` has capacity 1000) — they are not lost. When the user resumes (switching back to timestamp sort), `wsEntries` filtering runs again on the full ring buffer, so entries received during pause will appear. The only loss scenario is if >1000 entries arrive during pause (ring buffer overflow), which is acceptable for typical monitoring use.
   - Show a small indicator in the controls area: `{livePaused && <span class="ht-text-xs ht-text-warning">Live updates paused</span>}` with a "Resume" button that resets sort to timestamp desc
9. Run tests to verify all pass

## Test Strategy

**File:** `frontend/src/components/shared/log-table.test.tsx`

TDD: write tests first. The `sortEntries` function can be tested directly (export it). Component tests verify the UI integration: clicking headers, `aria-sort` placement, and the pause indicator. Existing sort tests for Timestamp should still pass with no changes.

## Review Guidance

- Verify `sortEntries` is a named, exported function — not inline in the render body
- Verify `aria-sort` appears on exactly one `<th>` at a time (the active sort column)
- Verify the live-pause gate: when `livePaused` is true, `wsEntries` are excluded from `allEntries`, not just hidden
- Verify "Resume" button resets to `{ column: "timestamp", asc: false }` — the default
- Verify no new dependencies were added
- Design constraint: `localeCompare` is used for App and Message sort. If performance is a concern at 500+ entries, verify the `sorted.slice(0, 500)` render cap still applies after sorting

## Visual Verification

| Page | Setup | Verify |
|------|-------|--------|
| Global logs page | With multiple log entries at different levels | Click Level header — entries sort by severity. Click again — reverses. Arrow indicator shows on Level column only |
| Global logs page | With entries from multiple apps | Click App header — entries sort alphabetically by app key |
| Global logs page | Click Level sort while logs are streaming | "Live updates paused" indicator appears. New log entries stop appearing in the table. Click "Resume" — table returns to timestamp sort, new entries flow in |
| App detail logs section | Same page, app-scoped logs | Sort buttons work identically. App column not shown (as expected for app-scoped logs) |

## Activity Log

- 2026-03-24T17:30:00Z — system — lane=planned — WP created
- 2026-03-24T23:04:42Z — system — lane=doing — moved from planned
- 2026-03-24T23:15:07Z — system — lane=done — moved from doing
