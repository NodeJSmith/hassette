---
depends_on:
- WP01
- WP02
- WP04
lane: done
title: Doc updates, inline docstring updates, file follow-up issue for code generation
work_package_id: WP05
---

## Objectives & Success Criteria

After this WP:

- `docs/pages/testing/index.md` accurately reflects the new behavior of `RecordingApi`, `_RecordingSyncFacade`, and the iterative drain:
  - Quick Start uses `assert_called("turn_on", ...)` directly (no `call_service` intermediate).
  - The "`turn_on`/`turn_off`/`toggle_service` are recorded as `call_service`" warning admonition is removed.
  - The `api.sync` warning is replaced with a note that `api.sync` is now a recording facade; sync calls appear in `api_recorder.calls` and can be asserted against using the same method names.
  - The `task_bucket` warning is replaced with a note that the drain now handles arbitrary-depth task chains and surfaces handler exceptions via `DrainError`. Users importing `DrainError` from `hassette.test_utils` can catch drain-time handler crashes.
- Inline docstrings are updated:
  - `src/hassette/test_utils/recording_api.py` module docstring example uses the new assertion shape.
  - `RecordingApi` class docstring example uses the new shape.
  - `src/hassette/test_utils/app_harness.py` module docstring example uses the new shape.
- `uv run mkdocs build --strict` succeeds.
- A GitHub issue is filed documenting the deferred `_RecordingSyncFacade` code-generation work.

## Subtasks

1. **Quick Start update** — in `docs/pages/testing/index.md` Quick Start section, change the `assert_called` call from `assert_called("call_service", service="turn_on", target={"entity_id": "light.hallway"}, domain="light", ...)` to the new direct form. Match the shape in the design's canonical table: `assert_called("turn_on", entity_id="light.hallway", domain="light")`.

2. **Remove turn_on delegation warning** — delete the `!!! warning "turn_on, turn_off, and toggle_service are recorded as call_service"` admonition and its code example.

3. **Update "Asserting API Calls" section note** — the note about `turn_on`/`turn_off`/`toggle_service` delegation should be removed; replace with a note that these methods now record under their own names. Keep the partial-matching semantics note.

4. **Update api.sync warning** — replace the "`api.sync` is a `Mock()` instance" warning admonition with a `!!! note "api.sync is a recording facade"` block explaining that sync API calls appear in the same `api_recorder.calls` list and are asserted the same way as async calls.

5. **Update task_bucket warning** — replace the "Fire-and-forget handler work is not tracked" warning with a note reflecting the new behavior: drain now handles arbitrary-depth task chains; handler exceptions surface via `DrainError`. Include a brief snippet showing how to catch `DrainError`:
   ```python
   from hassette.test_utils import AppTestHarness, DrainError

   try:
       await harness.simulate_state_change(...)
   except DrainError as e:
       # Handler task raised e.task_exceptions[0][1]
       raise
   ```

6. **Read-only check: Configuration Errors section** — read the `AppConfigurationError` section and confirm it still matches current behavior. **Do not edit unless you find an actual staleness bug**; this is a verification pass, not a content update.

7. **Inline docstring update: `recording_api.py`** — find the module docstring example (top of file, around line 13) and update `assert_called("call_service", ...)` patterns to the new shape.

8. **Inline docstring update: `RecordingApi` class** — find the class docstring example (around line 111) and update similarly.

9. **Inline docstring update: `app_harness.py`** — find the module docstring example (around line 13) and update.

10. **File follow-up issue** — use `gh-issue create` (or `gh issue create` as fallback) to file a GitHub issue. Title: `Extend generate_sync_facade.py to generate _RecordingSyncFacade`. Body content:
    - Context: revision 2 of spec 2035 proposed AST body-copy generation to prevent drift between `RecordingApi` and `_RecordingSyncFacade`.
    - Why deferred: the existing generator only produces signature-level wrappers; body-copy + `self`→`self._parent` AST rewriting is substantial new infrastructure. Two rounds of challenge review surfaced correctness concerns (allowlist vs full rewrite, `await` invariant, method exclusions) that made the scope larger than expected.
    - Interim safety net: `tests/unit/test_recording_sync_facade_drift.py` verifies that `_RecordingSyncFacade`'s public write-method name set covers `ApiSyncFacade`'s. This catches missing methods but not body divergence.
    - Scope of the deferred work: extend `tools/generate_sync_facade.py` to parse `RecordingApi` via AST, emit a `_RecordingSyncFacade` class with matching methods whose bodies are rewritten from `self.X` → `self._parent.X`, and fail loudly if any body contains `await`.
    - Required preparation: refactor `RecordingApi.get_entity_or_none` / `get_state_or_none` to call `_get_raw_state`/`_convert_state` directly, eliminating their internal `await` calls.
    - Labels: `enhancement`, `core`, `dx`, `size:large`
    - Milestone: `Stability` (or the current default — ask if unsure)

11. Run `uv run mkdocs build --strict 2>&1 | tail -30` to verify the doc build succeeds with no new warnings beyond the known baseline.

## Test Strategy

N/A — no testable code changes. Verification is:
- `uv run mkdocs build --strict` succeeds (link integrity + no missing references).
- Visual review: every code example on the testing page compiles (mentally or via a quick `python -c` of key snippets).
- GitHub issue exists and has the correct title, labels, and milestone.

## Review Guidance

- **Blocker**: `hassette[test]` reappearing anywhere in the doc (it must not — that was fixed in the prior docs sync).
- **Blocker**: Any code example using the old `assert_called("call_service", service="turn_on", ...)` shape.
- **Blocker**: The `api.sync` Mock warning remaining in the doc — the new behavior is a recording facade, not a Mock.
- **Blocker**: The task_bucket false-idle warning remaining — the drain now handles depth-N chains.
- **Warning**: Missing `DrainError` mention in the task_bucket section — users need to know about the new exception.
- **Warning**: The follow-up issue lacking the interim safety net context — reviewers need to understand that drift is guarded, not open.
- **Warning**: Inline docstring examples not updated — source-level docs are the first thing devs see when exploring the code.
- Verify the doc examples are consistent with the canonical kwargs table in the design.
- Confirm that the follow-up issue has been filed and reference its URL in the commit message for this WP.

## Activity Log

- 2026-04-09T15:48:23Z — system — lane=doing — moved from planned
- 2026-04-09T15:54:08Z — system — lane=done — moved from doing
