# Plan Validation Report: Extract Shared DI Layer

## Verdict: **PASS**

Zero coverage gaps, zero contradictions, `context.md` is complete with all required sections. All FR/AC identifiers use the correct format. Spot-checks against the actual source tree confirm the task files' factual claims (import lines, docstring cross-refs, `PUBLIC_MODULES` entries, `XREF_MAP` entries) are accurate as of this validation.

---

## Requirement Inventory

**Total FRs: 15** — FR#1 through FR#15, all match `^FR#\d+$`.
**Total ACs: 8** — AC#1 through AC#8, all match `^AC#\d+$`.

No identifier format violations found.

---

## Traceability Matrix

| Requirement | Task(s) | Verify item present? |
|---|---|---|
| FR#1 (`di/` package public API) | T01 | Yes |
| FR#2 (`ParameterMatcher` Protocol) | T01 | Yes |
| FR#3 (`TypeMatcher`) | T01 | Yes |
| FR#4 (`AnnotatedMatcher`) | T01 | Yes |
| FR#5 (`build_injection_plan`) | T01 | Yes |
| FR#6 (`CallableInvoker`) | T01 | Yes |
| FR#7 (`validate_di_signature`) | T01 | Yes |
| FR#8 (`AnnotationDetails` moves, widened `T`, `source_type` field) | T02 | Yes |
| FR#9 (`identity` moves) | T02 | Yes |
| FR#10 (`ParameterInjector` refactor) | T03 | Yes |
| FR#11 (`extract_from_event_type` deleted) | T03 | Yes |
| FR#12 (`has_dependency_injection` deleted) | T03 | Yes |
| FR#13 (`extract_from_annotated` deleted) | T03 | Yes |
| FR#14 (`extract_from_signature` deleted) | T03 | Yes |
| FR#15 (`AnnotatedMatcher` source_type override) | T01 | Yes |
| AC#1 (existing DI test suites pass after import updates) | T04 | Yes |
| AC#2 (handler/bus integration tests pass unmodified) | T03 | Yes |
| AC#3 (`hassette.di` import resolves) | T01 | Yes |
| AC#4 (`bus/extraction.py` empty/deleted) | T03 | Yes |
| AC#5 (`D.*` aliases import from `hassette.di`) | T02 | Yes |
| AC#6 (standalone non-Event integration test) | T01 | Yes |
| AC#7 (standalone `AnnotatedMatcher` unit test) | T01 | Yes |
| AC#8 (Pyright clean on changed files) | T03 | Yes |

Every task's `implements:` frontmatter list matches its `## Verify` checklist exactly — no FR/AC is claimed in one and missing from the other, in either direction, for T01–T04.

Task dependency chain (T01 → T02 → T03 → T04, with T03/T04 also depending on earlier tasks) matches the order the design doc's Architecture and Replacement Targets sections imply: primitives first, then the `dependencies.py` swap, then the bus refactor, then the test/doc/tooling cleanup.

---

## Coverage Gaps

None. Every `create`/`modify`/`delete` entry in design.md's "Impact > Changed Files" section is claimed by exactly one task's Target Files list:

- All five new `di/` modules and their four new test files → T01
- `event_handling/dependencies.py` → T02
- `bus/injection.py`, `bus/extraction.py` (delete) → T03
- Four `tests/integration/test_*.py` files, docs pages, snippet file, and both `tools/docs/*.py` scripts → T04

`docs/pages/core-concepts/bus/handlers.md` is not in design.md's "Impact > Changed Files" list, but design.md carries an explicit gap-check annotation (`<!-- Gap check 2026-07-07: 1 gap included — docs/pages/core-concepts/bus/handlers.md:58 cross-ref link → T04 -->`) confirming this was already reconciled into T04. Verified: T04's Target Files and Prompt do include `handlers.md`, and the line-58 cross-reference (`[hassette.event_handling.dependencies]`) does exist in the file as described.

`tools/docs/check_xref_coverage.py` is also absent from the "Impact > Changed Files" list but is covered in the "Documentation Updates" section and correctly picked up in T04's Target Files and Prompt. Verified the actual `XREF_MAP["AnnotationDetails"]` entry exists at the location T04 describes.

---

## Contradictions

None found. Cross-checked task Prompt/Focus claims against the actual source tree:

- `bus/injection.py` currently imports `extract_from_signature` from `.extraction` and gates `Event` behind `TYPE_CHECKING` — matches T03's described starting state and required changes.
- `bus/listeners.py` imports `ParameterInjector` from `hassette.bus.injection` (not from `.extraction`) — confirms T03's Focus note that `listeners.py` needs no source change, even though design.md's Impact list optimistically marks it "modify." T03 lists it as `read`, and its Focus section explains why no modification is needed. This is design.md being cautious, not a task defect — not counted as a contradiction since the task's investigation and conclusion are internally consistent and verified correct.
- `bus/__init__.py` re-exports only `Bus`, `BusErrorContext`, `BusSyncFacade`, and listener dataclasses — no `extraction` or `injection` re-exports, confirming T03's Focus note ("expect no change needed").
- All four test files (`test_extraction.py`, `test_injection.py`, `test_annotation_conversion.py`, `test_type_detection.py`) import from `hassette.bus.extraction` exactly as T04 describes, with matching function names.
- `docs/pages/core-concepts/bus/dependency-injection.md` and `custom-extractors.md` both contain the `[hassette.event_handling.dependencies.AnnotationDetails]` cross-reference exactly where T04 says to update it.
- `tools/docs/gen_ref_pages.py:48` lists `"hassette.bus.extraction"` in `PUBLIC_MODULES` exactly as described.

No task prompt asserts something a sibling task contradicts. Field ordering, method signatures, and matcher construction order (`AnnotatedMatcher` before `TypeMatcher`) are stated consistently across design.md and T01/T03.

---

## context.md Completeness

All required sections present:
1. `## Problem & Motivation` — present, matches design.md's Problem section
2. `## Visual Artifacts` — present ("None") — correct, this is a backend-only refactor with no frontend/UI surface
3. `## Key Decisions` — present, 10 decisions, consistent with design.md
4. `## Constraints & Anti-Patterns` — present, matches design.md Non-Goals and Key Constraints
5. `## Design Doc References` — present
6. `## Convention Examples` (optional) — present

---

## Vague Verification Criteria

None. Every `## Verify` checklist item names a concrete, checkable condition (specific function/class behavior, specific import resolving, specific file containing/not containing code, specific test outcome) rather than a generic "works correctly" or "is implemented" placeholder.

---

## Visual Artifact Coverage

Not applicable. `context.md` correctly records "None" for Visual Artifacts — this design is a pure internal Python refactor (no `frontend/`, no `.tsx`/`.css` files touched, no doc screenshots affected). No task Focus section is expected to carry visual-artifact detail, and none do.

---

## Warnings (non-blocking)

1. **AC#8 attribution nuance.** Design.md's AC#8 text reads "Pyright reports no new errors on the changed files (`FR#8`)," tying the Pyright check specifically to FR#8 (the `AnnotationDetails` `T`-bound widening, implemented in T02). The task that actually claims AC#8 in its `implements`/`Verify` list is T03, not T02 — though T02's Focus section does separately instruct "Check that Pyright shows no new errors after this change." Since T03 runs after T02 in the dependency chain, a Pyright check at T03 would still catch any error T02 introduced, so this does not create an actual coverage gap — but the AC-to-task attribution doesn't literally match the FR the AC's parenthetical cites. Consider having T02 explicitly check Pyright too, or note in T03 that its Pyright verification is cumulative across T01–T03's changes (it already lists "changed files" without qualification, which reads as cumulative).

2. **design.md Impact list omissions.** Two files that tasks correctly touch (`docs/pages/core-concepts/bus/handlers.md`, `tools/docs/check_xref_coverage.py`) are missing from design.md's own "Impact > Changed Files" table, even though both are covered elsewhere in design.md (a gap-check annotation for the former, the "Documentation Updates" section for the latter) and both are correctly picked up by T04. This is an internal inconsistency within design.md's own sections, not a task-file defect — flagged for awareness only, no action needed on the tasks.
