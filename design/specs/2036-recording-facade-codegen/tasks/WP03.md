---
work_package_id: "WP03"
title: "Replace _RecordingSyncFacade with generator output and add runtime smoke test"
lane: "planned"
depends_on: ["WP01", "WP02"]
---

## Objectives & Success Criteria

Run the generator from WP02 to replace the hand-written `src/hassette/test_utils/sync_facade.py` with generated output. Diff-review the replacement for behavioral equivalence. Add the runtime coroutine + async-generator smoke test and the `Api`-vs-`RecordingApi` write-method parity test. **Do NOT yet delete the old drift test** — that happens in WP04 once the CI gate is in place.

Observable outcomes:

- `src/hassette/test_utils/sync_facade.py` is replaced by the output of `uv run tools/generate_sync_facade.py --target recording`.
- `git diff src/hassette/test_utils/sync_facade.py` (before vs after WP03) shows only the expected transformations: body-copy rewrites, consistent docstrings, constants-based stub messages, pinned class header. No behavioral changes beyond what the design prescribes.
- The class header in the new file exactly matches the "Class header template (pinned)" text in `design.md`.
- `uv run pytest tests/unit/test_recording_sync_facade.py -n 2 --dist loadscope -v` passes — the existing tests continue to work against the generated file.
- New test `test_body_copied_methods_are_sync` in `tests/unit/test_recording_sync_facade.py` passes — iterates every body-copied method on `_RecordingSyncFacade`, invokes with stub args, asserts each return value is not a `CoroutineType` or `AsyncGeneratorType`.
- New test `test_api_write_methods_covered_by_recording_api` in a new file `tests/unit/test_recording_api_write_parity.py` passes — compares the set of public write-method names on `Api` against the set on `RecordingApi`, fails if `Api` has any write method that `RecordingApi` lacks.
- The existing test `tests/unit/test_recording_sync_facade_drift.py` **still passes** against the generated file (because the generated file still has all the method names the drift test checks). This WP does not delete it — WP04 does.
- Updated message-string assertions in `tests/unit/test_recording_sync_facade.py` import and reference the `_STUB_MSG_*` constants from `tools.generate_sync_facade` instead of duplicating message substrings inline.
- `uv run pyright src/hassette/test_utils/sync_facade.py tests/unit/test_recording_sync_facade.py tests/unit/test_recording_api_write_parity.py` is clean.
- `uv run ruff check src/hassette/test_utils/sync_facade.py tests/unit/test_recording_sync_facade.py tests/unit/test_recording_api_write_parity.py` is clean.
- The previously-intentional `pyright: ignore[reportUnusedClass]` on `_RecordingSyncFacade` is preserved in the generator's class header template (if the generated file still needs it; otherwise dropped).

## Subtasks

1. **Run the generator**: `uv run tools/generate_sync_facade.py --target recording`. Confirm it writes `src/hassette/test_utils/sync_facade.py` without errors.
2. **Diff-review the replacement** (behavioral parity check from `design.md` → "Behavioral parity verification"). Run `git diff src/hassette/test_utils/sync_facade.py` and walk through the diff section by section:
   - For each body-copied method, confirm the new body is a verbatim transformation of the corresponding `RecordingApi` async method body: `self.X` → `self._parent.X`, `await` removed, docstring preserved.
   - For each NotImplementedError stub, confirm the message text matches the `_STUB_MSG_STATE_CONVERSION` or `_STUB_MSG_GENERIC` template with `{name}` filled in.
   - Confirm the class header matches the pinned template text in `design.md` exactly.
   - Confirm the import block is derived — no stale or missing imports.
   - If any difference is **not** explicable by these transformations, it is a generator bug. Escalate to WP02 for a fix before continuing.
3. **Import stub message constants in tests** (Finding 19): update `tests/unit/test_recording_sync_facade.py` tests that assert on error message substrings. Change from inline substring checks (e.g., `assert "Seed state via" in str(exc.value)`) to imports from `tools.generate_sync_facade`: `from tools.generate_sync_facade import _STUB_MSG_GENERIC, _STUB_MSG_STATE_CONVERSION` and assert using `.format(name=...)` matches. If `tools` is not importable from tests, either add an `__init__.py` or re-export the constants from `src/hassette/test_utils/sync_facade.py` as `_STUB_MSG_GENERIC = "..."` — but note this would be a divergence from the generator as source-of-truth, so prefer the direct import if possible.
4. **Add the runtime coroutine + async-generator smoke test** to `tests/unit/test_recording_sync_facade.py` as `test_body_copied_methods_are_sync`:
   - Construct a `RecordingApi` with a seeded `StateProxy` stub (use existing fixtures — check what the file already uses for similar setup).
   - Build `_RecordingSyncFacade(parent=recording_api)`.
   - Enumerate body-copied method names. Options for enumeration:
     - Import the list from the generator's output (if the generator emits a module-level tuple of generated method names)
     - Or hard-code the current body-copied method set in the test (turn_on, turn_off, toggle_service, call_service, set_state, fire_event, get_state, get_states, get_entity, get_entity_or_none, entity_exists, get_state_or_none) with a comment saying "keep in sync with generator"
   - For each method, invoke with stub args (e.g., `entity_id="sensor.test"`, `state="on"`, `event_type="foo"` — use the existing test fixtures where possible).
   - Assert:
     ```python
     if isinstance(result, (types.CoroutineType, types.AsyncGeneratorType)):
         if isinstance(result, types.CoroutineType):
             result.close()
         raise AssertionError(
             f"{method_name}() returned a {type(result).__name__} — "
             f"body-copy produced hidden async call"
         )
     ```
5. **Add `tests/unit/test_recording_api_write_parity.py`** (new file, Finding 14):
   - `test_api_write_methods_covered_by_recording_api`:
     - Build the set of public write-method names on `Api` (use `ApiProtocol` as the source, or introspect `Api` directly via `inspect.getmembers` filtered to `iscoroutinefunction`, excluding read-only methods by name pattern or a hardcoded known-read list).
     - Build the set of public async method names on `RecordingApi` via the same introspection.
     - Assert `write_methods_on_api - methods_on_recording_api == set()`. On failure, the assertion message should list the missing methods and instruct the developer to add them to `RecordingApi`.
   - This is a separate file so the deleted drift test doesn't take it down with it.
6. **Run all tests touching the sync facade**: `uv run pytest tests/unit/test_recording_sync_facade.py tests/unit/test_recording_api.py tests/unit/test_recording_api_write_parity.py tests/unit/test_recording_sync_facade_drift.py -n 2 --dist loadscope -v`. All must pass. The drift test is specifically expected to pass because the generated file still satisfies method-name parity.
7. **Run the full existing test suite on `test_utils`** to catch any regression: `uv run pytest tests/unit/test_recording*.py tests/integration/test_app_test_harness.py -n 2 --dist loadscope`. Fix any fallout from the file replacement.
8. **Do NOT delete `tests/unit/test_recording_sync_facade_drift.py`** in this WP. Do NOT modify pre-commit or lint.yml in this WP. Those belong to WP04.
9. **Verify the generator's `--check` mode now passes** against the committed file: `uv run tools/generate_sync_facade.py --target recording --check`. Should exit 0 — the committed file is byte-equal to fresh generator output.

## Test Strategy

**Unit tests in `tests/unit/test_recording_sync_facade.py`:**

- `test_body_copied_methods_are_sync` (NEW) — runtime coroutine/async-generator smoke test. Iterates every body-copied method, invokes with stub args, asserts non-coroutine return. Closes leaked coroutines via `result.close()` to suppress `RuntimeWarning`. Includes method name in failure message.
- Existing tests — **must continue to pass unchanged in behavior**. Message-string assertions are updated to reference `_STUB_MSG_*` constants but test the same conditions. If any existing test fails, the generator has produced a behaviorally-different file and must be fixed.

**Unit tests in `tests/unit/test_recording_api_write_parity.py`** (new file):

- `test_api_write_methods_covered_by_recording_api` — compares `Api`'s public write-method set against `RecordingApi`'s method set. Fails if `Api` has a write method `RecordingApi` lacks.

**Unit tests in `tests/unit/test_recording_sync_facade_drift.py`:**

- **Must still pass** against the generated file. The generator emits every name on `ApiSyncFacade`, so the drift test's method-name parity check should naturally succeed. If it fails, the generator is missing methods — fix WP02 before continuing.

**Existing tests in `tests/unit/test_recording_api.py`:**

- Must continue to pass unchanged. WP01's refactor + WP03's file replacement should not affect them.

**Exclusions:**

- No generator changes in this WP (those are in WP02).
- No drift test deletion, no pre-commit hook changes, no CI changes — all in WP04.

## Review Guidance

**Design constraints to verify:**

- The generated class header in the committed `sync_facade.py` exactly matches the "Class header template (pinned)" text in `design.md`. Diff the two literally. Any wording change is a blocker — either update the template in the design doc (with justification) or fix the generator. Blocker.
- All body-copied methods in the generated file contain **no** `await` keywords and **no** `async def`. If any survive, either WP02's rewriter is broken or a `RecordingApi` method slipped through without the WP01 refactor being applied. Blocker.
- The runtime smoke test (`test_body_copied_methods_are_sync`) actually exercises every body-copied method — not just turn_on. The reviewer should count the methods invoked in the test and compare against the body-copied method list in the generated file. Blocker if the count is short.
- The smoke test's failure message includes the method name. A bare `assert not True` is a Finding 21 regression. Blocker.
- The smoke test closes leaked coroutines via `result.close()`. A failing test that leaks coroutines will spam `RuntimeWarning` — Finding 21 regression. Blocker.
- The smoke test checks **both** `CoroutineType` and `AsyncGeneratorType`. Missing either is a Finding 18 regression. Blocker.
- The Api-vs-RecordingApi parity test lives in a **separate file** from the old drift test, so WP04's deletion does not take it down. Blocker if it's in `test_recording_sync_facade_drift.py`.
- Message-string assertions in `test_recording_sync_facade.py` import `_STUB_MSG_*` constants rather than duplicating substrings. If the reviewer sees inline substring assertions after this WP, that's a Finding 19 regression. Blocker for new tests; warning for any pre-existing inline assertions the WP left alone.

**Behavioral parity check (the manual one-shot from `design.md`):**

- The reviewer must walk the `git diff` of `src/hassette/test_utils/sync_facade.py` before/after WP03 and confirm every difference is either (a) a body-copy transformation, (b) a docstring preservation, (c) the pinned class header template, or (d) derived imports. Any other difference is a potential behavioral change and must be justified or fixed. This is a BLOCKER — the WP cannot merge without this review.

**Warning-level deviations (note but not block):**

- Whether the smoke test enumerates method names via introspection or a hardcoded list. Both work; the design prefers introspection if feasible.
- Whether the write-method parity test uses `ApiProtocol` or direct `Api` introspection. Both work.

**Run these before approving:**

- `uv run pytest tests/unit/test_recording_sync_facade.py tests/unit/test_recording_api.py tests/unit/test_recording_api_write_parity.py tests/unit/test_recording_sync_facade_drift.py -n 2 --dist loadscope -v`
- `uv run tools/generate_sync_facade.py --target recording --check` — must exit 0
- `uv run pyright src/hassette/test_utils/sync_facade.py tests/unit/test_recording_sync_facade.py tests/unit/test_recording_api_write_parity.py`
- `uv run ruff check src/hassette/test_utils/sync_facade.py tests/unit/test_recording_sync_facade.py tests/unit/test_recording_api_write_parity.py`
