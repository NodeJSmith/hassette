## Verdict: FAIL

> 1 contradiction found: T03's Prompt code implements the exact `DEMO_*=` stdout key-value protocol that design.md's Replacement Targets table explicitly marks for removal, and that conflicts with FR#10's "human-readable URLs" requirement. Coverage is otherwise complete (all 10 FRs and 7 ACs are implemented and verified), context.md is complete, and no identifier format violations exist — but the contradiction and its downstream implications must be resolved before execution.

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1  | T01 | "`docker compose -f scripts/docker/ha-demo.yml up -d --wait` starts all three services and exits 0 (requires setting `HA_CONFIG_PATH` to a valid HA config dir first)" |
| FR#2  | T02 | "After `DemoStack.__exit__`, `docker ps --filter \"label=com.docker.compose.project=hassette-demo\"` returns no containers" |
| FR#3  | T01 | "`DEMO_HA_PORT=29123 DEMO_HASSETTE_PORT=29126 DEMO_VITE_PORT=29173 docker compose ... up -d --wait` starts on the overridden ports" |
| FR#4  | T01 | "`docker compose -f scripts/docker/ha-demo.yml ps` shows all three services as healthy" |
| FR#5  | T02 | "HA fixture config is copied to a fresh tmpdir on each start (verify tmpdir path exists during `__enter__`, is cleaned up after `__exit__`)" |
| FR#6  | T02 | "`.demo-data/` directory exists on the host after a demo run and contains `hassette.db`" |
| FR#7  | T01 | "With the stack running, editing a `.tsx` file triggers HMR reload in the browser" |
| FR#8  | T01 | "All three ports are bound to `127.0.0.1` (verify with `docker compose port` or `ss -tlnp`)" |
| FR#9  | T04 | "`capture_screenshots.py` starts and stops the demo stack by importing DemoStack directly — no `hassette_demo.py` subprocess is spawned" |
| FR#10 | T03 | "Running `uv run python scripts/hassette_demo.py` prints human-readable URLs after startup, blocks until Ctrl-C, then tears down" |
| AC#1  | T03 | "All three services start and URLs are printed within 120s" |
| AC#2  | T03 | "Ctrl-C during any phase leaves no orphaned containers (`docker ps` shows none with `hassette-demo` project)" |
| AC#3  | T04 | "`uv run python scripts/capture_screenshots.py` produces the same screenshot files as the current script for the same manifest and demo state" |
| AC#4  | T03 | "Editing a `.tsx` file while the demo runs triggers HMR reload without a full page refresh" |
| AC#5  | T03 | "All three demo ports are reachable via SSH tunnel from another machine" |
| AC#6  | T03 | "`DEMO_HA_PORT=29129 DEMO_HASSETTE_PORT=29126 DEMO_VITE_PORT=29173 uv run python scripts/hassette_demo.py` starts on overridden ports" |
| AC#7  | T05 | "Combined line count of `hassette_demo.py` + `capture_screenshots.py` + `demo_stack.py` is under 250 lines (`wc -l scripts/hassette_demo.py scripts/capture_screenshots.py scripts/demo_stack.py`)" |

Total FRs: 10 (FR#1–FR#10), all mapped to exactly one task, each with a matching Verify item.
Total ACs: 7 (AC#1–AC#7), all mapped to exactly one task, each with a matching Verify item.

## Coverage Gaps

None. Every FR#N and AC#N from design.md appears in exactly one task's `implements` field, and every one has a corresponding `- [ ] FR#N:` / `- [ ] AC#N:` item in that task's Verify section.

## Contradictions

- **T03 Prompt** vs. **design.md Replacement Targets table and FR#10**: T03's Prompt gives this exact code to implement:
  ```python
  print(f"DEMO_HA_URL=http://localhost:{demo.ha_port}", flush=True)
  print(f"DEMO_HASSETTE_URL=http://localhost:{demo.hassette_port}", flush=True)
  print(f"DEMO_FRONTEND_URL=http://localhost:{demo.vite_port}", flush=True)
  print("DEMO_READY=true", flush=True)
  ```
  and states: *"Keep the `DEMO_*=` key-value output lines for now — they are consumed by `demo-verify` and agent skills."*

  This is the literal `DEMO_*=` stdout key-value protocol that design.md's Replacement Targets table explicitly marks for elimination: *"`DEMO_*=` stdout key-value protocol | Human-readable print statements + fixed ports | Remove"*. It also conflicts with **FR#10** — *"`hassette_demo.py` prints human-readable URLs after startup"* — and with the User Scenario step: *"Then: human-readable URLs printed when all services healthy."* Design.md's own `DemoStack` code sample models the intended output as `print(f"Frontend: http://localhost:{demo.vite_port}")`, not a `KEY=value` line. T03's own Verify item for FR#10 ("prints human-readable URLs after startup") is therefore not satisfied by the Prompt's own code — the task is internally inconsistent as well as inconsistent with the design doc.

## Warnings

- **T03's stated justification for keeping `DEMO_*=` does not hold once T05 is read.** T03 says the lines are kept because they're "consumed by `demo-verify` and agent skills," and that "T05 will update those consumers to use fixed ports, at which point these lines become human-readable only." But: (1) T05's Target Files list does **not** include `scripts/hassette_demo.py`, so no task ever converts these lines to human-readable format — the legacy protocol output is permanent under the plan as written. (2) T05's actual `demo-verify` rewrite polls `http://localhost:18126/api/health` and `/api/apps` directly rather than parsing `hassette_demo.py`'s stdout, and the `ui-qa` skill update (harness.md, SKILL.md) also moves to fixed URLs instead of stdout parsing — so by the time T05 lands, nothing actually depends on the `DEMO_*=` lines anymore, and they become inert dead code that no task removes. Recommend either: have T03 print the human-readable format directly (since FR#3 fixed ports mean no consumer needs runtime discovery), or add an explicit task/step that strips the legacy print lines once T05's consumers are updated.

- **design.md's Documentation Updates section names a file no task addresses.** Design.md states: *"`design/specs/048-visual-qa-environment/design.md` — Update if it references the demo protocol (read and verify during implementation)."* This file does not appear in T05's Target Files (CLAUDE.md, `.claude/skills/ui-qa/references/harness.md`, `.claude/skills/ui-qa/SKILL.md`, `scripts/README.md`, `.mise/tasks/demo-verify`, `tools/frontend/ui_qa_capture.py`) or in any other task's Target Files. This isn't tied to an FR/AC identifier so it doesn't register as a formal coverage gap, but it is an explicit design.md instruction with no task ownership — it will be silently skipped unless picked up during execution.

- **Ambiguous ownership of the "Unix-only" platform check.** T02's Focus section says *"Unix-only: the current script rejects Windows at startup. Preserve this check,"* implying `DemoStack` (or something) should retain it, but T02's Prompt (`__enter__`/`__exit__` steps) never actually includes a platform check — the only `sys.platform == "win32"` guard shown anywhere in the tasks is in T03's `hassette_demo.py` example. T04 (`capture_screenshots.py`) has no platform check at all in its Prompt or Focus, and no visibility into whether the current script has one to preserve. Low-severity, but worth a one-line clarification on whether the check belongs in `DemoStack.__enter__` (so both consumer scripts inherit it) or is intentionally only in `hassette_demo.py`.

## context.md Completeness

All required sections present with non-empty content: `## Problem & Motivation`, `## Visual Artifacts` ("None."), `## Key Decisions`, `## Constraints & Anti-Patterns`, `## Design Doc References`. Optional `## Convention Examples` is present with "None — no convention examples captured during discovery." No missing or empty sections.

## Identifier Format Violations

None. All identifiers in design.md and all task frontmatter/Verify sections match `^FR#\d+$` / `^AC#\d+$` (confirmed via regex scan across `design.md` and `tasks/T*.md`).
