# WP01: Schema migration and registration cleanup

**Fixes:** 1 (DB telemetry collapse / dead upserts)
**Lane:** todo
**Depends on:** nothing
**Blocks:** WP02 (tests reference new schema)

## Summary

Write migration 004 to clean up both registration tables, then update the registration code paths to use plain INSERT.

## Acceptance Criteria

- [ ] Migration `004_drop_upsert_columns.py` drops `first_registered_at` and `last_registered_at` from both `listeners` and `scheduled_jobs` tables
- [ ] Migration drops `UNIQUE` constraints from both tables
- [ ] Migration uses the SQLite table-rebuild pattern from migration 003
- [ ] `_do_register_listener()` uses plain `INSERT ... RETURNING id` (no `ON CONFLICT`)
- [ ] `_do_register_job()` uses plain `INSERT ... RETURNING id` (no `ON CONFLICT`)
- [ ] `ListenerRegistration` dataclass: `first_registered_at` and `last_registered_at` removed
- [ ] `ScheduledJobRegistration` dataclass: `first_registered_at` and `last_registered_at` removed
- [ ] `bus_service.py` call site: stops passing timestamp fields to `ListenerRegistration`
- [ ] `scheduler_service.py` call site: stops passing timestamp fields to `ScheduledJobRegistration`
- [ ] Existing tests pass (update any that reference removed fields/columns)

## Files to modify

| File | Change |
|------|--------|
| `src/hassette/migrations/versions/004_drop_upsert_columns.py` | New — table rebuild for both tables |
| `src/hassette/core/command_executor.py` | Replace upsert with plain INSERT in both `_do_register_listener` and `_do_register_job` |
| `src/hassette/core/registration.py` | Remove timestamp fields from both dataclasses |
| `src/hassette/core/bus_service.py` | Stop passing `first_registered_at`/`last_registered_at` |
| `src/hassette/core/scheduler_service.py` | Stop passing `first_registered_at`/`last_registered_at` |

## Regression tests

- Register two listeners with same handler+topic but different predicates → verify two distinct DB rows
- Verify both tables lack `first_registered_at`/`last_registered_at` columns after migration
- Verify job registration uses plain INSERT (no ON CONFLICT behavior)

## Implementation notes

- Reference migration 003 for the SQLite table-rebuild pattern (SQLite cannot drop columns or constraints in-place; must CREATE new table, copy data, DROP old, RENAME)
- The `_do_register_listener` docstring references ON CONFLICT — update it
- The `_do_register_job` docstring references ON CONFLICT — update it
