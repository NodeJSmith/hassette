# WP02: Telemetry query fixes and dashboard error data

**Fixes:** 2 (dashboard errors wrong IDs), 7 (graceful degradation)
**Lane:** todo
**Depends on:** WP01 (migration must be applied first for schema consistency)
**Blocks:** nothing

## Summary

Fix the two independent bugs in `get_recent_errors()` (missing SQL columns + wrong dict key), rename `timestamp` → `execution_start_ts` across all layers, and add specific exception handling to dashboard endpoints.

## Acceptance Criteria

### Fix 2: Dashboard errors

- [ ] `get_recent_errors()` session-scoped handler query: adds `hi.listener_id` to SELECT
- [ ] `get_recent_errors()` session-scoped job query: adds `je.job_id` to SELECT
- [ ] `get_recent_errors()` session-scoped handler query: adds `AND hi.listener_id IS NOT NULL` filter
- [ ] `get_recent_errors()` all-sessions handler query: adds `hi.listener_id` to SELECT (if missing)
- [ ] `get_recent_errors()` all-sessions job query: adds `je.job_id` to SELECT (if missing)
- [ ] `HandlerErrorEntry.timestamp` renamed to `execution_start_ts` in Pydantic model
- [ ] `JobErrorEntry.timestamp` renamed to `execution_start_ts` in Pydantic model
- [ ] `dashboard_errors()` route: reads `err.get("execution_start_ts", 0.0)` in both branches
- [ ] `frontend/src/api/endpoints.ts`: `DashboardErrorEntry.timestamp` renamed to `execution_start_ts`
- [ ] `frontend/src/components/dashboard/error-feed.tsx`: all `.timestamp` references updated to `.execution_start_ts`
- [ ] Old mock-based test updated or replaced with real-SQL integration test

### Fix 7: Graceful degradation

- [ ] `dashboard_kpis()`: wrapped in `try/except sqlite3.Error` with zeroed fallback + warning log
- [ ] `dashboard_errors()`: wrapped in `try/except sqlite3.Error` with empty fallback + warning log
- [ ] `dashboard_app_grid()`: existing `except Exception` narrowed to `except sqlite3.Error`
- [ ] `import sqlite3` added to telemetry routes module

## Files to modify

| File | Change |
|------|--------|
| `src/hassette/core/telemetry_query_service.py` | Add missing columns to SELECT in `get_recent_errors()` both branches; add NULL filter |
| `src/hassette/web/routes/telemetry.py` | Fix dict key reads in `dashboard_errors()`; add try/except to KPIs and errors; narrow app_grid exception |
| `src/hassette/web/models.py` | Rename `timestamp` → `execution_start_ts` on `HandlerErrorEntry` and `JobErrorEntry` |
| `frontend/src/api/endpoints.ts` | Rename `timestamp` → `execution_start_ts` on `DashboardErrorEntry` |
| `frontend/src/components/dashboard/error-feed.tsx` | Update `.timestamp` references |
| `tests/integration/test_web_api.py` | Fix or replace mock-based dashboard errors test |

## Regression tests

- Real-SQL integration test: trigger handler and job errors, call `GET /telemetry/dashboard/errors`, assert `listener_id` > 0, `job_id` > 0, `execution_start_ts` is realistic epoch-seconds
- Mock `sqlite3.Error` from telemetry service → verify all three dashboard endpoints return 200 with zeroed/empty data
- Verify `TypeError` from telemetry service still propagates as 500

## Implementation notes

- Check both branches of `get_recent_errors()` — session-scoped (identifiable by `WHERE ... AND hi.session_id = ?`) and all-sessions (no session_id filter)
- The all-sessions branch already has `AND hi.listener_id IS NOT NULL`; verify session-scoped branch gets it too
- Frontend error-feed component uses `.timestamp` for `useRelativeTime()` hook and `errorEntryKey()` — both need updating
- Check for any error-feed test files that reference `.timestamp`
