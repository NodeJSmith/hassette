---
work_package_id: "WP01"
title: "Remove callable_name LRU cache, cache handler_name as field, remove dead code"
lane: "planned"
plan_section: "1. Remove callable_name LRU cache and cache result as field"
depends_on: []
---

## Objectives & Success Criteria

- `callable_name()` in `src/hassette/utils/func_utils.py` has no `@lru_cache` decorator
- `callable_name()` has a docstring warning about per-call `inspect.unwrap()` overhead
- `Listener.handler_name` is a `str` field (not a property), computed once in `Listener.create()`
- `Listener.handler_short_name` is a `str` field computed from `handler_name` in `Listener.create()`
- `callable_name(handler)` is called exactly once in `Listener.create()` and the result is reused for ParameterInjector, handler_name field, and RateLimiter
- Dead code removed: `T = TypeVar("T", covariant=True)` from `bus/bus.py:106`, `T = TypeVar("T")` from `scheduler/classes.py:21`, `self.listener_seq = itertools.count(1)` from `bus_service.py:58` (and `import itertools` if unused), `_get_matching_listeners` from `bus_service.py:232-235`
- ID generation comments added to `bus/listeners.py` and `scheduler/classes.py` documenting the single-threaded creation invariant
- All existing tests pass

## Subtasks

1. Remove `@lru_cache(maxsize=1024)` from `callable_name()` in `src/hassette/utils/func_utils.py:49`
2. Add docstring warning to `callable_name()`: "Performs `inspect.unwrap()` on every call. Cache the result at construction time rather than calling in hot paths."
3. In `src/hassette/bus/listeners.py`, change `handler_name` from a `@property` (lines 91-93) to a `str` field on the dataclass
4. In `src/hassette/bus/listeners.py`, change `handler_short_name` from a `@property` (lines 95-97) to a `str` field on the dataclass
5. In `Listener.create()`, compute `name = callable_name(handler)` once at the top, pass it to `ParameterInjector` (currently line 193), the `handler_name` constructor arg, and `RateLimiter` (currently line 218)
6. Remove `T = TypeVar("T", covariant=True)` from `src/hassette/bus/bus.py:106` and its `TypeVar` import if unused
7. Remove `T = TypeVar("T")` from `src/hassette/scheduler/classes.py:21` and its `TypeVar` import from line 6 if unused
8. Remove `self.listener_seq = itertools.count(1)` from `src/hassette/core/bus_service.py:58` and `import itertools` if unused
9. Remove `_get_matching_listeners` method from `src/hassette/core/bus_service.py:232-235`
10. Add single-threaded invariant comments above `seq = itertools.count(1)` in both `src/hassette/bus/listeners.py:23` and `src/hassette/scheduler/classes.py:19`

## Test Strategy

In `tests/unit/test_func_utils.py`:
- `test_callable_name_no_lru_cache` — verify `callable_name` is not decorated with `lru_cache` (check `hasattr(callable_name, 'cache_info')` is False)

Existing tests in `tests/unit/test_func_utils.py` and `tests/integration/test_listeners.py` must continue to pass — they exercise `callable_name`, `handler_name`, and `handler_short_name` without depending on caching behavior.

## Review Guidance

- **BLOCKER**: `callable_name()` must have NO `@lru_cache` decorator remaining
- **BLOCKER**: `handler_name` and `handler_short_name` must be fields, not properties — check that `Listener` dataclass definition lists them as `str` fields
- **BLOCKER**: `callable_name(handler)` must be called exactly ONCE in `Listener.create()` — grep for `callable_name` in the method body; should appear once
- **WARNING**: Dead code removal must not remove anything still referenced — grep for each removed symbol across the entire `src/` tree
- Verify `import itertools` is removed from `bus_service.py` only if no other usage remains

## Verification Command

```bash
uv run nox -s dev
```

## Activity Log

- 2026-03-28T20:00:00Z — system — lane=planned — WP created
