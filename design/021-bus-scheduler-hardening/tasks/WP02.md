---
work_package_id: "WP02"
title: "Make Listener.matches() synchronous"
lane: "planned"
plan_section: "4. Synchronous Listener.matches()"
depends_on: []
---

## Objectives & Success Criteria

- `Listener.matches()` in `src/hassette/bus/listeners.py` is a synchronous method (no `async` keyword)
- The 2 call sites in `src/hassette/core/bus_service.py` use `listener.matches(ev)` without `await`
- All existing tests pass
- No predicate in the codebase is async (verified by the existing `TypeError` guard in `predicates.py:499-509`)

## Subtasks

1. In `src/hassette/bus/listeners.py:142`, remove `async` from `matches()` method signature — change `async def matches(self, ev: "Event[Any]") -> bool:` to `def matches(self, ev: "Event[Any]") -> bool:`
2. In `src/hassette/core/bus_service.py:190`, change `if await listener.matches(event):` to `if listener.matches(event):`
3. In `src/hassette/core/bus_service.py:235`, change `if await listener.matches(event):` to `if listener.matches(event):`

## Test Strategy

No new tests needed — this is a mechanical removal of `async`/`await` with no behavioral change. Existing tests in `tests/integration/test_bus.py` and `tests/integration/test_listeners.py` exercise the predicate matching path.

Verify the predicate system rejects async predicates: `tests/integration/test_listeners.py` should already have tests for this (the `TypeError` guard at `predicates.py:499-509`).

## Review Guidance

- **BLOCKER**: `matches()` must NOT be `async` — check the method signature
- **BLOCKER**: No `await` before `listener.matches(` anywhere in the codebase — `grep -r "await.*matches(" src/`
- **WARNING**: If any test mocks `matches()` as a coroutine, it must be updated to return a plain `bool`

## Verification Command

```bash
uv run nox -s dev
```

## Activity Log

- 2026-03-28T20:00:00Z — system — lane=planned — WP created
