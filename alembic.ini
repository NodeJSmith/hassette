# Alembic configuration — development convenience only.
# DatabaseService configures Alembic programmatically at runtime.

[alembic]
script_location = src/hassette/migrations
sqlalchemy.url = sqlite:///hassette.db
