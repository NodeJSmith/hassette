/** Shared field-label styling for detail panes. Per-site layout classes stay inline at the call site. */
export const FIELD_LABEL_CLASS =
  "font-mono text-xs uppercase tracking-[var(--text-label-tracking)] text-foreground-faint";
